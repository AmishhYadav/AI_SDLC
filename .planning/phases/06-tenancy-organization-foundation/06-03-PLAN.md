---
phase: 06-tenancy-organization-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/backend/src/organization/api/dto/create-organization.dto.ts
  - packages/backend/src/organization/api/dto/organization-response.dto.ts
  - packages/backend/src/organization/api/dto/add-member.dto.ts
  - packages/backend/src/organization/api/dto/member-response.dto.ts
  - packages/backend/src/organization/persistence/organization.repository.ts
  - packages/backend/src/organization/persistence/member.repository.ts
  - packages/backend/src/organization/application/organization.service.ts
  - packages/backend/src/organization/application/member.service.ts
  - packages/backend/src/organization/application/member.service.spec.ts
  - packages/backend/src/organization/application/organization.service.spec.ts
autonomous: true
requirements:
  - TENANT-03
  - TENANT-04
  - TENANT-05

must_haves:
  truths:
    - "OrganizationService.createOrganization(creatorEmail, dto) resolves the platform User by email (raw PrismaService, deletedAt:null), throws TENANT.USER_NOT_FOUND if absent, then creates Organization + ACTIVE OrganizationMember with joinedAt=now() atomically via $transaction (D-10)"
    - "OrganizationService.listMyOrgs(creatorEmail) resolves the User by email then returns their orgs via raw PrismaService (cross-org by design; @NoTenantScope route with no CLS userId; Organization has no organizationId FK)"
    - "OrganizationService.findById(id) validates that id matches CLS organizationId; throws TENANT.ORG_ACCESS_DENIED if mismatch (IDOR prevention)"
    - "MemberService.addMember() finds User by email via raw PrismaService; throws TENANT.USER_NOT_FOUND if not found; never creates User rows (D-13)"
    - "MemberService.addMember() calls upsertMember via raw PrismaService (not TenantedPrismaService) — upsert.where bypasses extension to avoid A3 conflict"
    - "MemberService.removeMember() counts ACTIVE members first; throws TENANT.LAST_MEMBER_REMOVAL if count <= 1 (D-15)"
    - "MemberService.removeMember() soft-deletes via MemberRepository.softDelete(): sets status=REMOVED, deletedAt, deletedBy from CLS userId (D-14)"
    - "MemberRepository.findManyByOrg() uses TenantedPrismaService.client — extension auto-injects organizationId + deletedAt:null (D-06)"
    - "Re-add: upsertMember with existing soft-removed row reactivates it (status=ACTIVE, deletedAt=null, joinedAt=now()) via upsert.update path (D-14)"
  artifacts:
    - path: "packages/backend/src/organization/application/organization.service.ts"
      provides: "createOrganization (email→User resolve + $transaction), listMyOrgs (email→User + unscoped), findById (id-matches-ctx IDOR guard)"
      contains: "class OrganizationService"
    - path: "packages/backend/src/organization/application/member.service.ts"
      provides: "addMember (user lookup + upsert), listMembers (scoped), removeMember (last-member guard + soft-delete)"
      contains: "class MemberService"
    - path: "packages/backend/src/organization/persistence/organization.repository.ts"
      provides: "OrganizationRepository using raw PrismaService (Organization has no organizationId FK)"
      contains: "class OrganizationRepository"
    - path: "packages/backend/src/organization/persistence/member.repository.ts"
      provides: "MemberRepository extends BaseRepository; upsertMember uses raw PrismaService"
      contains: "extends BaseRepository"
    - path: "packages/backend/src/organization/application/member.service.spec.ts"
      provides: "5 unit tests covering guardrail, re-add, user-not-found, list, soft-delete behaviors"
      exports: []
    - path: "packages/backend/src/organization/application/organization.service.spec.ts"
      provides: "4 unit tests: create USER_NOT_FOUND, atomic creator-as-ACTIVE-member (D-10), findById IDOR guard (T-06-12), findById happy path"
      exports: []
  key_links:
    - from: "OrganizationService.createOrganization() / listMyOrgs()"
      to: "PrismaService.user.findFirst({ where: { email, deletedAt: null } })"
      via: "resolve platform User.id from the authenticated email (@NoTenantScope route has no CLS userId)"
      pattern: "user\\.findFirst"
    - from: "OrganizationService.createOrganization()"
      to: "PrismaService.$transaction(tx => tx.organization.create + tx.organizationMember.create)"
      via: "raw $transaction client (extension does NOT propagate into interactive tx per RESEARCH A2)"
      pattern: "\\$transaction.*tx"
    - from: "MemberRepository.upsertMember()"
      to: "PrismaService (raw, injected separately from BaseRepository.scopedPrisma)"
      via: "raw upsert to avoid extension A3 conflict on upsert.where"
      pattern: "prisma\\.organizationMember\\.upsert"
    - from: "MemberRepository.findManyByOrg()"
      to: "TenantedPrismaService.client.organizationMember.findMany()"
      via: "extension auto-injects organizationId + deletedAt:null"
      pattern: "scopedPrisma\\.client\\.organizationMember\\.findMany"
---

<objective>
Build the organization data layer: DTOs, OrganizationRepository (raw PrismaService), MemberRepository (extends BaseRepository), OrganizationService, MemberService, and unit tests for MemberService business rules.

Purpose: This plan delivers the full CRUD business logic and persistence layer for TENANT-03/04/05. Runs in Wave 2 in parallel with Plan 06-02 (TenantGuard) — no file overlap. The controller and AppModule wiring come in Plan 06-04 (Wave 3). Three critical design constraints: (1) OrganizationRepository uses raw PrismaService because Organization has no organizationId FK; (2) MemberRepository.upsertMember uses raw PrismaService to avoid RESEARCH Open Question A3 (upsert.where + extension injection conflict); (3) createOrganization/listMyOrgs run on @NoTenantScope routes (Plan 06-04) where CLS has no userId, so OrganizationService resolves the platform User from the authenticated email — the service (not the controller) owns this lookup per CLAUDE.md §6.

Output: 10 files across packages/backend/src/organization/ (new directory tree). No schema changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-tenancy-organization-foundation/06-CONTEXT.md
@.planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md
@.planning/phases/06-tenancy-organization-foundation/06-PATTERNS.md
@.planning/phases/06-tenancy-organization-foundation/06-01-SUMMARY.md

<interfaces>
<!-- From packages/backend/src/common/pagination/cursor-pagination.dto.ts (DTO scaffold pattern) -->
<!-- import { IsInt, IsOptional, IsString, Max, Min } from 'class-validator'; -->
<!-- import { Type } from 'class-transformer'; -->
<!-- @IsOptional() @IsString() cursor?: string; -->

<!-- From packages/backend/src/tenancy/base-repository.ts (created 06-01) -->
<!-- abstract class BaseRepository { -->
<!--   constructor(protected readonly scopedPrisma: TenantedPrismaService, protected readonly cls: ClsService) {} -->
<!--   protected getSoftDeleteData(): { deletedAt: Date; deletedBy: string | null } -->
<!-- } -->

<!-- From packages/backend/src/tenancy/tenant-context.service.ts (created 06-01) -->
<!-- class TenantContextService { getOrganizationId(): string | undefined; getUserId(): string | undefined; getOrganizationMemberId(): string | undefined; } -->

<!-- From packages/database/prisma/schema/organization.prisma -->
<!-- Organization: id, name(@@unique), slug(@unique), status, createdBy, deletedAt, deletedBy -->
<!-- OrganizationMember: id, organizationId, userId, status (INVITED/ACTIVE/SUSPENDED/REMOVED), joinedAt?, invitedAt, createdBy, deletedAt, deletedBy; @@unique([organizationId, userId]) -->

<!-- From packages/database/prisma/schema/identity.prisma -->
<!-- User: id, email(@unique), firstName, status, deletedAt -->

<!-- From packages/backend/src/authorization/permission-resolver.service.ts (Prisma query style + service scaffold) -->
<!-- const user = await this.prisma.user.findFirst({ where: { email, deletedAt: null }, select: { ... } }); -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DTOs + OrganizationRepository + MemberRepository</name>
  <files>
    packages/backend/src/organization/api/dto/create-organization.dto.ts,
    packages/backend/src/organization/api/dto/organization-response.dto.ts,
    packages/backend/src/organization/api/dto/add-member.dto.ts,
    packages/backend/src/organization/api/dto/member-response.dto.ts,
    packages/backend/src/organization/persistence/organization.repository.ts,
    packages/backend/src/organization/persistence/member.repository.ts
  </files>
  <read_first>
    - packages/backend/src/common/pagination/cursor-pagination.dto.ts (DTO class-validator + class-transformer scaffold — lines 1-27; import pattern, decorator stacking, @IsOptional)
    - .planning/phases/06-tenancy-organization-foundation/06-PATTERNS.md (DTO section — CreateOrganizationDto field validators; add-member section — @IsEmail; repository sections for OrganizationRepository and MemberRepository)
    - .planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md Pattern 4 (Unscoped Operations — OrganizationRepository.create/findById/findByMemberUserId use raw PrismaService; list of all legitimate unscoped operations)
    - .planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md Pattern 3 (BaseRepository pattern — MemberRepository.findManyByOrg, softDelete; the upsert MUST use raw PrismaService separately)
    - .planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md Open Question A3 (upsert.where extension conflict — MemberRepository.upsertMember must inject raw PrismaService, not use scopedPrisma.client)
    - packages/database/prisma/schema/organization.prisma (Organization fields for create data; OrganizationMember @@unique([organizationId, userId]) for upsert.where field 'organizationId_userId')
    - packages/backend/src/tenancy/base-repository.ts (just written — exact abstract class interface to extend)
  </read_first>
  <action>
    Create directory tree: packages/backend/src/organization/api/dto/, packages/backend/src/organization/persistence/.

    DTOs — follow cursor-pagination.dto.ts scaffold exactly (class-validator decorators on every field):

    create-organization.dto.ts: class CreateOrganizationDto with @IsString() @MinLength(2) @MaxLength(100) name: string; @IsString() @IsAlphanumeric() (or @Matches(/^[a-z0-9-]+$/)) @MinLength(2) @MaxLength(60) slug: string. Import IsString, MinLength, MaxLength (+ IsAlphanumeric or Matches) from 'class-validator'. The global ValidationPipe (whitelist:true) strips unknown fields automatically.

    organization-response.dto.ts: plain class OrganizationResponseDto with typed properties: id: string, name: string, slug: string, status: string, createdAt: Date, createdBy: string | null. No validation decorators (server-produced). No deletedAt/deletedBy exposure (sensitive).

    add-member.dto.ts: class AddMemberDto with @IsEmail() email: string. Import IsEmail from 'class-validator'.

    member-response.dto.ts: plain class MemberResponseDto with id: string, organizationId: string, userId: string, status: string, joinedAt: Date | null, createdAt: Date. No deletedAt/deletedBy (sensitive fields).

    ---

    organization.repository.ts: @Injectable() class OrganizationRepository. Constructor takes only PrismaService (private readonly prisma: PrismaService) — RAW, unscoped. Organization has no organizationId FK; it IS the root entity. Three methods:
    - create(data: { name: string; slug: string; createdBy: string }): Promise<Organization> — this.prisma.organization.create({ data })
    - findById(id: string): Promise<Organization | null> — this.prisma.organization.findUnique({ where: { id } }) (Organization is root; no org-scope needed; findUnique is safe here because we query by the PK @id, not injecting orgId)
    - findByMemberUserId(userId: string): Promise<Organization[]> — this.prisma.organization.findMany({ where: { members: { some: { userId, status: 'ACTIVE', deletedAt: null } }, deletedAt: null } })

    member.repository.ts: @Injectable() class MemberRepository extends BaseRepository. Constructor takes TenantedPrismaService AND raw PrismaService (for upsert). Call super(scopedPrisma, cls) from BaseRepository. Four methods:
    - findManyByOrg(): Promise<OrganizationMember[]> — this.scopedPrisma.client.organizationMember.findMany() — extension auto-injects organizationId + deletedAt:null
    - findById(id: string): Promise<OrganizationMember | null> — this.scopedPrisma.client.organizationMember.findFirst({ where: { id } }) — use findFirst (NOT findUnique) per BaseRepository contract
    - softDelete(id: string): Promise<void> — this.scopedPrisma.client.organizationMember.update({ where: { id }, data: { ...this.getSoftDeleteData(), status: 'REMOVED' } })
    - upsertMember(organizationId: string, userId: string): Promise<OrganizationMember> — this.prisma.organizationMember.upsert({ where: { organizationId_userId: { organizationId, userId } }, create: { organizationId, userId, status: 'ACTIVE', joinedAt: new Date(), createdBy: userId }, update: { status: 'ACTIVE', deletedAt: null, joinedAt: new Date(), updatedBy: userId } }) — uses raw this.prisma (injected alongside scopedPrisma) to avoid RESEARCH A3 extension.where conflict

    Import Organization, OrganizationMember from @prisma/client (or from @repo/database if re-exported there; check the existing import pattern in permission-resolver.service.ts).
  </action>
  <verify>
    <automated>cd /Users/amish/AI_SDLC && npm run -w packages/backend build 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "@IsEmail" packages/backend/src/organization/api/dto/add-member.dto.ts returns a match
    - grep "@IsString\|@MinLength\|@MaxLength" packages/backend/src/organization/api/dto/create-organization.dto.ts returns at least 2 matches
    - organization-response.dto.ts and member-response.dto.ts do NOT contain deletedAt or deletedBy fields (sensitive data exclusion)
    - grep "PrismaService" packages/backend/src/organization/persistence/organization.repository.ts returns a match AND grep "TenantedPrismaService" returns 0 matches (org repo must be unscoped)
    - grep "extends BaseRepository" packages/backend/src/organization/persistence/member.repository.ts returns a match
    - grep "organizationMember\.findMany\|organizationMember\.findFirst\|organizationMember\.update" packages/backend/src/organization/persistence/member.repository.ts returns matches (scoped operations)
    - grep "organizationMember\.upsert" packages/backend/src/organization/persistence/member.repository.ts returns a match using raw PrismaService
    - grep "findUnique" packages/backend/src/organization/persistence/member.repository.ts returns 0 matches (BaseRepository contract: never findUnique for scoped lookups)
    - Build exits 0
  </acceptance_criteria>
  <done>DTOs compile with class-validator; OrganizationRepository uses raw PrismaService only; MemberRepository extends BaseRepository and uses raw PrismaService for upsert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OrganizationService + MemberService (business logic)</name>
  <files>
    packages/backend/src/organization/application/organization.service.ts,
    packages/backend/src/organization/application/member.service.ts
  </files>
  <read_first>
    - packages/backend/src/authorization/permission-resolver.service.ts (service scaffold: @Injectable, constructor pattern, Prisma query style with findFirst + where deletedAt:null + select projections; null-return handling; lines 28-101)
    - .planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md (Creator-as-Active-Member pattern — createOrganization $transaction code block; last-member guardrail pattern; user lookup fail-closed pattern; upsert re-add reactivation logic)
    - .planning/phases/06-tenancy-organization-foundation/06-PATTERNS.md (OrganizationService section — $transaction pattern, unscoped access; MemberService section — fail-closed user lookup, last-member count, re-add upsert)
    - .planning/phases/06-tenancy-organization-foundation/06-CONTEXT.md (D-09 any authenticated user can create org; D-10 creator as ACTIVE member joinedAt=now(); D-11 ACTIVE membership = authorization; D-12 list/read only own orgs; D-13 add-member existing users only; D-14 soft-delete + reactivate; D-15 last-member guardrail)
    - packages/backend/src/tenancy/tenancy-error-codes.ts (TENANT_ERROR_CODES.USER_NOT_FOUND, LAST_MEMBER_REMOVAL — created in 06-01)
    - packages/database/prisma/schema/organization.prisma (OrganizationMember status enum values INVITED/ACTIVE/SUSPENDED/REMOVED; $transaction behavior note)
  </read_first>
  <behavior>
    OrganizationService:
    - createOrganization(creatorEmail: string, dto: CreateOrganizationDto): Organization — resolve the platform User by email (raw PrismaService, deletedAt:null); throw TENANT.USER_NOT_FOUND if absent; then $transaction atomically creates org + ACTIVE OrganizationMember with joinedAt=new Date(), createdBy=user.id, status='ACTIVE' overriding schema default 'INVITED' (D-10). Rationale: this runs on a @NoTenantScope route (Plan 06-04) so CLS has no userId — identity comes from the authenticated email.
    - listMyOrgs(creatorEmail: string): Organization[] — resolve User by email (throw TENANT.USER_NOT_FOUND if absent), then unscoped findMany on org repo; cross-org by design; called from @NoTenantScope route
    - findById(id: string): Organization — read ctxOrgId from TenantContextService; validate id === ctxOrgId; throw TENANT.ORG_ACCESS_DENIED if mismatch; then findById via org repo (IDOR prevention)

    MemberService:
    - addMember(email: string): OrganizationMember — find User by email (raw prisma); throw TENANT.USER_NOT_FOUND if not found (never create); call memberRepo.upsertMember (handles both new-add and re-add reactivation)
    - listMembers(): OrganizationMember[] — memberRepo.findManyByOrg() (scoped, auto-filtered)
    - removeMember(memberId: string): void — count ACTIVE members for current org (raw prisma); throw TENANT.LAST_MEMBER_REMOVAL if count <= 1 (D-15); then memberRepo.softDelete(memberId) (D-14)
  </behavior>
  <action>
    organization.service.ts: @Injectable() class OrganizationService. Constructor injects OrganizationRepository (private readonly orgRepo), PrismaService (private readonly prisma — for $transaction and raw User lookups), TenantContextService (private readonly ctx — for reading the CLS organizationId in findById). Import NotFoundException and ForbiddenException from '@nestjs/common' and TENANT_ERROR_CODES from tenancy-error-codes.

    Implement createOrganization(creatorEmail: string, dto: CreateOrganizationDto): Promise<Organization>. First resolve the platform user: const user = await this.prisma.user.findFirst({ where: { email: creatorEmail, deletedAt: null }, select: { id: true } }); if (!user) throw new NotFoundException({ errorCode: TENANT_ERROR_CODES.USER_NOT_FOUND, message: 'No user found for the authenticated account.' }). Then run this.prisma.$transaction(async (tx) => { const org = await tx.organization.create({ data: { name: dto.name, slug: dto.slug, createdBy: user.id } }); await tx.organizationMember.create({ data: { organizationId: org.id, userId: user.id, status: 'ACTIVE', joinedAt: new Date(), createdBy: user.id } }); return org; }). The tx client is the raw transaction client — $extends does not propagate into interactive transactions per RESEARCH A2. (This route is @NoTenantScope, so CLS has no userId; the authenticated email is the identity source.)

    Implement listMyOrgs(creatorEmail: string): Promise<Organization[]>: resolve const user = await this.prisma.user.findFirst({ where: { email: creatorEmail, deletedAt: null }, select: { id: true } }); if (!user) throw new NotFoundException({ errorCode: TENANT_ERROR_CODES.USER_NOT_FOUND, message: 'No user found for the authenticated account.' }); then return this.orgRepo.findByMemberUserId(user.id) — unscoped by design; the caller is authenticated but not org-scoped (used in @NoTenantScope route).

    Implement findById(id: string): Promise<Organization> reading ctxOrgId = this.ctx.getOrganizationId(); if (id !== ctxOrgId) throw new ForbiddenException({ errorCode: TENANT_ERROR_CODES.ORG_ACCESS_DENIED, message: 'Access denied.' }); then return this.orgRepo.findById(id) with null check.

    ---

    member.service.ts: @Injectable() class MemberService. Constructor injects MemberRepository (private readonly memberRepo), PrismaService (private readonly prisma — for User lookup and count), TenantContextService (private readonly ctx).

    Implement addMember(email: string): Promise<OrganizationMember>: read organizationId = this.ctx.getOrganizationId()! (asserted non-null; TenantGuard ensured it's set); find user = await this.prisma.user.findFirst({ where: { email, deletedAt: null }, select: { id: true } }); if (!user) throw new NotFoundException({ errorCode: TENANT_ERROR_CODES.USER_NOT_FOUND, message: 'No user found with that email address.' }) — NEVER call user.create() (D-13); return this.memberRepo.upsertMember(organizationId, user.id).

    Implement listMembers(): Promise<OrganizationMember[]> calling this.memberRepo.findManyByOrg() — extension handles scoping.

    Implement removeMember(memberId: string): Promise<void>: read organizationId = this.ctx.getOrganizationId()!; count active members = await this.prisma.organizationMember.count({ where: { organizationId, status: 'ACTIVE', deletedAt: null } }); if (activeCount <= 1) throw new ForbiddenException({ errorCode: TENANT_ERROR_CODES.LAST_MEMBER_REMOVAL, message: 'Cannot remove the last active member of an organization.' }) (D-15); then await this.memberRepo.softDelete(memberId).

    Import NotFoundException, ForbiddenException from '@nestjs/common'. Import TENANT_ERROR_CODES from tenancy-error-codes. Follow CLAUDE.md §6: no business logic in controllers — all logic here in service layer.
  </action>
  <verify>
    <automated>cd /Users/amish/AI_SDLC && npm run -w packages/backend build 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\\$transaction" packages/backend/src/organization/application/organization.service.ts returns a match (atomic create)
    - grep "user\.findFirst" packages/backend/src/organization/application/organization.service.ts returns a match (creator resolved from authenticated email — @NoTenantScope route has no CLS userId)
    - grep "USER_NOT_FOUND" packages/backend/src/organization/application/organization.service.ts returns a match (fail-closed when the authenticated account has no platform User row)
    - grep "status.*ACTIVE.*joinedAt" packages/backend/src/organization/application/organization.service.ts returns a match (D-10 creator-as-member with joinedAt)
    - grep "USER_NOT_FOUND" packages/backend/src/organization/application/member.service.ts returns a match
    - grep "user\.create\|User\.create" packages/backend/src/organization/application/member.service.ts returns 0 matches (no JIT user creation D-13)
    - grep "LAST_MEMBER_REMOVAL" packages/backend/src/organization/application/member.service.ts returns a match (D-15 guardrail)
    - grep "\.count(" packages/backend/src/organization/application/member.service.ts returns a match (count ACTIVE members before remove)
    - grep "ORG_ACCESS_DENIED" packages/backend/src/organization/application/organization.service.ts returns a match (IDOR prevention in findById)
    - Build exits 0
  </acceptance_criteria>
  <done>OrganizationService and MemberService compile; all D-09..D-15 business rules encoded; no JIT user creation; last-member guardrail in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: member.service.spec.ts (unit tests for guardrail, re-add, and rejection behaviors)</name>
  <files>
    packages/backend/src/organization/application/member.service.spec.ts
  </files>
  <read_first>
    - packages/backend/src/authorization/permission-resolver.service.spec.ts (FULL FILE — exact mock setup lines 8-9, beforeEach clearAllMocks + constructor lines 29-35, describe/it/expect structure; null-return and error-throw test patterns)
    - .planning/phases/06-tenancy-organization-foundation/06-PATTERNS.md (member.service.spec.ts section — mock setup for MemberRepository, raw PrismaService, TenantContextService; 5 test case list)
    - packages/backend/src/organization/application/member.service.ts (just written — exact constructor signature and method names)
    - packages/backend/src/organization/persistence/member.repository.ts (just written — upsertMember, findManyByOrg, softDelete method names)
    - packages/backend/src/tenancy/tenancy-error-codes.ts (TENANT_ERROR_CODES for assertion of error codes)
  </read_first>
  <behavior>
    Five unit tests for MemberService business rules (no real DB — plain constructor instantiation):
    - Test 1 (USER_NOT_FOUND): addMember('nonexistent@test.com'); mockPrisma.user.findFirst returns null → throws NotFoundException with errorCode containing 'USER_NOT_FOUND'; upsertMember is never called
    - Test 2 (re-add reactivation): addMember('existing@test.com'); mockPrisma.user.findFirst returns { id: 'user-1' } → upsertMember called with (orgId, 'user-1') — confirms upsert path, not create
    - Test 3 (LAST_MEMBER_REMOVAL): removeMember('member-1'); mockPrisma.organizationMember.count returns 1 → throws ForbiddenException with errorCode containing 'LAST_MEMBER_REMOVAL'; softDelete never called
    - Test 4 (normal remove): removeMember('member-1'); count returns 2 → softDelete called with 'member-1'; no exception
    - Test 5 (list members): listMembers() → calls memberRepo.findManyByOrg(); returns the mock result
  </behavior>
  <action>
    Create packages/backend/src/organization/application/member.service.spec.ts using Vitest (describe, it, expect, vi, beforeEach). Direct constructor instantiation — no NestJS TestingModule.

    Mock setup at module scope:
    - mockMemberRepo = { findManyByOrg: vi.fn(), upsertMember: vi.fn(), softDelete: vi.fn(), findById: vi.fn() }
    - mockPrisma = { user: { findFirst: vi.fn() }, organizationMember: { count: vi.fn() } }
    - mockCtx = { getOrganizationId: vi.fn().mockReturnValue('org-123'), getUserId: vi.fn().mockReturnValue('caller-user-id'), getOrganizationMemberId: vi.fn() }

    In beforeEach: vi.clearAllMocks(); service = new MemberService(mockMemberRepo as never, mockPrisma as never, mockCtx as never).

    Write 5 tests:
    - Test 1: mockPrisma.user.findFirst.mockResolvedValue(null); await expect(service.addMember('nonexistent@test.com')).rejects.toMatchObject({ response: { errorCode: expect.stringContaining('USER_NOT_FOUND') } }); expect(mockMemberRepo.upsertMember).not.toHaveBeenCalled().
    - Test 2: mockPrisma.user.findFirst.mockResolvedValue({ id: 'user-1' }); mockMemberRepo.upsertMember.mockResolvedValue({ id: 'member-1', userId: 'user-1', status: 'ACTIVE' }); await service.addMember('existing@test.com'); expect(mockMemberRepo.upsertMember).toHaveBeenCalledWith('org-123', 'user-1').
    - Test 3: mockPrisma.organizationMember.count.mockResolvedValue(1); await expect(service.removeMember('member-1')).rejects.toMatchObject({ response: { errorCode: expect.stringContaining('LAST_MEMBER_REMOVAL') } }); expect(mockMemberRepo.softDelete).not.toHaveBeenCalled().
    - Test 4: mockPrisma.organizationMember.count.mockResolvedValue(2); await service.removeMember('member-1'); expect(mockMemberRepo.softDelete).toHaveBeenCalledWith('member-1').
    - Test 5: const fakeMembers = [{ id: 'm1' }, { id: 'm2' }]; mockMemberRepo.findManyByOrg.mockResolvedValue(fakeMembers); const result = await service.listMembers(); expect(result).toEqual(fakeMembers); expect(mockMemberRepo.findManyByOrg).toHaveBeenCalled().
  </action>
  <verify>
    <automated>cd /Users/amish/AI_SDLC && npm test --workspace=packages/backend -- --reporter=verbose --run --testPathPattern="member\.service\.spec" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All 5 tests pass (green)
    - Test 1 (USER_NOT_FOUND): upsertMember.not.toHaveBeenCalled() assertion present and passing
    - Test 3 (LAST_MEMBER_REMOVAL): softDelete.not.toHaveBeenCalled() assertion present and passing
    - Test 2 (re-add): upsertMember called with ('org-123', 'user-1') — confirms upsert path (not create)
    - grep -c "it(" packages/backend/src/organization/application/member.service.spec.ts returns >= 5
    - No NestJS TestingModule import in the test file (direct constructor instantiation)
  </acceptance_criteria>
  <done>All 5 MemberService unit tests pass; USER_NOT_FOUND, LAST_MEMBER_REMOVAL, and re-add upsert behaviors have explicit test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: organization.service.spec.ts (unit tests for create atomicity, USER_NOT_FOUND, and IDOR guard)</name>
  <files>
    packages/backend/src/organization/application/organization.service.spec.ts
  </files>
  <read_first>
    - packages/backend/src/organization/application/member.service.spec.ts (just written — mirror its mock-setup + direct-constructor style for consistency)
    - packages/backend/src/organization/application/organization.service.ts (just written — exact constructor signature and method names)
    - packages/backend/src/organization/persistence/organization.repository.ts (just written — findById, findByMemberUserId method names)
    - packages/backend/src/tenancy/tenancy-error-codes.ts (TENANT_ERROR_CODES.USER_NOT_FOUND, ORG_ACCESS_DENIED)
  </read_first>
  <behavior>
    Four unit tests for OrganizationService (no real DB — plain constructor instantiation; $transaction mocked):
    - Test 1 (create USER_NOT_FOUND): createOrganization('nobody@test.com', dto); mockPrisma.user.findFirst returns null → throws NotFoundException with errorCode containing 'USER_NOT_FOUND'; $transaction never called
    - Test 2 (create atomic creator-as-ACTIVE-member, D-10): user.findFirst returns { id: 'user-1' }; $transaction invokes its callback with a mock tx → returns the created org AND tx.organizationMember.create is called with status:'ACTIVE', a Date joinedAt, userId:'user-1'
    - Test 3 (findById IDOR guard, T-06-12): ctx.getOrganizationId returns 'org-123'; findById('different-org') → throws ForbiddenException with errorCode containing 'ORG_ACCESS_DENIED'; orgRepo.findById never called
    - Test 4 (findById happy path): ctx.getOrganizationId returns 'org-123'; orgRepo.findById returns { id: 'org-123' }; findById('org-123') returns it
  </behavior>
  <action>
    Create packages/backend/src/organization/application/organization.service.spec.ts using Vitest (describe, it, expect, vi, beforeEach). Direct constructor instantiation — no NestJS TestingModule.

    Mock setup at module scope:
    - mockOrgRepo = { create: vi.fn(), findById: vi.fn(), findByMemberUserId: vi.fn() }
    - mockTx = { organization: { create: vi.fn() }, organizationMember: { create: vi.fn() } }
    - mockPrisma = { user: { findFirst: vi.fn() }, $transaction: vi.fn() }
    - mockCtx = { getOrganizationId: vi.fn(), getUserId: vi.fn(), getOrganizationMemberId: vi.fn() }

    In beforeEach: vi.clearAllMocks(); re-arm the transaction runner (clearAllMocks wipes the implementation): mockPrisma.$transaction.mockImplementation(async (cb: (tx: unknown) => unknown) => cb(mockTx)); service = new OrganizationService(mockOrgRepo as never, mockPrisma as never, mockCtx as never).

    Write 4 tests:
    - Test 1: mockPrisma.user.findFirst.mockResolvedValue(null); await expect(service.createOrganization('nobody@test.com', { name: 'X', slug: 'x' } as never)).rejects.toMatchObject({ response: { errorCode: expect.stringContaining('USER_NOT_FOUND') } }); expect(mockPrisma.$transaction).not.toHaveBeenCalled().
    - Test 2: mockPrisma.user.findFirst.mockResolvedValue({ id: 'user-1' }); mockTx.organization.create.mockResolvedValue({ id: 'org-1', name: 'X', slug: 'x' }); const result = await service.createOrganization('creator@test.com', { name: 'X', slug: 'x' } as never); expect(result).toMatchObject({ id: 'org-1' }); expect(mockTx.organizationMember.create).toHaveBeenCalledWith(expect.objectContaining({ data: expect.objectContaining({ userId: 'user-1', status: 'ACTIVE' }) })); expect(mockTx.organizationMember.create.mock.calls[0][0].data.joinedAt).toBeInstanceOf(Date).
    - Test 3: mockCtx.getOrganizationId.mockReturnValue('org-123'); await expect(service.findById('different-org')).rejects.toMatchObject({ response: { errorCode: expect.stringContaining('ORG_ACCESS_DENIED') } }); expect(mockOrgRepo.findById).not.toHaveBeenCalled().
    - Test 4: mockCtx.getOrganizationId.mockReturnValue('org-123'); mockOrgRepo.findById.mockResolvedValue({ id: 'org-123' }); const result = await service.findById('org-123'); expect(result).toMatchObject({ id: 'org-123' }).
  </action>
  <verify>
    <automated>cd /Users/amish/AI_SDLC && npm test --workspace=packages/backend -- --reporter=verbose --run --testPathPattern="organization\.service\.spec" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass (green)
    - Test 1 (USER_NOT_FOUND): $transaction.not.toHaveBeenCalled() assertion present and passing
    - Test 2 (D-10): organizationMember.create called with status:'ACTIVE' and a Date joinedAt — creator recorded as ACTIVE member atomically
    - Test 3 (T-06-12 IDOR): findById on mismatched id throws ORG_ACCESS_DENIED and orgRepo.findById.not.toHaveBeenCalled()
    - grep -c "it(" packages/backend/src/organization/application/organization.service.spec.ts returns >= 4
    - No NestJS TestingModule import in the test file (direct constructor instantiation)
  </acceptance_criteria>
  <done>All 4 OrganizationService unit tests pass; create-org atomicity/creator-as-member, USER_NOT_FOUND fail-closed, and findById IDOR guard have explicit coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Service | Validated DTOs arrive; services must never accept organizationId from request body |
| Service → Repository | Service derives organizationId from CLS (TenantContextService); never from method params passed through from request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Elevation of Privilege | MemberService.addMember JIT user creation | mitigate | addMember rejects with TENANT.USER_NOT_FOUND if User row not found; user.create() is never called (D-13). Unit test 1 verifies upsertMember is not called on null user lookup. |
| T-06-11 | Denial of Service | Last-member removal (org orphaning) | mitigate | count ACTIVE members before remove; throw TENANT.LAST_MEMBER_REMOVAL if count <= 1 (D-15). Unit test 3 verifies softDelete not called when count=1. |
| T-06-12 | Tampering | IDOR via mismatched path id vs X-Organization-Id | mitigate | OrganizationService.findById validates path id === ctx.getOrganizationId(); throws TENANT.ORG_ACCESS_DENIED on mismatch before querying. |
| T-06-13 | Tampering | organizationId mass-assignment via request body | mitigate | DTOs never include organizationId field; global ValidationPipe whitelist:true strips any injected field; organizationId is always sourced from CLS. |
| T-06-14 | Information Disclosure | User existence oracle via add-member | mitigate | TENANT.USER_NOT_FOUND error only discloses that no user exists with that email — not org-level information. Message is actionable but doesn't reveal internal IDs. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed this plan. |
</threat_model>

<verification>
After all four tasks complete:

npm test --workspace=packages/backend -- --reporter=verbose --run --testPathPattern="service.spec"
npm run -w packages/backend build

Both must exit 0. (The "service.spec" pattern runs both member.service.spec and organization.service.spec.)
</verification>

<success_criteria>
- 5 member.service.spec.ts tests green
- 4 organization.service.spec.ts tests green (create atomicity + USER_NOT_FOUND fail-closed, findById IDOR guard)
- OrganizationRepository has ZERO TenantedPrismaService imports (pure raw PrismaService)
- MemberRepository.upsertMember uses raw PrismaService (grep confirms organizationMember.upsert on this.prisma, not this.scopedPrisma.client)
- MemberRepository.findManyByOrg/softDelete/findById use this.scopedPrisma.client (grep confirms)
- OrganizationService.createOrganization resolves the creator User by email then uses $transaction with status='ACTIVE' and joinedAt=new Date() for the member
- MemberService.addMember never calls any create method on user (grep -c user.create returns 0)
- tsc build exits 0 for all 10 files
</success_criteria>

<output>
Create .planning/phases/06-tenancy-organization-foundation/06-03-SUMMARY.md when done
</output>
