---
phase: 06-tenancy-organization-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/tenancy/tenancy-error-codes.ts
  - packages/backend/src/tenancy/decorators/no-tenant-scope.decorator.ts
  - packages/backend/src/tenancy/tenant-context.service.ts
  - packages/backend/src/tenancy/tenanted-prisma.service.ts
  - packages/backend/src/tenancy/base-repository.ts
  - packages/backend/src/tenancy/tenanted-prisma.service.spec.ts
autonomous: true
requirements:
  - TENANT-01
  - TENANT-02
  - SEAM-05

must_haves:
  truths:
    - "TenantedPrismaService auto-injects organizationId + deletedAt:null into args.where on org-scoped model eligible operations when CLS orgId is set"
    - "TenantedPrismaService throws ForbiddenException with errorCode TENANT.NO_ORG_CONTEXT when orgId is undefined on a scoped model eligible operation — never a silent full-table read (D-08 fail-closed)"
    - "findUnique and findUniqueOrThrow operations bypass injection entirely (UNIQUE_OPERATIONS set)"
    - "create/createMany/createManyAndReturn operations bypass injection entirely (NO_WHERE_OPERATIONS set — no where arg)"
    - "BaseRepository abstract class exposes getSoftDeleteData() reading userId from CLS for deletedBy"
    - "NoTenantScope decorator sets IS_NO_TENANT_SCOPE_KEY='isNoTenantScope' metadata (mirrors @Public() verbatim)"
    - "TENANT_ERROR_CODES catalog exposes MISSING_ORG_HEADER, ORG_ACCESS_DENIED, NO_ORG_CONTEXT, USER_NOT_FOUND, LAST_MEMBER_REMOVAL"
    - "TenantContextService provides typed getOrganizationId/getUserId/getOrganizationMemberId CLS getters"
  artifacts:
    - path: "packages/backend/src/tenancy/tenanted-prisma.service.ts"
      provides: "Prisma $extends scoped client (D-05, D-06)"
      contains: "ORG_SCOPED_MODELS"
    - path: "packages/backend/src/tenancy/tenanted-prisma.service.spec.ts"
      provides: "Unit tests for all 5 injection/skip/fail-closed behaviors"
      exports: []
    - path: "packages/backend/src/tenancy/base-repository.ts"
      provides: "Abstract BaseRepository with getSoftDeleteData()"
      contains: "abstract class BaseRepository"
    - path: "packages/backend/src/tenancy/decorators/no-tenant-scope.decorator.ts"
      provides: "@NoTenantScope opt-out decorator"
      contains: "IS_NO_TENANT_SCOPE_KEY"
    - path: "packages/backend/src/tenancy/tenancy-error-codes.ts"
      provides: "5-entry TENANT error catalog"
      contains: "TENANT_ERROR_CODES"
    - path: "packages/backend/src/tenancy/tenant-context.service.ts"
      provides: "Typed CLS getter service"
      contains: "TenantContextService"
  key_links:
    - from: "TenantedPrismaService.$extends closure"
      to: "ClsService.get('organizationId')"
      via: "CLS read at query execution time inside $allOperations callback (not construction time)"
      pattern: "cls\\.get.*organizationId"
    - from: "BaseRepository.getSoftDeleteData()"
      to: "ClsService.get('userId')"
      via: "deletedBy field assignment"
      pattern: "getSoftDeleteData.*deletedBy"
---

<objective>
Stand up the tenancy enforcement core: error catalog, @NoTenantScope opt-out decorator, typed CLS accessor service, Prisma $extends scoped client (TenantedPrismaService), and abstract BaseRepository. These are the foundational building blocks every subsequent plan in Phase 6 depends on.

Purpose: Implement the DECIDED enforcement mechanism (D-05/D-06) — TenantedPrismaService wraps PrismaService with a $extends query hook that reads organizationId from CLS at query execution time and auto-injects it with deletedAt:null into eligible operations on org-owned models. Fail-closed (D-08): no orgId in CLS on a scoped model operation throws ForbiddenException — never a silent full-table read. No new packages required; nestjs-cls@6.2.1 and @prisma/client@6.19.3 already installed.

Output: Six files in packages/backend/src/tenancy/ (new directory). Schema is READ-ONLY — no .prisma edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-tenancy-organization-foundation/06-CONTEXT.md
@.planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md
@.planning/phases/06-tenancy-organization-foundation/06-PATTERNS.md

<interfaces>
<!-- From packages/backend/src/authorization/authorization-error-codes.ts (exact createErrorCatalog pattern) -->
<!-- import { createErrorCatalog } from '../common/error-catalog/create-error-catalog'; -->
<!-- export const AUTHZ_ERROR_CODES = createErrorCatalog('AUTHZ', ['PERMISSION_DENIED'] as const); -->

<!-- From packages/backend/src/auth/decorators/public.decorator.ts (verbatim pattern for @NoTenantScope) -->
<!-- import { SetMetadata } from '@nestjs/common'; -->
<!-- export const IS_PUBLIC_KEY = 'isPublic'; -->
<!-- export const Public = (): MethodDecorator & ClassDecorator => SetMetadata(IS_PUBLIC_KEY, true); -->

<!-- From packages/backend/src/authorization/permission-resolver.service.ts (ClsService.get pattern) -->
<!-- constructor(private readonly prisma: PrismaService, private readonly cls: ClsService) {} -->
<!-- const cached = this.cls.get<Set<string>>(cacheKey); -->

<!-- From packages/database/prisma/schema/organization.prisma (ORG_SCOPED_MODELS reference) -->
<!-- OrganizationMember has organizationId FK; Organization does NOT have organizationId FK -->
<!-- Models with organizationId FK: OrganizationMember, Project, ProjectMember, Team, Repository, Role, Permission -->

<!-- From packages/backend/src/audit/audit-context-provider.interface.ts -->
<!-- export interface AuditContext { organizationId: string; userId?: string; } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tenancy error codes + @NoTenantScope decorator + TenantContextService</name>
  <files>
    packages/backend/src/tenancy/tenancy-error-codes.ts,
    packages/backend/src/tenancy/decorators/no-tenant-scope.decorator.ts,
    packages/backend/src/tenancy/tenant-context.service.ts
  </files>
  <read_first>
    - packages/backend/src/authorization/authorization-error-codes.ts (exact createErrorCatalog pattern — line 1-3; as const assertion is load-bearing)
    - packages/backend/src/common/error-catalog/create-error-catalog.ts (createErrorCatalog generics to understand the as const requirement)
    - packages/backend/src/auth/decorators/public.decorator.ts (copy verbatim, rename only; lines 1-6)
    - packages/backend/src/authorization/permission-resolver.service.ts (cls.get pattern lines 47-50; constructor DI lines 28-32)
    - .planning/phases/06-tenancy-organization-foundation/06-PATTERNS.md (Pattern assignments for tenancy-error-codes.ts, no-tenant-scope.decorator.ts, and tenant-context.service.ts sections)
  </read_first>
  <action>
    Create the new directory packages/backend/src/tenancy/ and the subdirectory packages/backend/src/tenancy/decorators/.

    tenancy-error-codes.ts: Import createErrorCatalog from '../common/error-catalog/create-error-catalog'. Export TENANT_ERROR_CODES = createErrorCatalog('TENANT', ['MISSING_ORG_HEADER', 'ORG_ACCESS_DENIED', 'NO_ORG_CONTEXT', 'USER_NOT_FOUND', 'LAST_MEMBER_REMOVAL'] as const). The 'as const' assertion is REQUIRED — it narrows the tuple to a literal union for the generic return type inference.

    Semantics of each code (per D-02, D-08, D-13, D-15):
    - MISSING_ORG_HEADER: X-Organization-Id header absent on a tenant-scoped route
    - ORG_ACCESS_DENIED: generic 403 for both "org not found" and "caller not an ACTIVE member" — never disclose which (D-02)
    - NO_ORG_CONTEXT: fail-closed extension error when scoped model query attempted with no orgId in CLS (D-08)
    - USER_NOT_FOUND: add-member email not found in User table (D-13; actionable but no internal ID disclosure)
    - LAST_MEMBER_REMOVAL: cannot remove the last ACTIVE member of an org (D-15)

    decorators/no-tenant-scope.decorator.ts: Verbatim copy of public.decorator.ts with two renames: IS_PUBLIC_KEY → IS_NO_TENANT_SCOPE_KEY = 'isNoTenantScope', Public → NoTenantScope. Export both: the key constant and the decorator factory. The MethodDecorator & ClassDecorator union type is REQUIRED — allows usage at both method and class level.

    tenant-context.service.ts: @Injectable() class TenantContextService. Constructor takes only ClsService (ClsService is globally available via ClsModule.forRoot; no module import needed). Three public getters using typed cls.get<string>(): getUserId(): string | undefined, getOrganizationId(): string | undefined, getOrganizationMemberId(): string | undefined. Each returns cls.get<string>('userId'|'organizationId'|'organizationMemberId') — returns undefined when key is not set (pre-TenantGuard or @NoTenantScope routes).
  </action>
  <verify>
    <automated>cd /Users/amish/AI_SDLC && npm run -w packages/backend build 2>&1 | grep -E "error TS|^$" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TENANT_ERROR_CODES" packages/backend/src/tenancy/tenancy-error-codes.ts returns > 0
    - grep "MISSING_ORG_HEADER\|ORG_ACCESS_DENIED\|NO_ORG_CONTEXT\|USER_NOT_FOUND\|LAST_MEMBER_REMOVAL" packages/backend/src/tenancy/tenancy-error-codes.ts produces 5 matches
    - grep "IS_NO_TENANT_SCOPE_KEY.*isNoTenantScope" packages/backend/src/tenancy/decorators/no-tenant-scope.decorator.ts returns a match
    - grep "MethodDecorator.*ClassDecorator" packages/backend/src/tenancy/decorators/no-tenant-scope.decorator.ts returns a match
    - grep "getOrganizationId\|getUserId\|getOrganizationMemberId" packages/backend/src/tenancy/tenant-context.service.ts produces 3 matches
    - npm run build exits 0 (no TypeScript errors in the new files)
  </acceptance_criteria>
  <done>Three files created; tsc build exits 0 with no errors in the tenancy directory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TenantedPrismaService ($extends) + BaseRepository abstract class</name>
  <files>
    packages/backend/src/tenancy/tenanted-prisma.service.ts,
    packages/backend/src/tenancy/base-repository.ts
  </files>
  <read_first>
    - .planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md Pattern 1 (TenantedPrismaService $extends — the full code block is the implementation reference; read the ORG_SCOPED_MODELS, NO_WHERE_OPERATIONS, UNIQUE_OPERATIONS set definitions and the $allOperations callback verbatim)
    - .planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md Pattern 3 (BaseRepository abstract class — getSoftDeleteData() helper)
    - .planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md Pitfalls 1-4 (findUnique pitfall, fail-open scoping, guard order, bootstrap deadlock)
    - .planning/phases/06-tenancy-organization-foundation/06-RESEARCH.md Open Questions (A3 upsert behavior — MemberRepository must use raw PrismaService for upsert)
    - packages/backend/src/authorization/permission-resolver.service.ts (constructor DI pattern lines 28-32; cls.get pattern lines 47-50)
    - .planning/phases/06-tenancy-organization-foundation/06-PATTERNS.md (TenantedPrismaService spec section — capturedExtension mock pattern validates the testability design)
    - packages/database/prisma/schema/organization.prisma (confirm OrganizationMember has organizationId FK; confirm Organization does NOT — critical for ORG_SCOPED_MODELS list)
  </read_first>
  <behavior>
    TenantedPrismaService injection behaviors (verified by Task 3 unit tests):
    - Scoped model (e.g. 'organizationMember') + eligible operation (e.g. 'findMany') + orgId present in CLS → args.where merged with { organizationId: orgId, deletedAt: null }
    - Scoped model + eligible operation + orgId UNDEFINED in CLS → throws ForbiddenException({ errorCode: TENANT_ERROR_CODES.NO_ORG_CONTEXT }) — never silent pass-through (D-08)
    - Scoped model + operation in NO_WHERE_OPERATIONS ('create', 'createMany', 'createManyAndReturn') → args NOT mutated
    - Scoped model + operation in UNIQUE_OPERATIONS ('findUnique', 'findUniqueOrThrow') → args NOT mutated
    - Non-scoped model ('organization', 'auditLog', 'user', etc.) + any operation → args NOT mutated regardless of CLS state

    BaseRepository behaviors:
    - getSoftDeleteData() returns { deletedAt: new Date(), deletedBy: cls.get('userId') ?? null }
    - Subclasses access the scoped client via this.scopedPrisma.client; extension auto-injects WHERE clauses
  </behavior>
  <action>
    tenanted-prisma.service.ts: @Injectable() class TenantedPrismaService. At module scope (not inside class): define three const Sets — ORG_SCOPED_MODELS, NO_WHERE_OPERATIONS, UNIQUE_OPERATIONS — as documented in RESEARCH.md Pattern 1 (copy the exact set members). ORG_SCOPED_MODELS must include 'organizationMember', 'project', 'projectMember', 'team', 'repository', 'role', 'permission' — note 'organization' is NOT in the list (Organization has no organizationId FK; it is the root entity).

    Constructor takes PrismaService and ClsService. Capture cls in a closure variable before calling $extends (the closure reads per-request context at query execution time, not at construction time — RESEARCH Pattern 1 key insight). Call prisma.$extends({ query: { $allModels: { async $allOperations({ model, operation, args, query }) { ... } } } }). Inside the callback: (1) skip if model NOT in ORG_SCOPED_MODELS, (2) skip if operation in NO_WHERE_OPERATIONS, (3) skip if operation in UNIQUE_OPERATIONS, (4) read orgId = cls.get<string>('organizationId'), (5) if orgId is null or undefined: throw new ForbiddenException with { errorCode: TENANT_ERROR_CODES.NO_ORG_CONTEXT, message: 'No active organization context.' } — NEVER silently skip, (6) type-assert args as { where?: Record<string, unknown> } and merge: args.where = { ...(args.where ?? {}), organizationId: orgId, deletedAt: null }, (7) return query(args). Expose the result as public readonly client using 'as unknown as PrismaClient' type assertion to avoid DI-incompatible inferred types (RESEARCH Open Question 2).

    base-repository.ts: abstract class BaseRepository (NOT @Injectable() — it is a base class, not a provider directly). Constructor(protected readonly scopedPrisma: TenantedPrismaService, protected readonly cls: ClsService). Single method: protected getSoftDeleteData(): { deletedAt: Date; deletedBy: string | null } returns { deletedAt: new Date(), deletedBy: this.cls.get<string>('userId') ?? null }. Add a JSDoc comment warning: "Subclasses MUST use scopedPrisma.client.<model>.findFirst (never findUnique) for org-scoped lookups. For re-add upsert operations, use raw PrismaService injected separately (RESEARCH A3 — upsert.where may conflict with extension injection)."
  </action>
  <verify>
    <automated>cd /Users/amish/AI_SDLC && npm run -w packages/backend build 2>&1 | grep -E "error TS" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "ORG_SCOPED_MODELS" packages/backend/src/tenancy/tenanted-prisma.service.ts returns a match (Set defined at module scope)
    - grep "NO_WHERE_OPERATIONS\|UNIQUE_OPERATIONS" packages/backend/src/tenancy/tenanted-prisma.service.ts produces 2 matches
    - grep "ForbiddenException\|NO_ORG_CONTEXT" packages/backend/src/tenancy/tenanted-prisma.service.ts returns a match (fail-closed path)
    - grep "deletedAt.*null" packages/backend/src/tenancy/tenanted-prisma.service.ts returns a match (injection path)
    - grep "abstract class BaseRepository" packages/backend/src/tenancy/base-repository.ts returns a match
    - grep "getSoftDeleteData" packages/backend/src/tenancy/base-repository.ts returns a match
    - Build exits 0 (no TypeScript errors in either file)
    - 'organization' does NOT appear in ORG_SCOPED_MODELS set (Organization has no organizationId FK)
  </acceptance_criteria>
  <done>TenantedPrismaService and BaseRepository compiled; ORG_SCOPED_MODELS includes 'organizationMember' but not 'organization'; fail-closed path exists for missing orgId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tenanted-prisma.service.spec.ts (unit tests for extension injection logic)</name>
  <files>
    packages/backend/src/tenancy/tenanted-prisma.service.spec.ts
  </files>
  <read_first>
    - packages/backend/src/authorization/permission-resolver.service.spec.ts (full file — vi.fn() mock setup lines 8-9, beforeEach clearAllMocks + constructor instantiation pattern lines 29-35, describe/it/expect structure throughout)
    - .planning/phases/06-tenancy-organization-foundation/06-PATTERNS.md (TenantedPrismaService spec section — capturedExtension approach for mocking $extends; the exact mock shape to use)
    - packages/backend/src/tenancy/tenanted-prisma.service.ts (just written — exact class signatures, ORG_SCOPED_MODELS, UNIQUE_OPERATIONS, NO_WHERE_OPERATIONS sets)
    - packages/backend/vitest.config.ts (test runner config — confirm no custom setup)
  </read_first>
  <behavior>
    Five test cases covering all critical injection/skip/fail-closed behaviors:
    - Test 1 (injection): model='organizationMember', operation='findMany', cls.get returns 'org-123' → after $allOperations, args.where contains { organizationId: 'org-123', deletedAt: null }
    - Test 2 (fail-closed): model='organizationMember', operation='findMany', cls.get returns undefined → $allOperations throws; error contains 'NO_ORG_CONTEXT'
    - Test 3 (NO_WHERE skip): model='organizationMember', operation='create', cls.get returns 'org-123' → args.where NOT mutated
    - Test 4 (UNIQUE skip): model='organizationMember', operation='findUnique', cls.get returns 'org-123' → args.where NOT mutated
    - Test 5 (non-scoped skip): model='auditLog' (not in ORG_SCOPED_MODELS), operation='findMany', cls.get returns 'org-123' → args.where NOT mutated
  </behavior>
  <action>
    Create tenanted-prisma.service.spec.ts using Vitest (describe, it, expect, vi, beforeEach from 'vitest'). No NestJS TestingModule — plain constructor instantiation for unit test speed.

    Mock setup at module scope: capturedExtensionFn = null (will hold the $allOperations callback); mockPrisma = { $extends: vi.fn().mockImplementation(ext => { capturedExtensionFn = ext.query.$allModels.$allOperations; return { organizationMember: { findMany: vi.fn() } }; }) }; mockCls = { get: vi.fn() }.

    In beforeEach: vi.clearAllMocks(); set capturedExtensionFn = null; instantiate service = new TenantedPrismaService(mockPrisma as never, mockCls as never). After construction, capturedExtensionFn holds the callback extracted from the extension.

    Helper: callAllOperations(model: string, operation: string, where?: Record<string, unknown>): calls capturedExtensionFn({ model, operation, args: { where: where ?? {} }, query: vi.fn().mockResolvedValue([]) }) and returns the promise.

    Write the 5 test cases using the helper:
    - Test 1: mockCls.get.mockReturnValue('org-123'); await callAllOperations('organizationMember', 'findMany', {}); inspect args object (capture via mockQuery spy) for organizationId and deletedAt.
    - Test 2: mockCls.get.mockReturnValue(undefined); await expect(callAllOperations('organizationMember', 'findMany')).rejects.toThrow(); verify thrown error errorCode includes 'NO_ORG_CONTEXT'.
    - Tests 3-5: mockCls.get.mockReturnValue('org-123'); call with the skip operation/model; assert query was called with unmodified args (no organizationId injected).

    Use 'as never' for mock type assertions per the codebase pattern.
  </action>
  <verify>
    <automated>cd /Users/amish/AI_SDLC && npm test --workspace=packages/backend -- --reporter=verbose --run --testPathPattern="tenanted-prisma\.service\.spec" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Test runner exits 0 (all tests green)
    - 5 test cases present: injection, fail-closed, NO_WHERE skip, UNIQUE skip, non-scoped skip
    - Test 2 (fail-closed): the assertion verifies the thrown exception contains 'NO_ORG_CONTEXT' (not just any exception)
    - Test 4 (UNIQUE skip): model='organizationMember', operation='findUnique' — args.where is NOT mutated with organizationId
    - grep -c "it(" packages/backend/src/tenancy/tenanted-prisma.service.spec.ts returns >= 5
  </acceptance_criteria>
  <done>All 5 unit tests pass; the critical fail-closed and UNIQUE skip behaviors have explicit test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLS → Prisma extension | organizationId read from AsyncLocalStorage inside the $allOperations callback; if CLS is not populated, the extension must deny (not pass through) |
| Application → Database | $extends wraps every query on org-owned models; bypass (unscoped client use) leaks cross-tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | TenantedPrismaService.$extends (fail-open scoping) | mitigate | Throw ForbiddenException(TENANT.NO_ORG_CONTEXT) when orgId undefined on scoped model — never silent pass-through. Unit test T3-fail-closed confirms this (D-08). |
| T-06-02 | Tampering | args.where injection path | mitigate | organizationId is always server-set from CLS; it is NEVER accepted from the HTTP request body (DTOs use whitelist: true via global ValidationPipe already active from Phase 3). |
| T-06-03 | Information Disclosure | findUnique type bypass | mitigate | UNIQUE_OPERATIONS skip set prevents Prisma type-system rejection from findUnique.where non-unique field injection. BaseRepository must use findFirst exclusively. |
| T-06-04 | Information Disclosure | non-scoped model fall-through | mitigate | Only models in ORG_SCOPED_MODELS are subject to injection; non-scoped models (Organization, User, AuditLog) pass through unchanged — this is correct behavior, not a vulnerability. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs zero new packages; all dependencies vetted in prior phases per RESEARCH.md Package Legitimacy Audit. |
</threat_model>

<verification>
After all three tasks complete:

npm test --workspace=packages/backend -- --reporter=verbose --run --testPathPattern="tenanted-prisma"

Expected: all tests green, no TypeScript errors.

Also verify the directory structure:
ls packages/backend/src/tenancy/
Expected: tenancy-error-codes.ts, tenant-context.service.ts, tenanted-prisma.service.ts, tenanted-prisma.service.spec.ts, base-repository.ts; decorators/no-tenant-scope.decorator.ts
</verification>

<success_criteria>
- npm test --workspace=packages/backend -- --run --testPathPattern="tenanted-prisma" exits 0 with 5 tests green
- npm run -w packages/backend build exits 0 with no errors in src/tenancy/
- TENANT_ERROR_CODES has exactly 5 entries (verify with grep -c)
- IS_NO_TENANT_SCOPE_KEY = 'isNoTenantScope' is exported from no-tenant-scope.decorator.ts
- abstract class BaseRepository is exported with getSoftDeleteData() defined
- 'organization' is NOT in ORG_SCOPED_MODELS (it has no organizationId FK)
</success_criteria>

<output>
Create .planning/phases/06-tenancy-organization-foundation/06-01-SUMMARY.md when done
</output>
