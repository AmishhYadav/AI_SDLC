---
phase: "07-project-foundation"
plan: 3
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - packages/backend/src/contracts/organization/organization.port.ts
  - packages/backend/src/contracts/organization/organization-summary.dto.ts
  - packages/backend/src/contracts/organization/index.ts
  - packages/backend/src/organization/application/organization-port.adapter.ts
  - packages/backend/src/organization/application/organization-port.adapter.spec.ts
  - packages/backend/src/organization/organization.module.ts
  - packages/backend/src/app.module.ts
autonomous: true
requirements:
  - PROJ-04

must_haves:
  truths:
    - "src/contracts/organization/ contains pure TypeScript interfaces and a DI token with zero imports from any domain module or @repo/database (D-11)"
    - "OrganizationPortAdapter.getOrganizationSummary(orgId) returns OrganizationSummaryDto or null using raw PrismaService.organization.findUnique with explicit deletedAt:null (D-12, D-13)"
    - "OrganizationPortAdapter.getProjectSummary(projectId) returns ProjectSummaryDto or null using raw PrismaService.project.findUnique with explicit deletedAt:null (D-13)"
    - "OrganizationPortAdapter.isActiveMember(orgId, userId) returns boolean using raw PrismaService.organizationMember.findFirst (D-13)"
    - "OrganizationPortAdapter NEVER uses TenantedPrismaService — the port may be called by cross-org consumers (D-12)"
    - "OrganizationModule provides ORGANIZATION_PORT token bound to OrganizationPortAdapter and exports ORGANIZATION_PORT"
    - "AppModule imports ProjectModule and TeamModule so all project and team routes are live"
    - "Summary DTOs expose only id/name/key/status/organizationId — no Prisma entity types, no soft-delete metadata (D-13)"
  artifacts:
    - path: "packages/backend/src/contracts/organization/organization.port.ts"
      provides: "ORGANIZATION_PORT Symbol, OrganizationPort interface, OrganizationSummaryDto, ProjectSummaryDto"
      exports: ["ORGANIZATION_PORT", "OrganizationPort", "OrganizationSummaryDto", "ProjectSummaryDto"]
      contains: "Symbol('ORGANIZATION_PORT')"
    - path: "packages/backend/src/contracts/organization/index.ts"
      provides: "Barrel re-export of all contracts"
      contains: "export * from"
    - path: "packages/backend/src/organization/application/organization-port.adapter.ts"
      provides: "OrganizationPortAdapter implements OrganizationPort using raw PrismaService"
      exports: ["OrganizationPortAdapter"]
    - path: "packages/backend/src/organization/organization.module.ts"
      provides: "Updated module: provides ORGANIZATION_PORT, exports ORGANIZATION_PORT"
      contains: "ORGANIZATION_PORT"
    - path: "packages/backend/src/app.module.ts"
      provides: "Updated AppModule: imports ProjectModule, TeamModule"
      contains: "ProjectModule"
  key_links:
    - from: "packages/backend/src/organization/organization.module.ts"
      to: "packages/backend/src/contracts/organization/organization.port.ts"
      via: "{ provide: ORGANIZATION_PORT, useClass: OrganizationPortAdapter } + exports: [ORGANIZATION_PORT]"
      pattern: "ORGANIZATION_PORT"
    - from: "packages/backend/src/organization/application/organization-port.adapter.ts"
      to: "PrismaService"
      via: "Raw PrismaService (never TenantedPrismaService) for all three port methods"
      pattern: "private readonly prisma: PrismaService"
    - from: "packages/backend/src/app.module.ts"
      to: "packages/backend/src/project/project.module.ts"
      via: "ProjectModule in imports array"
      pattern: "ProjectModule"
    - from: "packages/backend/src/app.module.ts"
      to: "packages/backend/src/team/team.module.ts"
      via: "TeamModule in imports array"
      pattern: "TeamModule"
---

<objective>
Create the published OrganizationPort contract, its in-process implementation adapter, wire the DI token into OrganizationModule, and wire ProjectModule + TeamModule into AppModule.

Purpose: Deliver PROJ-04 — the first contracts/<domain>/ port pattern in the codebase that Phases 8 and 9 will template for all 14 domains. The port is pure TypeScript interfaces + a Symbol DI token with zero domain imports (extraction boundary). The adapter uses raw PrismaService and is cross-org safe.

Output: 5 new files + 2 existing files updated. Depends on plans 07-01 and 07-02 because AppModule wiring needs ProjectModule and TeamModule to exist.
</objective>

<execution_context>
@/Users/amish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/amish/AI_SDLC/.planning/PROJECT.md
@/Users/amish/AI_SDLC/.planning/ROADMAP.md
@/Users/amish/AI_SDLC/.planning/phases/07-project-foundation/07-CONTEXT.md
@/Users/amish/AI_SDLC/.planning/phases/07-project-foundation/07-RESEARCH.md
@/Users/amish/AI_SDLC/.planning/phases/07-project-foundation/07-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. -->

From packages/backend/src/organization/organization.module.ts (CURRENT STATE — read before modifying):
  @Module({
    imports: [TenancyModule],
    controllers: [OrganizationController],
    providers: [OrganizationService, MemberService, OrganizationRepository, MemberRepository],
    // NOTE: no exports currently — we will ADD exports: [ORGANIZATION_PORT]
  })
  export class OrganizationModule {}

From packages/backend/src/app.module.ts (CURRENT STATE — read before modifying):
  // Current imports array ends with: OrganizationModule (line ~97)
  // We will ADD ProjectModule and TeamModule after OrganizationModule

From packages/backend/src/organization/persistence/organization.repository.ts:
  // Raw PrismaService constructor pattern (no BaseRepository extension)
  // This is the exact pattern OrganizationPortAdapter follows

Analog files to READ before implementing:
  packages/backend/src/organization/persistence/organization.repository.ts  -- raw PrismaService constructor + query pattern (OrganizationPortAdapter mirrors this)
  packages/backend/src/organization/organization.module.ts                   -- current module state before modifications
  packages/backend/src/app.module.ts                                         -- current AppModule state before modifications

CONTRACTS CONSTRAINT (D-11): src/contracts/organization/ files MUST import NOTHING from:
  - Any domain module (src/organization/, src/project/, src/team/)
  - @repo/database (Prisma types)
  - nestjs/* (NestJS decorators)
  Summary DTOs are plain TypeScript readonly interfaces only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrganizationPort contracts (greenfield)</name>
  <files>
    packages/backend/src/contracts/organization/organization.port.ts,
    packages/backend/src/contracts/organization/organization-summary.dto.ts,
    packages/backend/src/contracts/organization/index.ts
  </files>
  <read_first>
    - packages/backend/src/planning/phases/07-project-foundation/07-RESEARCH.md § Pattern 7 (OrganizationPort interface specification — no analog in codebase)
    - packages/backend/src/planning/phases/07-project-foundation/07-CONTEXT.md § D-11, D-12, D-13 (port surface + DI token contract)
    NOTE: Read the RESEARCH.md Pattern 7 and CONTEXT.md D-11/D-12/D-13 sections specifically — these are the sole specification for these greenfield files. There is no codebase analog to mirror.
  </read_first>
  <action>
    organization.port.ts: Export the Symbol DI token and the port interface. Zero imports — this file has NO import statements at all.

    Contents:
      export const ORGANIZATION_PORT = Symbol('ORGANIZATION_PORT');

      export interface OrganizationPort {
        getOrganizationSummary(orgId: string): Promise<OrganizationSummaryDto | null>;
        getProjectSummary(projectId: string): Promise<ProjectSummaryDto | null>;
        isActiveMember(orgId: string, userId: string): Promise<boolean>;
      }

    But OrganizationSummaryDto and ProjectSummaryDto can be co-located in the same file or imported from organization-summary.dto.ts. Place them in organization-summary.dto.ts and import ONLY from './organization-summary.dto' in organization.port.ts. That one local import within contracts/ is acceptable — the constraint is no domain-module imports.

    organization-summary.dto.ts: Plain TypeScript readonly interface. Zero imports.
      export interface OrganizationSummaryDto {
        readonly id: string;
        readonly name: string;
        readonly slug: string;
        readonly status: string;
      }
      export interface ProjectSummaryDto {
        readonly id: string;
        readonly name: string;
        readonly key: string;
        readonly status: string;
        readonly organizationId: string;
      }

    Fields allowed (D-13): id, name, slug (org), key (project), status, organizationId. NEVER: deletedAt, deletedBy, updatedBy, updatedAt, cuid internal IDs beyond the entity's own id.

    index.ts: Two export-star lines only:
      export * from './organization.port';
      export * from './organization-summary.dto';
  </action>
  <verify>
    <automated>grep -rn "import" packages/backend/src/contracts/organization/ | grep -v "from '\\./" | grep -v "^Binary"</automated>
  </verify>
  <acceptance_criteria>
    - organization-summary.dto.ts has ZERO import statements
    - organization.port.ts imports ONLY from './organization-summary.dto' (local within contracts/) — no @repo/database, no nestjs, no domain modules
    - ORGANIZATION_PORT is exported as Symbol('ORGANIZATION_PORT')
    - OrganizationPort interface has exactly 3 methods: getOrganizationSummary, getProjectSummary, isActiveMember
    - Both summary DTOs use readonly fields only
    - ProjectSummaryDto includes organizationId field (needed by downstream cross-org consumers)
    - index.ts contains two export * from lines
    - grep command above returns NO lines (no external imports in contracts/) — only the ./organization-summary.dto internal import is acceptable
  </acceptance_criteria>
  <done>Three contracts files created; zero imports from external modules; ORGANIZATION_PORT Symbol exported; summary DTOs are plain readonly TypeScript interfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OrganizationPortAdapter and unit test</name>
  <files>
    packages/backend/src/organization/application/organization-port.adapter.ts,
    packages/backend/src/organization/application/organization-port.adapter.spec.ts
  </files>
  <read_first>
    - packages/backend/src/organization/persistence/organization.repository.ts (raw PrismaService constructor + findUnique/findFirst patterns — OrganizationPortAdapter mirrors this constructor style)
    - packages/backend/src/organization/application/member.service.spec.ts (mock setup pattern for unit tests)
    - packages/backend/src/contracts/organization/organization.port.ts (just created — OrganizationPort interface to implement)
    - packages/backend/src/contracts/organization/organization-summary.dto.ts (just created — DTOs to return)
    - packages/database/prisma/schema/organization.prisma (Organization + Project + OrganizationMember exact fields for query construction)
  </read_first>
  <behavior>
    - Test 1: getOrganizationSummary('known-org-id') — prisma.organization.findUnique called with where: { id, deletedAt: null }; returns OrganizationSummaryDto with id/name/slug/status only
    - Test 2: getOrganizationSummary('unknown-org-id') — findUnique returns null; adapter returns null (no throw)
    - Test 3: getProjectSummary('known-project-id') — prisma.project.findUnique called with where: { id, deletedAt: null }; returns ProjectSummaryDto with id/name/key/status/organizationId only
    - Test 4: getProjectSummary('unknown-project-id') — returns null
    - Test 5: isActiveMember('org-id', 'user-id') — prisma.organizationMember.findFirst called with where: { organizationId, userId, status: 'ACTIVE', deletedAt: null }; returns true if result non-null
    - Test 6: isActiveMember with non-member — findFirst returns null; returns false
  </behavior>
  <action>
    OrganizationPortAdapter is @Injectable() class that implements OrganizationPort from src/contracts/organization/. Constructor: (private readonly prisma: PrismaService). Import PrismaService from '@repo/database'. Import OrganizationPort, OrganizationSummaryDto, ProjectSummaryDto from '../../contracts/organization'.

    CRITICAL: constructor uses raw PrismaService ONLY — never TenantedPrismaService. The port may be called by future cross-org consumers (Phase 8/9 workflows) that do not have an org in CLS context. Using TenantedPrismaService would throw NO_ORG_CONTEXT in those consumers.

    getOrganizationSummary(orgId: string): Promise<OrganizationSummaryDto | null>:
      const org = await this.prisma.organization.findUnique({ where: { id: orgId, deletedAt: null }, select: { id: true, name: true, slug: true, status: true } });
      if (!org) return null;
      return { id: org.id, name: org.name, slug: org.slug, status: org.status };

    getProjectSummary(projectId: string): Promise<ProjectSummaryDto | null>:
      const p = await this.prisma.project.findUnique({ where: { id: projectId, deletedAt: null }, select: { id: true, name: true, key: true, status: true, organizationId: true } });
      if (!p) return null;
      return { id: p.id, name: p.name, key: p.key, status: p.status, organizationId: p.organizationId };

    isActiveMember(orgId: string, userId: string): Promise<boolean>:
      const member = await this.prisma.organizationMember.findFirst({ where: { organizationId: orgId, userId, status: 'ACTIVE', deletedAt: null }, select: { id: true } });
      return member !== null;

    Note: getOrganizationSummary and getProjectSummary use findUnique (not findFirst) because neither Organization nor Project are queries through TenantedPrismaService here — this is raw PrismaService, so findUnique type constraints are safe. The primary-key id lookups are valid for findUnique.

    organization-port.adapter.spec.ts: mock setup — const mockPrisma = { organization: { findUnique: vi.fn() }, project: { findUnique: vi.fn() }, organizationMember: { findFirst: vi.fn() } }. new OrganizationPortAdapter(mockPrisma as never). Cover all 6 behaviors above.
  </action>
  <verify>
    <automated>npm run test --workspace=packages/backend -- --run --reporter=verbose src/organization/application/organization-port.adapter.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - organization-port.adapter.spec.ts exits 0 with all 6 tests passing
    - OrganizationPortAdapter constructor injects PrismaService (NOT TenantedPrismaService) — grep confirms: class OrganizationPortAdapter { constructor(private readonly prisma: PrismaService)
    - organization-port.adapter.ts does NOT import TenantedPrismaService — grep confirms absence
    - getOrganizationSummary uses select: { id, name, slug, status } — no other fields exposed
    - getProjectSummary uses select: { id, name, key, status, organizationId } — no deletedAt/deletedBy in return
    - isActiveMember query includes status: 'ACTIVE' and deletedAt: null in where clause
    - Adapter returns null (not throws) when entity is not found
  </acceptance_criteria>
  <done>OrganizationPortAdapter spec passes; adapter uses raw PrismaService; all three port methods implemented correctly with explicit deletedAt:null guards.</done>
</task>

<task type="auto">
  <name>Task 3: Module wiring — OrganizationModule + AppModule</name>
  <files>
    packages/backend/src/organization/organization.module.ts,
    packages/backend/src/app.module.ts
  </files>
  <read_first>
    - packages/backend/src/organization/organization.module.ts (current state — read ENTIRE file before editing; add port provider + export without breaking existing providers)
    - packages/backend/src/app.module.ts (current state — read ENTIRE file before editing; add ProjectModule + TeamModule after OrganizationModule in imports array)
    - packages/backend/src/project/project.module.ts (just created in plan 07-01 — confirm class name ProjectModule)
    - packages/backend/src/team/team.module.ts (just created in plan 07-02 — confirm class name TeamModule)
    - packages/backend/src/contracts/organization/organization.port.ts (just created — confirm ORGANIZATION_PORT export name)
    - packages/backend/src/organization/application/organization-port.adapter.ts (just created — confirm class name)
  </read_first>
  <action>
    OrganizationModule update (surgical edit — do not rewrite unchanged lines):
      Add to imports at top of file:
        import { ORGANIZATION_PORT } from '../contracts/organization';
        import { OrganizationPortAdapter } from './application/organization-port.adapter';
      Add to providers array: { provide: ORGANIZATION_PORT, useClass: OrganizationPortAdapter }
      Add exports array (new): exports: [ORGANIZATION_PORT]
      Keep all existing providers (OrganizationService, MemberService, OrganizationRepository, MemberRepository) unchanged.
      Final module decorator: @Module({ imports: [TenancyModule], controllers: [OrganizationController], providers: [OrganizationService, MemberService, OrganizationRepository, MemberRepository, { provide: ORGANIZATION_PORT, useClass: OrganizationPortAdapter }], exports: [ORGANIZATION_PORT] })

    AppModule update (surgical edit — add two imports + two entries in imports array):
      Add import lines near OrganizationModule import:
        import { ProjectModule } from './project/project.module';
        import { TeamModule } from './team/team.module';
      Add to imports array after OrganizationModule:
        ProjectModule,
        TeamModule,
      Do NOT modify any other section (providers, guards, interceptors, existing modules).

    Downstream usage pattern for Phase 8/9 consumers (add as a comment in organization.module.ts exports):
      // Consumers inject by token: @Inject(ORGANIZATION_PORT) private readonly orgPort: OrganizationPort
      // Consuming modules must import OrganizationModule to access the exported token.
  </action>
  <verify>
    <automated>npm run build --workspace=packages/backend -- --noEmit 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - organization.module.ts exports array contains ORGANIZATION_PORT
    - organization.module.ts providers array contains { provide: ORGANIZATION_PORT, useClass: OrganizationPortAdapter }
    - organization.module.ts existing providers (OrganizationService, MemberService, OrganizationRepository, MemberRepository) are unchanged
    - app.module.ts imports array contains ProjectModule and TeamModule (after OrganizationModule)
    - app.module.ts import statements include ProjectModule from './project/project.module' and TeamModule from './team/team.module'
    - No other lines in app.module.ts are modified
    - npm run build --workspace=packages/backend -- --noEmit exits 0 (full compilation clean)
    - npm run test --workspace=packages/backend -- --run exits 0 (all specs pass)
  </acceptance_criteria>
  <done>OrganizationModule provides and exports ORGANIZATION_PORT; AppModule wires ProjectModule and TeamModule; full build and test suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OrganizationPortAdapter → PrismaService | Cross-org capable; no CLS context assumed; raw queries with explicit filters |
| contracts/ → consumer modules | Pure TS boundary; no domain internals can cross this boundary into the contract definition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Information Disclosure | OrganizationPortAdapter.getProjectSummary | mitigate | Raw PrismaService with explicit deletedAt:null in where; select lists only public fields (id, name, key, status, organizationId); no soft-delete metadata or internal fields exposed |
| T-07-13 | Information Disclosure | OrganizationPortAdapter.getOrganizationSummary | mitigate | select: { id, name, slug, status }; explicit deletedAt:null filter; returns null (not throws) for not-found — no information about whether entity exists vs is soft-deleted |
| T-07-14 | Tampering | src/contracts/organization/ boundary | mitigate | Zero imports from any domain module or @repo/database; plain TypeScript interfaces only; extraction boundary structurally enforced by the absence of imports |
| T-07-15 | Elevation of Privilege | isActiveMember | mitigate | Explicit status: 'ACTIVE' + deletedAt: null in where clause; raw PrismaService with full org + user + status filter prevents stale/removed member from returning true |
| T-07-16 | Tampering | AppModule wiring (ProjectModule, TeamModule) | accept | Modules are leaf-level (import TenancyModule only); no cyclic DI risk; NestJS module resolution validates the graph at boot |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase |
</threat_model>

<verification>
Phase gate — run after this plan completes:
  npm run test --workspace=packages/backend -- --run
  npm run build --workspace=packages/backend -- --noEmit

Contract integrity check (run manually):
  grep -rn "import" packages/backend/src/contracts/organization/ | grep -v "from '\\./"
  # Must return 0 lines — no external imports in contracts/

Port binding check:
  grep -n "ORGANIZATION_PORT" packages/backend/src/organization/organization.module.ts
  # Must appear in both providers and exports

Full project/team route availability smoke test (requires running server):
  curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/v1/organizations/test/projects
  # Expected: 401 (auth required) or 403 — NOT 404 (route must exist)
</verification>

<success_criteria>
- 3 new files in packages/backend/src/contracts/organization/ with zero external imports
- OrganizationPortAdapter uses raw PrismaService only; all 3 port methods return lean DTOs or null
- organization-port.adapter.spec.ts passes with all 6 test cases
- OrganizationModule provides and exports ORGANIZATION_PORT
- AppModule wires ProjectModule and TeamModule
- Full build is clean (npm run build --noEmit exits 0)
- Full test suite passes (npm run test --run exits 0)
- PROJ-04 addressed: published contract available for Phase 8/9 consumers
</success_criteria>

<output>
Create .planning/phases/07-project-foundation/07-03-SUMMARY.md when done.
</output>
