---
phase: "07-project-foundation"
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/team/team-error-codes.ts
  - packages/backend/src/team/api/dto/create-team.dto.ts
  - packages/backend/src/team/api/dto/update-team.dto.ts
  - packages/backend/src/team/api/dto/team-response.dto.ts
  - packages/backend/src/team/api/dto/add-team-member.dto.ts
  - packages/backend/src/team/api/dto/team-member-response.dto.ts
  - packages/backend/src/team/persistence/team.repository.ts
  - packages/backend/src/team/persistence/team-member.repository.ts
  - packages/backend/src/team/application/team.service.ts
  - packages/backend/src/team/application/team.service.spec.ts
  - packages/backend/src/team/application/team-member.service.ts
  - packages/backend/src/team/application/team-member.service.spec.ts
  - packages/backend/src/team/api/team.controller.ts
  - packages/backend/src/team/team.module.ts
autonomous: true
requirements:
  - PROJ-03

must_haves:
  truths:
    - "POST /v1/organizations/:orgId/teams creates an org-scoped Team (no projectId — D-01) authorized by ACTIVE org membership (D-05)"
    - "GET/PATCH/DELETE /v1/organizations/:orgId/teams/:teamId list/read/update/soft-delete teams within the active org"
    - "POST /v1/organizations/:orgId/teams/:teamId/members validates that the target organizationMemberId is ACTIVE in the current org before upsert; re-add reactivates a soft-removed TeamMember (D-03)"
    - "DELETE /v1/organizations/:orgId/teams/:teamId/members/:memberId soft-deletes the TeamMember (deletedAt set); no last-member guardrail (D-05)"
    - "TeamMemberRepository uses raw PrismaService — never TenantedPrismaService — with explicit team: { organizationId, deletedAt: null } nested filter on EVERY query (critical: teamMember is NOT in ORG_SCOPED_MODELS)"
    - "TeamMember has no roleName field — AddTeamMemberDto has only organizationMemberId (D-04)"
    - "A path/header org mismatch returns 403 TEAM.ACCESS_DENIED"
  artifacts:
    - path: "packages/backend/src/team/team-error-codes.ts"
      provides: "TEAM error codes via createErrorCatalog"
      contains: "createErrorCatalog"
    - path: "packages/backend/src/team/persistence/team.repository.ts"
      provides: "Scoped Team CRUD (extends BaseRepository, findFirst not findUnique)"
      exports: ["TeamRepository"]
    - path: "packages/backend/src/team/persistence/team-member.repository.ts"
      provides: "Raw-PrismaService TeamMember CRUD with explicit org nested filter"
      exports: ["TeamMemberRepository"]
    - path: "packages/backend/src/team/application/team.service.ts"
      provides: "Team CRUD with assertPathMatchesContext, no transaction needed (no creator auto-add)"
      exports: ["TeamService"]
    - path: "packages/backend/src/team/application/team-member.service.ts"
      provides: "TeamMember lifecycle with ACTIVE org-member validation before upsert"
      exports: ["TeamMemberService"]
    - path: "packages/backend/src/team/api/team.controller.ts"
      provides: "REST routes for teams and team members under /organizations/:orgId/teams"
      exports: ["TeamController"]
    - path: "packages/backend/src/team/team.module.ts"
      provides: "NestJS module wiring all team providers"
      exports: ["TeamModule"]
  key_links:
    - from: "packages/backend/src/team/persistence/team-member.repository.ts"
      to: "PrismaService"
      via: "Raw PrismaService — never scopedPrisma — with team: { organizationId, deletedAt: null } nested filter"
      pattern: "team: { organizationId"
    - from: "packages/backend/src/team/application/team-member.service.ts"
      to: "packages/backend/src/tenancy/tenanted-prisma.service.ts"
      via: "scopedPrisma.client.organizationMember.findFirst for ACTIVE membership pre-check"
      pattern: "organizationMember.findFirst"
    - from: "packages/backend/src/team/application/team.service.ts"
      to: "packages/backend/src/tenancy/tenant-context.service.ts"
      via: "TenantContextService.getOrganizationId() in assertPathMatchesContext"
      pattern: "assertPathMatchesContext"
---

<objective>
Build the complete Team domain: org-scoped Team CRUD (create/list/read/update/soft-delete) and TeamMember lifecycle (add / list / remove / reactivate), wired under the URI-versioned REST surface at /v1/organizations/:orgId/teams.

Purpose: Deliver the Team + TeamMember portion of PROJ-03. Team is org-scoped (D-01) — no projectId FK. TeamMember has no roleName (D-04). TeamMember queries MUST use raw PrismaService with explicit team: { organizationId } nested filter because teamMember is absent from ORG_SCOPED_MODELS.

Output: 14 new files under packages/backend/src/team/. No existing files modified by this plan (app.module.ts wiring done in plan 07-03). Runs in parallel with plan 07-01 — zero file overlap.
</objective>

<execution_context>
@/Users/amish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/amish/AI_SDLC/.planning/PROJECT.md
@/Users/amish/AI_SDLC/.planning/ROADMAP.md
@/Users/amish/AI_SDLC/.planning/phases/07-project-foundation/07-CONTEXT.md
@/Users/amish/AI_SDLC/.planning/phases/07-project-foundation/07-RESEARCH.md
@/Users/amish/AI_SDLC/.planning/phases/07-project-foundation/07-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the live codebase. -->

From packages/backend/src/tenancy/tenanted-prisma.service.ts:
  // ORG_SCOPED_MODELS = { 'organization', 'project', 'projectMember', 'team' }
  // 'teamMember' is ABSENT — using scopedPrisma.client.teamMember.* is a silent full-table cross-tenant read
  // TeamMemberRepository MUST use raw PrismaService with explicit org nested filter

From packages/backend/src/tenancy/base-repository.ts:
  abstract class BaseRepository {
    constructor(protected readonly scopedPrisma: TenantedPrismaService, protected readonly cls: ClsService)
    protected getSoftDeleteData(): { deletedAt: Date; deletedBy: string | null }
    // TeamRepository (which covers 'team' in ORG_SCOPED_MODELS) extends this
    // TeamMemberRepository does NOT extend this — it uses raw PrismaService + ClsService only
  }

From packages/database/prisma/schema/organization.prisma (frozen — do not migrate):
  model Team {
    id             String    @id @default(cuid())
    organizationId String
    name           String
    description    String?
    type           TeamType
    createdBy      String?
    updatedBy      String?
    deletedAt      DateTime?
    deletedBy      String?
    createdAt      DateTime
    updatedAt      DateTime
    @@unique([organizationId, name])  // name duplicate → P2002 → 409 via PrismaExceptionFilter
    // NO projectId field
  }
  model TeamMember {
    id                   String    @id @default(cuid())
    teamId               String
    organizationMemberId String
    joinedAt             DateTime?
    createdBy            String?
    updatedBy            String?
    deletedAt            DateTime?
    deletedBy            String?
    createdAt            DateTime
    updatedAt            DateTime
    @@unique([teamId, organizationMemberId])  // upsert unique key
    // NO organizationId field, NO status field, NO roleName field
  }
  enum TeamType { CORE FEATURE SUPPORT TEMPORARY }

Analog files to READ before implementing:
  packages/backend/src/organization/persistence/organization.repository.ts  -- raw PrismaService pattern (TeamMemberRepository basis)
  packages/backend/src/organization/persistence/member.repository.ts        -- upsert pattern + soft-remove (TeamMemberRepository upsert)
  packages/backend/src/organization/application/organization.service.ts     -- assertPathMatchesContext + CRUD pattern (TeamService basis; no $transaction needed)
  packages/backend/src/organization/application/member.service.ts           -- addMember ACTIVE-check + removeMember (TeamMemberService basis)
  packages/backend/src/organization/application/organization.service.spec.ts -- mock setup pattern
  packages/backend/src/organization/application/member.service.spec.ts      -- mock + test pattern
  packages/backend/src/organization/api/organization.controller.ts          -- controller shape
  packages/backend/src/tenancy/tenancy-error-codes.ts                       -- createErrorCatalog usage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Error codes, DTOs, and repositories</name>
  <files>
    packages/backend/src/team/team-error-codes.ts,
    packages/backend/src/team/api/dto/create-team.dto.ts,
    packages/backend/src/team/api/dto/update-team.dto.ts,
    packages/backend/src/team/api/dto/team-response.dto.ts,
    packages/backend/src/team/api/dto/add-team-member.dto.ts,
    packages/backend/src/team/api/dto/team-member-response.dto.ts,
    packages/backend/src/team/persistence/team.repository.ts,
    packages/backend/src/team/persistence/team-member.repository.ts
  </files>
  <read_first>
    - packages/backend/src/tenancy/tenancy-error-codes.ts (createErrorCatalog pattern)
    - packages/backend/src/organization/api/dto/create-organization.dto.ts (DTO shape + validators)
    - packages/backend/src/organization/api/dto/organization-response.dto.ts (static from() + allowed fields)
    - packages/backend/src/organization/api/dto/add-member.dto.ts (add-member DTO shape)
    - packages/backend/src/organization/api/dto/member-response.dto.ts (member DTO static from())
    - packages/backend/src/organization/persistence/member.repository.ts (upsert raw PrismaService pattern)
    - packages/backend/src/organization/persistence/organization.repository.ts (raw PrismaService constructor pattern — TeamMemberRepository mirrors this)
    - packages/backend/src/tenancy/base-repository.ts (BaseRepository — TeamRepository extends it; TeamMemberRepository does NOT)
    - packages/database/prisma/schema/organization.prisma (Team + TeamMember exact fields — note NO status on TeamMember, NO roleName on TeamMember, NO organizationId on TeamMember)
  </read_first>
  <behavior>
    - Test 1 (compile gate): TypeScript accepts TeamMemberRepository without BaseRepository extension
    - Test 2: TeamResponseDto.from(team) exposes id, organizationId, name, description, type, createdAt, createdBy — no deletedAt/deletedBy/updatedBy/updatedAt
    - Test 3: TeamMemberResponseDto.from(tm) exposes id, teamId, organizationMemberId, joinedAt, createdAt — no status field (TeamMember has none in schema)
    - Test 4: AddTeamMemberDto has only organizationMemberId — no roleName field (D-04)
    - Test 5: team-member.repository.ts every method contains team: { organizationId nested filter — grep assertion
    - Test 6: team-member.repository.ts does NOT import or use TenantedPrismaService — grep assertion
    - Test 7: team.repository.ts uses findFirst (not findUnique) for findById
  </behavior>
  <action>
    team-error-codes.ts: createErrorCatalog('TEAM', ['NAME_DUPLICATE', 'MEMBER_NOT_ORG_MEMBER', 'ACCESS_DENIED'] as const). Import from '../common/error-catalog/create-error-catalog'.

    CreateTeamDto: @IsString() @MinLength(2) @MaxLength(100) name; @IsOptional() @IsString() @MaxLength(500) description?; @IsOptional() @IsEnum(TeamType) type? (import TeamType from '@repo/database', default to 'CORE' if omitted).

    UpdateTeamDto: @IsOptional() on all fields from CreateTeamDto (name, description, type).

    TeamResponseDto: import Team from '@repo/database'. Fields: id, organizationId, name, description, type, createdAt, createdBy. Static from(team: Team) with explicit mapping — never expose deletedAt/deletedBy/updatedBy/updatedAt.

    AddTeamMemberDto: ONLY @IsString() @IsUUID() organizationMemberId. NO roleName field (D-04 — TeamMember has no roleName in the frozen schema).

    TeamMemberResponseDto: import TeamMember from '@repo/database'. Fields: id, teamId, organizationMemberId, joinedAt, createdAt. No status field (TeamMember has no status column). Static from(tm: TeamMember) with explicit mapping.

    TeamRepository extends BaseRepository: constructor(scopedPrisma: TenantedPrismaService, cls: ClsService). Methods:
      findMany(query?: CursorPaginationDto): this.scopedPrisma.client.team.findMany() with cursor pagination.
      findById(id: string): this.scopedPrisma.client.team.findFirst({ where: { id } }) — NEVER findUnique.
      create(data: {organizationId,name,description?,type,createdBy}): this.scopedPrisma.client.team.create({ data }).
      update(id, data): this.scopedPrisma.client.team.updateMany({ where: { id }, data }).
      softDelete(id): this.scopedPrisma.client.team.updateMany({ where: { id }, data: this.getSoftDeleteData() }).

    TeamMemberRepository: DOES NOT extend BaseRepository. Constructor: (private readonly prisma: PrismaService, private readonly cls: ClsService). Private getOrgId(): string — reads this.cls.get<string>('organizationId'); if falsy throw ForbiddenException({ errorCode: TENANT_ERROR_CODES.NO_ORG_CONTEXT, message: 'No active organization context.' }).

    TeamMemberRepository methods:
      findManyByTeam(teamId: string): const organizationId = this.getOrgId(); return this.prisma.teamMember.findMany({ where: { teamId, deletedAt: null, team: { organizationId, deletedAt: null } } }).
      upsertMember(teamId, organizationMemberId, actorUserId): this.prisma.teamMember.upsert({ where: { teamId_organizationMemberId: { teamId, organizationMemberId } }, create: { teamId, organizationMemberId, joinedAt: new Date(), createdBy: actorUserId }, update: { deletedAt: null, joinedAt: new Date(), updatedBy: actorUserId } }). No organizationId in create — TeamMember has no organizationId FK.
      softRemove(id, teamId, actorUserId): const organizationId = this.getOrgId(); return this.prisma.teamMember.updateMany({ where: { id, teamId, deletedAt: null, team: { organizationId, deletedAt: null } }, data: { deletedAt: new Date(), deletedBy: actorUserId } }).

    Import TENANT_ERROR_CODES from '../../tenancy/tenancy-error-codes' in team-member.repository.ts for the NO_ORG_CONTEXT error code. Import PrismaService and TeamMember from '@repo/database'.
  </action>
  <verify>
    <automated>npm run build --workspace=packages/backend -- --noEmit 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - team-error-codes.ts exports TEAM_ERROR_CODES via createErrorCatalog('TEAM', ...)
    - AddTeamMemberDto has organizationMemberId field and does NOT have roleName field (D-04)
    - TeamMemberResponseDto.from() does NOT reference a status field (TeamMember has no status in schema)
    - team.repository.ts contains findFirst and does NOT contain findUnique
    - team-member.repository.ts does NOT extend BaseRepository and does NOT import TenantedPrismaService
    - team-member.repository.ts every query contains team: { organizationId — grep confirms for findManyByTeam and softRemove
    - team-member.repository.ts upsertMember create block does NOT contain organizationId (TeamMember has no organizationId FK)
    - npm run build --workspace=packages/backend -- --noEmit exits 0 (or no errors in src/team/)
  </acceptance_criteria>
  <done>All 8 files created; TeamMemberRepository uses raw PrismaService with explicit org nested filter on every query; TeamRepository extends BaseRepository and uses findFirst.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Team services and unit tests</name>
  <files>
    packages/backend/src/team/application/team.service.ts,
    packages/backend/src/team/application/team.service.spec.ts,
    packages/backend/src/team/application/team-member.service.ts,
    packages/backend/src/team/application/team-member.service.spec.ts
  </files>
  <read_first>
    - packages/backend/src/organization/application/organization.service.ts (assertPathMatchesContext + CRUD — TeamService mirrors this; no $transaction needed since team create does NOT auto-add creator)
    - packages/backend/src/organization/application/organization.service.spec.ts (mock setup + IDOR guard test)
    - packages/backend/src/organization/application/member.service.ts (addMember ACTIVE-check + soft-delete — TeamMemberService mirrors this)
    - packages/backend/src/organization/application/member.service.spec.ts (mock setup + test patterns)
    - packages/backend/src/team/team-error-codes.ts (just created — use TEAM_ERROR_CODES.ACCESS_DENIED / MEMBER_NOT_ORG_MEMBER)
    - packages/backend/src/team/persistence/team.repository.ts (just created — method signatures)
    - packages/backend/src/team/persistence/team-member.repository.ts (just created — method signatures)
  </read_first>
  <behavior>
    - Test 1: TeamService.createTeam — assertPathMatchesContext called; calls teamRepo.create with organizationId from CLS, actorUserId from ctx
    - Test 2: TeamService.createTeam — path/CLS mismatch → ForbiddenException with errorCode TEAM.ACCESS_DENIED
    - Test 3: TeamService.softDeleteTeam — calls teamRepo.softDelete; verifies getSoftDeleteData applied (deletedAt set)
    - Test 4: TeamService.findById — path mismatch → TEAM.ACCESS_DENIED; not-found → NotFoundException
    - Test 5: TeamMemberService.addMember — validates organizationMemberId is ACTIVE in org via scopedPrisma.client.organizationMember.findFirst; throws TEAM.MEMBER_NOT_ORG_MEMBER if not found
    - Test 6: TeamMemberService.addMember — calls teamMemberRepo.upsertMember on success
    - Test 7: TeamMemberService.removeMember — calls teamMemberRepo.softRemove; no last-member guardrail check
  </behavior>
  <action>
    TeamService constructor: (private readonly teamRepo: TeamRepository, private readonly ctx: TenantContextService). No PrismaService injection needed — team create does NOT auto-add creator as member (D-05 specifies no creator auto-add for teams, unlike D-08 for projects).

    createTeam(orgId, dto: CreateTeamDto): Promise<Team> — call assertPathMatchesContext(orgId); const actorUserId = this.ctx.getUserId()!; return this.teamRepo.create({ organizationId, name: dto.name, description: dto.description ?? null, type: dto.type ?? 'CORE', createdBy: actorUserId }).

    listTeams(orgId, query): assertPathMatchesContext(orgId); return this.teamRepo.findMany(query).

    findById(orgId, teamId): assertPathMatchesContext(orgId); const team = await this.teamRepo.findById(teamId); if (!team) throw NotFoundException({ errorCode: TEAM_ERROR_CODES.ACCESS_DENIED, message: 'Team not found.' }); return team.

    updateTeam(orgId, teamId, dto): assertPathMatchesContext(orgId); return this.teamRepo.update(teamId, dto).

    softDeleteTeam(orgId, teamId): assertPathMatchesContext(orgId); return this.teamRepo.softDelete(teamId).

    Private assertPathMatchesContext(orgId): compare against this.ctx.getOrganizationId(); mismatch → ForbiddenException({ errorCode: TEAM_ERROR_CODES.ACCESS_DENIED, message: 'Access denied.' }). Return organizationId.

    TeamMemberService constructor: (private readonly teamMemberRepo: TeamMemberRepository, private readonly scopedPrisma: TenantedPrismaService, private readonly ctx: TenantContextService).

    addMember(orgId, teamId, dto: AddTeamMemberDto): assertPathMatchesContext(orgId); validate: const member = await this.scopedPrisma.client.organizationMember.findFirst({ where: { id: dto.organizationMemberId, status: 'ACTIVE' } }); if (!member) throw NotFoundException({ errorCode: TEAM_ERROR_CODES.MEMBER_NOT_ORG_MEMBER, message: 'Organization member not found or not active.' }); actorUserId = this.ctx.getUserId()!; return this.teamMemberRepo.upsertMember(teamId, dto.organizationMemberId, actorUserId).

    listMembers(orgId, teamId): assertPathMatchesContext(orgId); return this.teamMemberRepo.findManyByTeam(teamId).

    removeMember(orgId, teamId, memberId): assertPathMatchesContext(orgId); const actorUserId = this.ctx.getUserId() ?? null; return this.teamMemberRepo.softRemove(memberId, teamId, actorUserId). No last-member guardrail (D-05).

    Private assertPathMatchesContext mirrors the project-service pattern, uses TEAM_ERROR_CODES.ACCESS_DENIED.

    team.service.spec.ts: mockTeamRepo: { create, findMany, findById, update, softDelete } — all vi.fn(). mockCtx: { getOrganizationId, getUserId } — vi.fn(). Cover tests 1–4.

    team-member.service.spec.ts: mockTeamMemberRepo: { upsertMember, findManyByTeam, softRemove } — vi.fn(). mockScopedPrisma: { client: { organizationMember: { findFirst: vi.fn() } } }. mockCtx. Cover tests 5–7.
  </action>
  <verify>
    <automated>npm run test --workspace=packages/backend -- --run --reporter=verbose src/team/application/team.service.spec.ts src/team/application/team-member.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - team.service.spec.ts exits 0 with all tests passing
    - team-member.service.spec.ts exits 0 with all tests passing
    - team.service.ts does NOT call this.prisma.$transaction (no creator auto-add for teams — D-05)
    - team.service.ts createTeam does NOT auto-add a TeamMember for the creator (confirmed by test and code inspection)
    - team-member.service.ts addMember calls this.scopedPrisma.client.organizationMember.findFirst before upsert
    - team-member.service.ts removeMember does NOT contain a count or last-member check
    - Test: addMember with non-ACTIVE organizationMemberId → TEAM.MEMBER_NOT_ORG_MEMBER
    - Test: createTeam path mismatch → TEAM.ACCESS_DENIED
  </acceptance_criteria>
  <done>Both service specs pass; TeamService uses no $transaction; TeamMemberService validates ACTIVE org membership before upsert.</done>
</task>

<task type="auto">
  <name>Task 3: Team controller and module</name>
  <files>
    packages/backend/src/team/api/team.controller.ts,
    packages/backend/src/team/team.module.ts
  </files>
  <read_first>
    - packages/backend/src/organization/api/organization.controller.ts (controller shape — @RequirePermissions, @GetCurrentUser, route structure)
    - packages/backend/src/organization/organization.module.ts (module structure — imports TenancyModule, providers list)
    - packages/backend/src/team/application/team.service.ts (just created — method signatures)
    - packages/backend/src/team/application/team-member.service.ts (just created — method signatures)
  </read_first>
  <action>
    TeamController: @Controller({ path: 'organizations/:orgId/teams', version: '1' }). No @NoTenantScope on any route.

    Use 'organization:read' and 'organization:manage' permission codes (not team:* codes — A-02 in RESEARCH.md; team-specific RBAC deferred, org-level codes are the correct approximation per D-05).

    Routes (all accept @Param('orgId') orgId: string):
      POST /        — @RequirePermissions('organization:manage'); @HttpCode(201); body: CreateTeamDto; returns TeamResponseDto.from(result)
      GET /         — @RequirePermissions('organization:read'); @Query() query: CursorPaginationDto; returns PaginatedResult<TeamResponseDto> as { data: items.map(TeamResponseDto.from), meta }
      GET /:teamId  — @RequirePermissions('organization:read'); returns TeamResponseDto.from(result)
      PATCH /:teamId — @RequirePermissions('organization:manage'); body: UpdateTeamDto; returns TeamResponseDto.from(result)
      DELETE /:teamId — @RequirePermissions('organization:manage'); @HttpCode(204); returns void

    TeamMember sub-routes:
      POST /:teamId/members  — @RequirePermissions('organization:manage'); @HttpCode(201); body: AddTeamMemberDto; returns TeamMemberResponseDto.from(result)
      GET /:teamId/members   — @RequirePermissions('organization:read'); returns TeamMemberResponseDto[] from listMembers result
      DELETE /:teamId/members/:memberId — @RequirePermissions('organization:manage'); @HttpCode(204); returns void

    Constructor: (private readonly teamService: TeamService, private readonly teamMemberService: TeamMemberService).

    TeamModule: @Module({ imports: [TenancyModule], controllers: [TeamController], providers: [TeamService, TeamMemberService, TeamRepository, TeamMemberRepository] }). No exports.
  </action>
  <verify>
    <automated>npm run build --workspace=packages/backend -- --noEmit 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TeamController @Controller path is 'organizations/:orgId/teams' with version: '1'
    - Team CRUD routes use 'organization:read' / 'organization:manage' (not project:* or team:* codes)
    - No @NoTenantScope decorator in team.controller.ts
    - TeamController has 8 route handlers (POST /, GET /, GET /:teamId, PATCH /:teamId, DELETE /:teamId, POST /:teamId/members, GET /:teamId/members, DELETE /:teamId/members/:memberId)
    - team.module.ts imports TenancyModule and lists all 4 providers: TeamService, TeamMemberService, TeamRepository, TeamMemberRepository
    - npm run build --workspace=packages/backend -- --noEmit exits 0
  </acceptance_criteria>
  <done>TeamController and TeamModule compile cleanly; all 8 routes wired with correct permission guards; no @NoTenantScope routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → TeamController | Untrusted orgId path param + teamId path param + request body cross here |
| TeamService → TenantContext CLS | CLS reads must match the URL path params; mismatch = IDOR attempt |
| TeamMemberRepository → PrismaService | teamMember has no organizationId FK; every query must include nested team: { organizationId } filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | TeamMemberRepository | mitigate | Raw PrismaService only (never TenantedPrismaService); every query includes explicit team: { organizationId, deletedAt: null } nested filter; cross-tenant TeamMember read is structurally impossible with this pattern |
| T-07-07 | Elevation of Privilege | TeamService / TeamMemberService | mitigate | assertPathMatchesContext(orgId) called at entry of every service method; path ≠ CLS org → 403 TEAM.ACCESS_DENIED |
| T-07-08 | Elevation of Privilege | TeamMemberService.addMember | mitigate | scopedPrisma.client.organizationMember.findFirst({ where: { id, status: 'ACTIVE' } }) before upsert; non-ACTIVE or cross-org orgMemberId → 404 TEAM.MEMBER_NOT_ORG_MEMBER; scopedPrisma auto-scopes organizationMember to current org |
| T-07-09 | Information Disclosure | TeamResponseDto / TeamMemberResponseDto | mitigate | Explicit static from() mapping; deletedAt/deletedBy/updatedBy/updatedAt excluded; TeamMemberResponseDto has no status field (consistent with schema) |
| T-07-10 | Tampering | TeamController request bodies | accept | GlobalValidationPipe (whitelist + forbidNonWhitelisted) already wired; strips unknown fields |
| T-07-11 | Tampering | AddTeamMemberDto | mitigate | No roleName field in DTO or schema (D-04); class-validator @IsUUID() on organizationMemberId |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase |
</threat_model>

<verification>
Per-plan gate:
  npm run test --workspace=packages/backend -- --run src/team/
  npm run build --workspace=packages/backend -- --noEmit 2>&1 | grep "error TS"

Critical assertion (run manually):
  grep -n "team: { organizationId" packages/backend/src/team/persistence/team-member.repository.ts
  # Must appear in findManyByTeam and softRemove methods
</verification>

<success_criteria>
- 14 files exist under packages/backend/src/team/
- TypeScript compilation is clean
- team.service.spec.ts and team-member.service.spec.ts both pass
- TeamMemberRepository uses raw PrismaService with team: { organizationId, deletedAt: null } nested filter on every query
- TeamMemberRepository does NOT extend BaseRepository and does NOT import TenantedPrismaService
- AddTeamMemberDto has only organizationMemberId field (no roleName)
- TeamMember soft-remove sets deletedAt; no last-member guardrail
- PROJ-03 (Team + TeamMember portion) addressed
</success_criteria>

<output>
Create .planning/phases/07-project-foundation/07-02-SUMMARY.md when done.
</output>
