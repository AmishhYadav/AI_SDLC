---
phase: "07-project-foundation"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/project/project-error-codes.ts
  - packages/backend/src/project/api/dto/create-project.dto.ts
  - packages/backend/src/project/api/dto/update-project.dto.ts
  - packages/backend/src/project/api/dto/project-response.dto.ts
  - packages/backend/src/project/api/dto/add-project-member.dto.ts
  - packages/backend/src/project/api/dto/project-member-response.dto.ts
  - packages/backend/src/project/persistence/project.repository.ts
  - packages/backend/src/project/persistence/project-member.repository.ts
  - packages/backend/src/project/application/project.service.ts
  - packages/backend/src/project/application/project.service.spec.ts
  - packages/backend/src/project/application/project-member.service.ts
  - packages/backend/src/project/application/project-member.service.spec.ts
  - packages/backend/src/project/api/project.controller.ts
  - packages/backend/src/project/project.module.ts
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03

must_haves:
  truths:
    - "POST /v1/organizations/:orgId/projects creates a project AND auto-adds the creator as an ACTIVE ProjectMember with roleName=OWNER in a single atomic transaction (PROJ-01, D-08)"
    - "GET /v1/organizations/:orgId/projects returns all non-deleted projects in the active org, scoped only by organizationId — no ProjectMember gate (PROJ-02, D-06)"
    - "GET /v1/organizations/:orgId/projects/:projectId returns a single project with assertPathMatchesContext enforced (PROJ-02)"
    - "POST /v1/organizations/:orgId/projects/:projectId/members validates that the target organizationMemberId is ACTIVE in the current org before upsert; re-add reactivates a soft-removed member (PROJ-03, D-10)"
    - "DELETE /v1/organizations/:orgId/projects/:projectId/members/:memberId soft-deletes (status=REMOVED, deletedAt set) without a last-member guardrail (D-10)"
    - "A duplicate project key within the same org is rejected with a 409 (via PrismaExceptionFilter P2002 path)"
    - "An invalid project key format is rejected with a 400 (via class-validator @Matches)"
    - "A path/header org mismatch returns 403 PROJECT.ACCESS_DENIED"
  artifacts:
    - path: "packages/backend/src/project/project-error-codes.ts"
      provides: "PROJECT and PROJECT_MEMBER error codes via createErrorCatalog"
      contains: "createErrorCatalog"
    - path: "packages/backend/src/project/persistence/project.repository.ts"
      provides: "Scoped Project CRUD (findFirst not findUnique)"
      exports: ["ProjectRepository"]
    - path: "packages/backend/src/project/persistence/project-member.repository.ts"
      provides: "Scoped ProjectMember reads + raw upsert + soft-remove"
      exports: ["ProjectMemberRepository"]
    - path: "packages/backend/src/project/application/project.service.ts"
      provides: "Project CRUD with assertPathMatchesContext + atomic $transaction creator-add"
      exports: ["ProjectService"]
    - path: "packages/backend/src/project/application/project-member.service.ts"
      provides: "ProjectMember lifecycle with ACTIVE org-member validation before upsert"
      exports: ["ProjectMemberService"]
    - path: "packages/backend/src/project/api/project.controller.ts"
      provides: "REST routes for projects and project members under /organizations/:orgId/projects"
      exports: ["ProjectController"]
    - path: "packages/backend/src/project/project.module.ts"
      provides: "NestJS module wiring all project providers"
      exports: ["ProjectModule"]
  key_links:
    - from: "packages/backend/src/project/application/project.service.ts"
      to: "packages/backend/src/project/persistence/project.repository.ts"
      via: "ProjectRepository injected in constructor"
      pattern: "ProjectRepository"
    - from: "packages/backend/src/project/application/project-member.service.ts"
      to: "packages/backend/src/tenancy/tenanted-prisma.service.ts"
      via: "TenantedPrismaService.client.organizationMember.findFirst for pre-upsert ACTIVE check"
      pattern: "organizationMember.findFirst"
    - from: "packages/backend/src/project/application/project.service.ts"
      to: "packages/backend/src/tenancy/tenant-context.service.ts"
      via: "TenantContextService.getOrganizationId() in assertPathMatchesContext"
      pattern: "assertPathMatchesContext"
---

<objective>
Build the complete Project domain: org-scoped Project CRUD, ProjectMember lifecycle (add / list / remove / reactivate), and the NestJS module that wires them under the URI-versioned REST surface at /v1/organizations/:orgId/projects.

Purpose: Deliver PROJ-01 (project create + creator auto-add), PROJ-02 (list/read all org projects), and the ProjectMember portion of PROJ-03 (add/list/remove members with reactivate-on-readd).

Output: 14 new files under packages/backend/src/project/. No existing files modified by this plan (app.module.ts wiring done in plan 07-03).
</objective>

<execution_context>
@/Users/amish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/amish/AI_SDLC/.planning/PROJECT.md
@/Users/amish/AI_SDLC/.planning/ROADMAP.md
@/Users/amish/AI_SDLC/.planning/phases/07-project-foundation/07-CONTEXT.md
@/Users/amish/AI_SDLC/.planning/phases/07-project-foundation/07-RESEARCH.md
@/Users/amish/AI_SDLC/.planning/phases/07-project-foundation/07-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the live codebase. -->
<!-- Read these files before implementing; do not re-derive from memory. -->

From packages/backend/src/tenancy/base-repository.ts:
  abstract class BaseRepository {
    constructor(protected readonly scopedPrisma: TenantedPrismaService, protected readonly cls: ClsService)
    protected getSoftDeleteData(): { deletedAt: Date; deletedBy: string | null }
    // MUST use findFirst (not findUnique) for all scoped single-row lookups
    // Raw PrismaService for upsert (RESEARCH A3)
  }

From packages/backend/src/tenancy/tenant-context.service.ts:
  class TenantContextService {
    getOrganizationId(): string;
    getUserId(): string | null;
    getOrganizationMemberId(): string | null;
  }

From packages/backend/src/tenancy/tenanted-prisma.service.ts:
  // ORG_SCOPED_MODELS includes: 'organization', 'project', 'projectMember', 'team'
  // 'teamMember' is NOT listed — do not use scopedPrisma for TeamMember
  class TenantedPrismaService {
    get client(): ExtendedPrismaClient  // auto-injects organizationId + deletedAt:null
  }

From packages/backend/src/common/error-catalog/create-error-catalog.ts:
  function createErrorCatalog(namespace: string, codes: readonly string[]): Record<...>
  // Usage: createErrorCatalog('PROJECT', ['KEY_DUPLICATE', ...] as const)

From packages/backend/src/common/pagination/cursor-pagination.dto.ts:
  class CursorPaginationDto { cursor?: string; limit?: number }

From packages/backend/src/common/pagination/pagination-meta.interface.ts:
  interface PaginatedResult<T> { data: T[]; meta: { nextCursor: string | null; hasNextPage: boolean } }

Analog files to READ before implementing (exact patterns to mirror):
  packages/backend/src/organization/application/organization.service.ts    -- createOrganization $transaction pattern + IDOR guard
  packages/backend/src/organization/application/member.service.ts          -- addMember/removeMember pattern + assertPathMatchesContext
  packages/backend/src/organization/persistence/member.repository.ts       -- BaseRepository extension + upsertMember raw PrismaService
  packages/backend/src/organization/api/organization.controller.ts         -- controller shape, @RequirePermissions, @GetCurrentUser
  packages/backend/src/organization/api/dto/organization-response.dto.ts   -- static from() pattern
  packages/backend/src/organization/api/dto/member-response.dto.ts         -- member DTO static from() pattern
  packages/backend/src/tenancy/tenancy-error-codes.ts                      -- createErrorCatalog usage pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Error codes, DTOs, and repositories</name>
  <files>
    packages/backend/src/project/project-error-codes.ts,
    packages/backend/src/project/api/dto/create-project.dto.ts,
    packages/backend/src/project/api/dto/update-project.dto.ts,
    packages/backend/src/project/api/dto/project-response.dto.ts,
    packages/backend/src/project/api/dto/add-project-member.dto.ts,
    packages/backend/src/project/api/dto/project-member-response.dto.ts,
    packages/backend/src/project/persistence/project.repository.ts,
    packages/backend/src/project/persistence/project-member.repository.ts
  </files>
  <read_first>
    - packages/backend/src/tenancy/tenancy-error-codes.ts (exact createErrorCatalog call to mirror)
    - packages/backend/src/organization/api/dto/create-organization.dto.ts (DTO shape + validator decorators)
    - packages/backend/src/organization/api/dto/organization-response.dto.ts (static from() + allowed fields)
    - packages/backend/src/organization/api/dto/member-response.dto.ts (member DTO static from())
    - packages/backend/src/organization/api/dto/add-member.dto.ts (add-member DTO shape)
    - packages/backend/src/organization/persistence/member.repository.ts (BaseRepository extension + upsert pattern)
    - packages/backend/src/tenancy/base-repository.ts (getSoftDeleteData, findFirst rule)
    - packages/database/prisma/schema/organization.prisma (Project + ProjectMember exact fields — key required, @@unique([organizationId,key]), @@unique([projectId,organizationMemberId]))
  </read_first>
  <behavior>
    - Test 1 (compile gate): TS compiler accepts CreateProjectDto with @Matches(/^[A-Z][A-Z0-9]{1,9}$/) on key field
    - Test 2: ProjectResponseDto.from(project) returns object without deletedAt/deletedBy/updatedBy fields
    - Test 3: ProjectMemberResponseDto.from(pm) exposes id, organizationId, projectId, organizationMemberId, roleName, status, joinedAt, createdAt — no audit columns
    - Test 4: ProjectRepository.findById uses findFirst (not findUnique) — grep assertion
    - Test 5: ProjectMemberRepository.upsertMember uses raw this.prisma.projectMember.upsert (not scopedPrisma) — grep assertion
  </behavior>
  <action>
    Create project-error-codes.ts using createErrorCatalog('PROJECT', ['KEY_DUPLICATE', 'KEY_INVALID_FORMAT', 'MEMBER_NOT_ORG_MEMBER', 'ACCESS_DENIED'] as const). Import from '../common/error-catalog/create-error-catalog'.

    CreateProjectDto: @IsString() @MinLength(2) @MaxLength(100) name; @IsString() @Matches(/^[A-Z][A-Z0-9]{1,9}$/, { message: 'key must be 2–10 uppercase letters/digits starting with a letter (e.g. PLAT, PROJ1)' }) key; @IsOptional() @IsString() @MaxLength(500) description?.

    UpdateProjectDto: @IsOptional() on name and description — key is NOT updatable (omit from update DTO).

    ProjectResponseDto: import Project from '@repo/database'. Fields: id, organizationId, name, key, description, status, createdAt, createdBy. Static from(p: Project): ProjectResponseDto with explicit field mapping only — never spread or expose deletedAt/deletedBy/updatedBy/updatedAt.

    AddProjectMemberDto: @IsString() @IsUUID() organizationMemberId; @IsOptional() @IsString() @MaxLength(50) roleName?. No email field (D-10: add by organizationMemberId).

    ProjectMemberResponseDto: import ProjectMember from '@repo/database'. Fields: id, organizationId, projectId, organizationMemberId, roleName, status, joinedAt, createdAt. Static from(pm: ProjectMember) with explicit mapping.

    ProjectRepository extends BaseRepository: constructor(scopedPrisma: TenantedPrismaService, cls: ClsService) — no raw PrismaService needed here (no upsert). Methods:
      findMany(query?: CursorPaginationDto): uses this.scopedPrisma.client.project.findMany() with cursor pagination if cursor provided.
      findById(id: string): uses this.scopedPrisma.client.project.findFirst({ where: { id } }) — NEVER findUnique.
      update(id: string, data: Partial<{name,description}>): this.scopedPrisma.client.project.updateMany({ where: { id }, data }).
      softDelete(id: string): this.scopedPrisma.client.project.updateMany({ where: { id }, data: { ...this.getSoftDeleteData(), status: 'ARCHIVED' } }).

    ProjectMemberRepository extends BaseRepository: constructor(scopedPrisma: TenantedPrismaService, cls: ClsService, private readonly prisma: PrismaService). Methods:
      findManyByProject(projectId: string): this.scopedPrisma.client.projectMember.findMany({ where: { projectId } }).
      findById(id: string): this.scopedPrisma.client.projectMember.findFirst({ where: { id } }).
      upsertMember(organizationId, projectId, organizationMemberId, roleName, actorUserId): raw this.prisma.projectMember.upsert using @@unique key projectId_organizationMemberId: { projectId, organizationMemberId }. Create data: { organizationId, projectId, organizationMemberId, status: 'ACTIVE', roleName, joinedAt: new Date(), createdBy: actorUserId }. Update data: { status: 'ACTIVE', deletedAt: null, joinedAt: new Date(), roleName, updatedBy: actorUserId }.
      softRemove(id: string, actorUserId: string): this.scopedPrisma.client.projectMember.updateMany({ where: { id }, data: { status: 'REMOVED', ...this.getSoftDeleteData() } }).

    Import PrismaService from '@repo/database'. Import ClsService from 'nestjs-cls'. Import TenantedPrismaService from '../../tenancy/tenanted-prisma.service'.
  </action>
  <verify>
    <automated>npm run build --workspace=packages/backend -- --noEmit 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - project-error-codes.ts contains createErrorCatalog('PROJECT', and exports PROJECT_ERROR_CODES
    - CreateProjectDto.key has @Matches(/^[A-Z][A-Z0-9]{1,9}$/) — grep confirms
    - UpdateProjectDto has no key field
    - ProjectResponseDto.from() does NOT reference deletedAt, deletedBy, updatedBy, updatedAt
    - AddProjectMemberDto has organizationMemberId (UUID) field and optional roleName; no email field
    - project.repository.ts contains findFirst and does NOT contain findUnique
    - project-member.repository.ts contains this.prisma.projectMember.upsert (raw) for upsertMember
    - project-member.repository.ts upsert create block contains organizationId: organizationId
    - project-member.repository.ts softRemove sets status: 'REMOVED'
    - npm run build --workspace=packages/backend -- --noEmit exits 0 (or produces no TS errors in src/project/)
  </acceptance_criteria>
  <done>All 8 files created; TypeScript compiles without errors in the project/ directory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project services and unit tests</name>
  <files>
    packages/backend/src/project/application/project.service.ts,
    packages/backend/src/project/application/project.service.spec.ts,
    packages/backend/src/project/application/project-member.service.ts,
    packages/backend/src/project/application/project-member.service.spec.ts
  </files>
  <read_first>
    - packages/backend/src/organization/application/organization.service.ts (createOrganization $transaction + assertPathMatchesContext IDOR guard — the exact pattern to mirror)
    - packages/backend/src/organization/application/organization.service.spec.ts (mock setup + transaction test pattern)
    - packages/backend/src/organization/application/member.service.ts (addMember ACTIVE-user check + removeMember soft-delete)
    - packages/backend/src/organization/application/member.service.spec.ts (mock setup + test patterns)
    - packages/backend/src/project/project-error-codes.ts (just created in Task 1 — use PROJECT_ERROR_CODES.ACCESS_DENIED / MEMBER_NOT_ORG_MEMBER)
    - packages/backend/src/project/persistence/project.repository.ts (just created — know the method signatures)
    - packages/backend/src/project/persistence/project-member.repository.ts (just created — know upsertMember signature)
    - packages/database/prisma/schema/organization.prisma (ProjectMemberStatus enum values)
  </read_first>
  <behavior>
    - Test 1: ProjectService.createProject — assertPathMatchesContext called; $transaction creates project then projectMember with status=ACTIVE, roleName='OWNER', organizationId explicit
    - Test 2: ProjectService.createProject — path/CLS mismatch throws ForbiddenException with errorCode PROJECT.ACCESS_DENIED
    - Test 3: ProjectService.findById — path/CLS mismatch throws ForbiddenException with PROJECT.ACCESS_DENIED
    - Test 4: ProjectService.listProjects — calls projectRepo.findMany(query), no membership gate
    - Test 5: ProjectMemberService.addMember — validates organizationMemberId is ACTIVE in org via scopedPrisma.client.organizationMember.findFirst; throws PROJECT.MEMBER_NOT_ORG_MEMBER if not found
    - Test 6: ProjectMemberService.addMember — calls projectMemberRepo.upsertMember on success (handles re-add reactivation transparently)
    - Test 7: ProjectMemberService.removeMember — calls projectMemberRepo.softRemove; no last-member count check (D-10)
  </behavior>
  <action>
    ProjectService constructor: (private readonly projectRepo: ProjectRepository, private readonly projectMemberRepo: ProjectMemberRepository, private readonly prisma: PrismaService, private readonly ctx: TenantContextService).

    createProject(orgId: string, dto: CreateProjectDto): Promise<Project> — call assertPathMatchesContext(orgId) first, then this.prisma.$transaction(async (tx) => { create project with tx.project.create({ data: { organizationId, name: dto.name, key: dto.key, description: dto.description ?? null, status: 'ACTIVE', createdBy: actorUserId } }); create projectMember with tx.projectMember.create({ data: { organizationId, projectId: project.id, organizationMemberId: orgMemberId, status: 'ACTIVE', roleName: 'OWNER', joinedAt: new Date(), createdBy: actorUserId } }); return project; }). Read actorUserId from this.ctx.getUserId()!, orgMemberId from this.ctx.getOrganizationMemberId()!.

    listProjects(orgId: string, query: CursorPaginationDto): call assertPathMatchesContext(orgId), return this.projectRepo.findMany(query).

    findById(orgId: string, projectId: string): Promise<Project> — call assertPathMatchesContext(orgId), fetch from projectRepo.findById(projectId), throw NotFoundException({ errorCode: PROJECT_ERROR_CODES.ACCESS_DENIED, message: 'Project not found.' }) if null.

    updateProject(orgId, projectId, dto: UpdateProjectDto): assertPathMatchesContext(orgId), then projectRepo.update(projectId, dto).

    softDeleteProject(orgId, projectId): assertPathMatchesContext(orgId), then projectRepo.softDelete(projectId).

    Private assertPathMatchesContext(orgId: string): string — compare orgId against this.ctx.getOrganizationId(); if mismatch throw ForbiddenException({ errorCode: PROJECT_ERROR_CODES.ACCESS_DENIED, message: 'Access denied.' }); return organizationId.

    ProjectMemberService constructor: (private readonly projectMemberRepo: ProjectMemberRepository, private readonly scopedPrisma: TenantedPrismaService, private readonly prisma: PrismaService, private readonly ctx: TenantContextService). Note: scopedPrisma is needed for the ACTIVE org-member validation check (organizationMember IS in ORG_SCOPED_MODELS).

    addMember(orgId, projectId, dto: AddProjectMemberDto): Promise<ProjectMember> — call assertPathMatchesContext(orgId), then validate: const member = await this.scopedPrisma.client.organizationMember.findFirst({ where: { id: dto.organizationMemberId, status: 'ACTIVE' } }); if (!member) throw NotFoundException({ errorCode: PROJECT_ERROR_CODES.MEMBER_NOT_ORG_MEMBER, message: 'Organization member not found or not active.' }); then call this.projectMemberRepo.upsertMember(organizationId, projectId, dto.organizationMemberId, dto.roleName ?? null, actorUserId).

    listMembers(orgId, projectId): assertPathMatchesContext(orgId), return projectMemberRepo.findManyByProject(projectId).

    removeMember(orgId, projectId, memberId): assertPathMatchesContext(orgId); actorUserId = this.ctx.getUserId() ?? null; return this.projectMemberRepo.softRemove(memberId, actorUserId). No last-member guardrail (D-10 explicitly excludes this).

    Private assertPathMatchesContext mirrors MemberService.assertPathMatchesContext but uses PROJECT_ERROR_CODES.ACCESS_DENIED.

    project.service.spec.ts: Mock setup per PATTERNS.md § project.service.spec.ts. mockTx: { project: { create: vi.fn() }, projectMember: { create: vi.fn() } }. mockPrisma: { $transaction: vi.fn() } — re-arm in beforeEach with mockImplementation(async cb => cb(mockTx)). mockCtx: getOrganizationId/getUserId/getOrganizationMemberId all vi.fn(). Cover all 7 behaviors listed above.

    project-member.service.spec.ts: mockProjectMemberRepo: { upsertMember, findManyByProject, softRemove }. mockScopedPrisma: { client: { organizationMember: { findFirst: vi.fn() } } }. mockCtx. Cover behaviors 5, 6, 7.
  </action>
  <verify>
    <automated>npm run test --workspace=packages/backend -- --run --reporter=verbose src/project/application/project.service.spec.ts src/project/application/project-member.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - project.service.spec.ts exits 0 with all describe blocks passing
    - project-member.service.spec.ts exits 0 with all describe blocks passing
    - project.service.ts createProject calls this.prisma.$transaction (not scopedPrisma.$transaction)
    - project.service.ts createProject passes organizationId explicitly in both tx.project.create and tx.projectMember.create data
    - project.service.ts createProject sets roleName: 'OWNER' on the auto-added ProjectMember (D-08)
    - project-member.service.ts addMember calls this.scopedPrisma.client.organizationMember.findFirst before upsert
    - project-member.service.ts removeMember does NOT contain a count or last-member check
    - Test: addMember with non-ACTIVE organizationMemberId rejects with PROJECT.MEMBER_NOT_ORG_MEMBER
    - Test: createProject path mismatch rejects with PROJECT.ACCESS_DENIED
  </acceptance_criteria>
  <done>Both service specs pass; services implement all behaviors per D-06/D-08/D-09/D-10; $transaction used for atomic creator-add.</done>
</task>

<task type="auto">
  <name>Task 3: Project controller and module</name>
  <files>
    packages/backend/src/project/api/project.controller.ts,
    packages/backend/src/project/project.module.ts
  </files>
  <read_first>
    - packages/backend/src/organization/api/organization.controller.ts (controller shape: @Controller path, @RequirePermissions, @GetCurrentUser, @NoTenantScope usage — note: ProjectController has NO @NoTenantScope routes)
    - packages/backend/src/organization/organization.module.ts (module structure: imports TenancyModule, providers list)
    - packages/backend/src/project/application/project.service.ts (just created — know method signatures)
    - packages/backend/src/project/application/project-member.service.ts (just created — know method signatures)
    - packages/database/prisma/seed.ts (confirm 'project:read' and 'project:manage' are seeded permission codes)
  </read_first>
  <action>
    ProjectController: @Controller({ path: 'organizations/:orgId/projects', version: '1' }). No @NoTenantScope on any route — all project routes require active tenant context.

    Routes (all accept @Param('orgId') orgId: string):
      POST /       — @RequirePermissions('project:manage'); @HttpCode(201); body: CreateProjectDto; returns ProjectResponseDto.from(result)
      GET /        — @RequirePermissions('project:read'); @Query() query: CursorPaginationDto; returns PaginatedResult<ProjectResponseDto> as { data: items.map(ProjectResponseDto.from), meta }
      GET /:projectId — @RequirePermissions('project:read'); returns ProjectResponseDto.from(result)
      PATCH /:projectId — @RequirePermissions('project:manage'); body: UpdateProjectDto; returns ProjectResponseDto.from(result)
      DELETE /:projectId — @RequirePermissions('project:manage'); @HttpCode(204); returns void

    Member sub-routes (nested under /:projectId/members):
      POST /:projectId/members — @RequirePermissions('project:manage'); @HttpCode(201); body: AddProjectMemberDto; returns ProjectMemberResponseDto.from(result)
      GET /:projectId/members  — @RequirePermissions('project:read'); returns ProjectMemberResponseDto[] mapped from listMembers result
      DELETE /:projectId/members/:memberId — @RequirePermissions('project:manage'); @HttpCode(204); returns void

    Constructor: (private readonly projectService: ProjectService, private readonly projectMemberService: ProjectMemberService).

    ProjectModule: @Module({ imports: [TenancyModule], controllers: [ProjectController], providers: [ProjectService, ProjectMemberService, ProjectRepository, ProjectMemberRepository] }). No exports (domain-internal).
  </action>
  <verify>
    <automated>npm run build --workspace=packages/backend -- --noEmit 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ProjectController @Controller path is 'organizations/:orgId/projects' with version: '1'
    - All GET routes use @RequirePermissions('project:read'); all mutating routes use @RequirePermissions('project:manage')
    - No @NoTenantScope decorator appears anywhere in project.controller.ts
    - ProjectController has 8 route handlers (POST /, GET /, GET /:projectId, PATCH /:projectId, DELETE /:projectId, POST /:projectId/members, GET /:projectId/members, DELETE /:projectId/members/:memberId)
    - project.module.ts imports TenancyModule and lists all 4 providers: ProjectService, ProjectMemberService, ProjectRepository, ProjectMemberRepository
    - npm run build --workspace=packages/backend -- --noEmit exits 0
  </acceptance_criteria>
  <done>ProjectController and ProjectModule compile cleanly; all 8 routes wired with correct permission guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → ProjectController | Untrusted orgId path param + projectId path param + request body cross here |
| ProjectService → TenantContext CLS | CLS reads must match the URL path params; mismatch = IDOR attempt |
| ProjectMemberService → ProjectMemberRepository upsert | organizationMemberId from request body must be validated ACTIVE before write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | ProjectService / ProjectMemberService | mitigate | assertPathMatchesContext(orgId) called at entry of every service method; path ≠ CLS org → 403 PROJECT.ACCESS_DENIED |
| T-07-02 | Information Disclosure | ProjectRepository.findById | mitigate | Uses findFirst (not findUnique); TenantedPrismaService auto-injects organizationId + deletedAt:null; cross-tenant project read is structurally impossible |
| T-07-03 | Elevation of Privilege | ProjectMemberService.addMember | mitigate | scopedPrisma.client.organizationMember.findFirst({ where: { id, status: 'ACTIVE' } }) before upsert; non-ACTIVE or cross-org orgMemberId → 404 MEMBER_NOT_ORG_MEMBER; scopedPrisma auto-scopes organizationMember to current org |
| T-07-04 | Information Disclosure | ProjectResponseDto / ProjectMemberResponseDto | mitigate | Explicit static from() mapping; no spread; deletedAt/deletedBy/updatedBy/updatedAt excluded from all response shapes |
| T-07-05 | Tampering | ProjectController request bodies | accept | GlobalValidationPipe (whitelist: true, forbidNonWhitelisted: true) already wired in AppModule; strips unknown fields automatically |
| T-07-06 | Tampering | CreateProjectDto.key | mitigate | @Matches(/^[A-Z][A-Z0-9]{1,9}$/) rejects invalid format at 400; P2002 → 409 via PrismaExceptionFilter for duplicate key within org |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase; all dependencies from prior phases |
</threat_model>

<verification>
Full phase gate (run after all three plans complete):
  npm run test --workspace=packages/backend -- --run
  npm run build --workspace=packages/backend -- --noEmit

Per-plan gate for this plan:
  npm run test --workspace=packages/backend -- --run src/project/
  npm run build --workspace=packages/backend -- --noEmit 2>&1 | grep "error TS"
</verification>

<success_criteria>
- 14 files exist under packages/backend/src/project/
- TypeScript compilation is clean
- project.service.spec.ts and project-member.service.spec.ts both pass
- POST /v1/organizations/:orgId/projects creates project + ACTIVE ProjectMember (OWNER) atomically
- List returns ALL org projects regardless of ProjectMember (D-06)
- Add-member validates ACTIVE org membership before upsert
- Re-add reactivates soft-removed members via upsert
- All response DTOs exclude soft-delete and audit columns
- PROJ-01, PROJ-02, PROJ-03 (ProjectMember portion) addressed
</success_criteria>

<output>
Create .planning/phases/07-project-foundation/07-01-SUMMARY.md when done.
</output>
