---
phase: 01-monorepo-tooling-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - packages/database/package.json
  - packages/database/.env.example
autonomous: false
requirements:
  - TOOL-06
user_setup:
  - service: supabase
    why: "Live Supabase Postgres credential in packages/database/.env must be rotated — it is real and currently valid (db.begukmntfkygqjpndbxt.supabase.co)"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Project Settings → Database → Reset database password → copy new connection string → update packages/database/.env"

must_haves:
  truths:
    - "git check-ignore packages/database/.env exits 0 (blocked by **/.env pattern)"
    - "packages/database/package.json has name '@repo/database', version '0.0.1', private true"
    - "packages/database/.env.example exists with only a placeholder DATABASE_URL — no real secrets"
    - "packages/database/package-lock.json no longer exists"
    - "Live Supabase credential rotated; old password invalidated"
    - "Honors CONTEXT.md decisions D-03 (no git-history purge — credential never committed), D-04 (rotate live Supabase credential), D-05 (add **/.env to .gitignore + commit .env.example placeholders), D-06 (additive .gitignore fix, no bare packages/ line per RESEARCH.md)"
  artifacts:
    - path: ".gitignore"
      provides: "Blocks all .env files from git tracking across the monorepo"
      contains: "**/.env"
    - path: "packages/database/package.json"
      provides: "npm workspace package identity; enables @repo/database symlink creation"
      contains: '"name": "@repo/database"'
    - path: "packages/database/.env.example"
      provides: "Documents DATABASE_URL key with a safe placeholder; committed to git"
      contains: "DATABASE_URL"
  key_links:
    - from: ".gitignore"
      to: "packages/database/.env"
      via: "**/.env glob pattern"
      pattern: '\*\*/.env'
---

<objective>
Establish the two hard prerequisites that must exist before workspace setup begins, and surface the live credential for mandatory rotation.

1. Fix .gitignore — the current 5-line file has a bare `.env` entry and no coverage for dist, turbo cache, or `.DS_Store`. Rewrite to a standard Node monorepo ignore set so packages (now untracked, not ignored) stay commit-able and secrets stay out.
2. Establish @repo/database identity — `packages/database/package.json` has no `name` field. npm workspaces silently skips members without a name; the `@repo/database` workspace symlink will never be created without this field.
3. Remove stale nested lockfile — `packages/database/package-lock.json` was created pre-workspace; once the root workspace takes over it causes `npm ci` failures.
4. Gate on credential rotation — `packages/database/.env` holds a live, valid Supabase connection string. Even though it was never committed (D-03 confirmed via git history), it must be rotated before CI can run safely.

Purpose: These are ordered prerequisites. Without them, npm install silently breaks, build artifacts pollute git, and an active database credential remains exposed.
Output: Fixed .gitignore, @repo/database identity, packages/database/.env.example, credential rotation confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-tooling-foundation/01-RESEARCH.md
@.planning/phases/01-monorepo-tooling-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix .gitignore + establish @repo/database identity + remove stale lockfile + create .env.example</name>
  <files>.gitignore, packages/database/package.json, packages/database/.env.example</files>
  <read_first>
    - .gitignore (current 5 lines: .agent/, /generated/prisma, node_modules, .env, .claude/)
    - packages/database/package.json (current content — no name field; has dependencies and prisma config)
    - packages/database/.env (read the KEY names only — never log values; need to produce .env.example keys)
    - 01-RESEARCH.md § "Code Examples" → ".gitignore (standard Node monorepo)" (the exact target shape)
    - 01-RESEARCH.md § "Critical Landmines" → Landmine 1 (name field blocking), Landmine 2 (lockfile), Landmine 3 (no packages/ line to remove — correction to D-06)
    - 01-PATTERNS.md § "packages/database/package.json (modified)" (target with name/version/private)
    - 01-PATTERNS.md § "packages/database/.env.example (new)" (key list + placeholder format)
  </read_first>
  <action>
    Step 1 — Rewrite .gitignore to the standard Node monorepo shape from 01-RESEARCH.md § Code Examples → ".gitignore (standard Node monorepo)". The current committed .gitignore has only ".agent/" (HEAD); the working-tree has 5 lines. The target shape is ADDITIVE: keep .agent/ and .claude/, replace bare `.env` with the **/.env glob pattern + **/.env.* negated by !**/.env.example, add **/dist/, .turbo/, **/generated/client/, .DS_Store and *.DS_Store, normalize node_modules to node_modules/. Do NOT add a bare `packages/` line — per RESEARCH.md Landmine 3, packages/ is untracked (not ignored), and no such line exists in the current file. The exact target content is in 01-RESEARCH.md § Code Examples.

    Step 2 — Add exactly three fields to the top of packages/database/package.json (before the "dependencies" key): "name": "@repo/database", "version": "0.0.1", "private": true. These are the ONLY changes to this file in this task — do not modify dependencies or the prisma config block. This is the blocking fix for npm workspace symlink creation (RESEARCH.md Landmine 1). The target shape is shown in 01-PATTERNS.md § "packages/database/package.json (modified)".

    Step 3 — Delete the file packages/database/package-lock.json. This nested lockfile predates workspace setup; once root npm workspaces takes over, it causes "multiple lockfiles found" warnings and `npm ci` failures. Run: rm packages/database/package-lock.json. Verify the file is gone.

    Step 4 — Create packages/database/.env.example with content from 01-PATTERNS.md § "packages/database/.env.example (new)": a single DATABASE_URL key with a postgresql://USER:PASSWORD@HOST:5432/postgres placeholder and a comment directing to the Supabase dashboard for rotation. Never write any real credential value into this file. The file MUST be committed to git (it is explicitly not-ignored via the !**/.env.example negation rule being added to .gitignore).
  </action>
  <verify>
    <automated>git check-ignore packages/database/.env && grep -q '"name": "@repo/database"' packages/database/package.json && test ! -f packages/database/package-lock.json && test -f packages/database/.env.example && grep -q 'DATABASE_URL' packages/database/.env.example && echo "ALL TASK-1 GATES PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains **/.env and **/.env.* lines with !**/.env.example negation
    - git check-ignore packages/database/.env exits 0
    - packages/database/package.json has "name": "@repo/database", "version": "0.0.1", "private": true as the first three keys
    - packages/database/package-lock.json does not exist (rm confirmed)
    - packages/database/.env.example exists, contains DATABASE_URL with a postgresql://USER:PASSWORD placeholder, contains no real credential values
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Rotate live Supabase database credential</name>
  <what-built>
    Task 1 has fixed .gitignore so packages/database/.env is now gitignored, and created packages/database/.env.example with a safe placeholder. The packages/database/.env file in your working tree still contains the LIVE, VALID Supabase Postgres credential for db.begukmntfkygqjpndbxt.supabase.co. Although this credential was never committed to git history (confirmed: D-03), it is real and must be rotated — anyone with file-system access has seen it, and CI runs should not start until it is invalidated.
  </what-built>
  <how-to-verify>
    Rotate the credential now — these are human-only dashboard actions:

    1. Open https://supabase.com/dashboard
    2. Select the project (host: db.begukmntfkygqjpndbxt.supabase.co)
    3. Navigate to: Project Settings → Database → Database password section
    4. Click "Reset database password" — generate a new strong password
    5. Copy the full new DATABASE_URL connection string shown after the reset
    6. Open packages/database/.env in your editor and replace the DATABASE_URL value with the new connection string
    7. Confirm connectivity: from packages/database/, run `node --experimental-strip-types prisma/seed.ts --dry-run` or `npx prisma db pull` to verify the new credential works
    8. Verify the OLD credential no longer authenticates (Supabase's reset invalidates it immediately)
  </how-to-verify>
  <resume-signal>Type "rotated" once the Supabase password has been reset in the dashboard and packages/database/.env has been updated with the new connection string.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer filesystem → git history | Secrets must never cross from .env files into tracked files or git commits |
| developer filesystem → Supabase cloud | DATABASE_URL grants write access to the production Postgres instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | packages/database/.env (live credential in working tree) | mitigate | **/.env + **/.env.* added to .gitignore in Task 1; git check-ignore gate in automated verify confirms exclusion; no real value ever written to tracked files |
| T-01-02 | Information Disclosure | Live Supabase credential — real and valid | mitigate | Blocking human checkpoint (Task 2) requires user to rotate password in Supabase dashboard; old credential invalidated before any CI run |
| T-01-03 | Information Disclosure | packages/database/.env.example committed with real values | mitigate | Task 1 creates .env.example with postgresql://USER:PASSWORD@HOST:5432/postgres placeholder only; executor must never substitute real secret values; reviewed in Task 2 before resuming |
| T-01-04 | Tampering | packages/database/package-lock.json stale nested lockfile | mitigate | Deleted in Task 1 Step 3; root lockfile becomes authoritative after Plan 02 npm install |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No npm installs in this plan; supply-chain checkpoint is in Plan 02 before first install |
</threat_model>

<verification>
After both tasks complete, confirm:

1. git check-ignore packages/database/.env exits 0
2. grep -q '"name": "@repo/database"' packages/database/package.json exits 0
3. test ! -f packages/database/package-lock.json exits 0
4. test -f packages/database/.env.example && grep -q "DATABASE_URL" packages/database/.env.example exits 0
5. git status shows packages/database/.env as untracked (never staged)
6. The Supabase credential in packages/database/.env is the newly-rotated one (user confirmed)
</verification>

<success_criteria>
1. packages/database/.env is gitignored — git check-ignore exits 0
2. packages/database/package.json has "name": "@repo/database" (blocking npm workspace fix)
3. packages/database/package-lock.json deleted (root lockfile becomes authoritative)
4. packages/database/.env.example committed with placeholder values only
5. Live Supabase credential rotated — old password invalidated
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-tooling-foundation/01-01-SUMMARY.md` when done
</output>
