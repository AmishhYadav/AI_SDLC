---
phase: 02-platform-kernel-bootstrap-config-error-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/package.json
  - packages/backend/nest-cli.json
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-14

must_haves:
  truths:
    - "packages/backend/package.json dependencies includes @nestjs/platform-express, @nestjs/config, zod"
    - "packages/backend/package.json devDependencies includes supertest, @types/supertest, @nestjs/testing"
    - "packages/backend/nest-cli.json entryFile is 'main' (not 'index')"
    - "npm ls @nestjs/platform-express -w @repo/backend resolves without error"
    - "npm run build --workspace packages/backend succeeds after entryFile change"
  artifacts:
    - path: "packages/backend/package.json"
      contains: "@nestjs/platform-express"
    - path: "packages/backend/nest-cli.json"
      contains: "\"entryFile\": \"main\""
  key_links:
    - from: "packages/backend/nest-cli.json"
      to: "packages/backend/src/main.ts"
      via: "entryFile: main → nest build compiles src/main.ts as the entry point"
      pattern: "\"entryFile\":\\s*\"main\""
---

<objective>
Gate the installation of 6 packages (3 runtime + 3 dev) behind a human legitimacy checkpoint, then install them and update nest-cli.json to point at main.ts. Without this plan nothing in Phase 2 compiles.

Purpose: All Phase 2 production code requires @nestjs/platform-express (HTTP adapter), @nestjs/config (config module), and zod (Zod schema validation). The integration test requires @nestjs/testing and supertest. The SWC nest build currently targets entryFile: "index"; main.ts will not be the compiled entry until that value changes to "main".

Output: Updated package.json with runtime + dev deps installed, nest-cli.json with entryFile "main", nest build succeeds.
</objective>

<execution_context>
@/Users/amish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-platform-kernel-bootstrap-config-error-contract/02-CONTEXT.md
@.planning/phases/02-platform-kernel-bootstrap-config-error-contract/02-RESEARCH.md
@.planning/phases/02-platform-kernel-bootstrap-config-error-contract/02-PATTERNS.md

<interfaces>
<!-- Existing package.json — add to, do not replace existing deps -->
<!-- From packages/backend/package.json (current state): -->
<!--   dependencies: @repo/database, @nestjs/common, @nestjs/core, reflect-metadata, rxjs -->
<!--   devDependencies: {} (empty) -->
<!--   scripts: build: nest build, typecheck: tsc --noEmit, lint: eslint 'src/**/*.ts', test: vitest run -->

<!-- Existing nest-cli.json — change entryFile only, preserve all other keys -->
<!-- From packages/backend/nest-cli.json (current state): -->
<!--   entryFile: "index"  ← CHANGE THIS TO "main" -->
<!--   compilerOptions.builder.type: "swc" ← KEEP -->
<!--   deleteOutDir: true ← KEEP -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before install</name>
  <read_first>
    .planning/phases/02-platform-kernel-bootstrap-config-error-contract/02-RESEARCH.md (## Package Legitimacy Audit section — lists the 3 runtime packages as [ASSUMED])
  </read_first>
  <what-built>
    Six packages are queued for installation. Three are runtime deps (required for the server to start):
      @nestjs/platform-express — HTTP adapter for NestJS
      @nestjs/config — configuration module with registerAs and ConfigService
      zod — schema validation library for fail-fast env validation

    Three are devDeps (required only for tests):
      @nestjs/testing — NestJS TestingModule for integration tests
      supertest — HTTP assertion library for integration tests
      @types/supertest — TypeScript types for supertest

    The RESEARCH.md Package Legitimacy Audit tagged the runtime packages [ASSUMED] because slopcheck was unavailable at research time. The dev packages were not in the audit. All six must be verified before install.
  </what-built>
  <how-to-verify>
    For each of the following packages, open the npm registry URL and confirm: (a) the package exists on the official npm registry, (b) the publisher is the expected org or author (NestJS org for @nestjs/*, colinhacks for zod, visionmedia for supertest, DefinitelyTyped for @types/*), (c) the package has significant download history (not a new or suspicious package), (d) no obvious supply-chain warnings on the page.

    URLs to visit:
      https://www.npmjs.com/package/@nestjs/platform-express
      https://www.npmjs.com/package/@nestjs/config
      https://www.npmjs.com/package/zod
      https://www.npmjs.com/package/@nestjs/testing
      https://www.npmjs.com/package/supertest
      https://www.npmjs.com/package/@types/supertest

    Expected: all six are legitimate, high-download, long-standing packages from expected publishers.
  </how-to-verify>
  <acceptance_criteria>
    - All six npm registry pages load and show the expected publisher
    - No package was created in the last 30 days
    - No package has fewer than 100k weekly downloads (all six are high-traffic packages)
    - No postinstall scripts are listed on any package page
  </acceptance_criteria>
  <resume-signal>Type "verified" to confirm all six packages are legitimate, or describe any issue found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install packages + update nest-cli.json entryFile</name>
  <read_first>
    packages/backend/package.json (current deps — do not overwrite existing @nestjs/common, @nestjs/core, reflect-metadata, rxjs, @repo/database)
    packages/backend/nest-cli.json (current entryFile: "index" — change to "main", preserve all other keys)
    .planning/phases/02-platform-kernel-bootstrap-config-error-contract/02-RESEARCH.md (## Standard Stack section for exact package names and ## Environment Availability for node/npm versions)
  </read_first>
  <action>
    Step 1 — Install runtime dependencies into packages/backend (not the workspace root):
      npm install --save @nestjs/platform-express @nestjs/config zod --workspace packages/backend

    Step 2 — Install dev dependencies into packages/backend:
      npm install --save-dev supertest @types/supertest @nestjs/testing --workspace packages/backend

    Step 3 — Update packages/backend/nest-cli.json: change the "entryFile" value from "index" to "main". Keep "compilerOptions.builder.type": "swc" and "deleteOutDir": true unchanged. The resulting entryFile must be the string "main" (no path, no extension).

    Landmine: do NOT remove the SWC builder config from nest-cli.json. Only the "entryFile" key changes.

    Step 4 — Verify the build still compiles (no main.ts yet, so build will succeed trivially but must not error on the entryFile change):
      npm run build --workspace packages/backend

    Note: The build will succeed even without main.ts in this step because nest build with SWC compiles whatever TypeScript files exist in src/; the entryFile change affects which compiled file NestJS uses as the entry at runtime, not what SWC compiles.
  </action>
  <verify>
    <automated>
      npm ls @nestjs/platform-express --workspace @repo/backend --depth=0 2>&amp;1 | grep "@nestjs/platform-express"
      npm ls @nestjs/config --workspace @repo/backend --depth=0 2>&amp;1 | grep "@nestjs/config"
      npm ls zod --workspace @repo/backend --depth=0 2>&amp;1 | grep "zod"
      npm ls supertest --workspace @repo/backend --depth=0 2>&amp;1 | grep "supertest"
      grep '"entryFile": "main"' packages/backend/nest-cli.json
      npm run build --workspace packages/backend
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/backend/package.json dependencies includes @nestjs/platform-express, @nestjs/config, zod
    - packages/backend/package.json devDependencies includes supertest, @types/supertest, @nestjs/testing
    - packages/backend/nest-cli.json shows "entryFile": "main" (not "index")
    - packages/backend/nest-cli.json still shows "builder": { "type": "swc" }
    - npm run build --workspace packages/backend exits 0 (SWC compiler finds no errors)
    - No changes to packages/database/package.json or root package.json
    - packages/database/prisma.zip is NOT committed (untracked file, must remain untracked)
  </acceptance_criteria>
  <done>All six packages are installed to @repo/backend, nest-cli.json points at "main", and the SWC build still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Untrusted external packages installed into the build tree |
| nest-cli.json → nest build | Build configuration change affects which file becomes the runtime entry point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm install (@nestjs/platform-express, @nestjs/config, zod, @nestjs/testing, supertest, @types/supertest) | mitigate | Blocking human checkpoint (Task 1) requires verifying each package on npmjs.com before any install runs; `gate="blocking-human"` cannot be auto-approved |
| T-02-01 | Information Disclosure | nest-cli.json entryFile | accept | Config-only change; no data flows through this file at runtime; only affects which compiled file NestJS loads as the entry point — low risk |
</threat_model>

<verification>
After both tasks complete:
- npm ls @nestjs/platform-express @nestjs/config zod --workspace @repo/backend shows all three at expected versions
- cat packages/backend/nest-cli.json shows entryFile "main" and builder type "swc"
- npm run build --workspace packages/backend exits 0
- git status shows packages/backend/package.json and packages/backend/nest-cli.json as modified; package-lock.json as modified; packages/database/prisma.zip remains untracked (not staged)
</verification>

<success_criteria>
- @nestjs/platform-express, @nestjs/config, zod are in packages/backend/package.json dependencies
- supertest, @types/supertest, @nestjs/testing are in packages/backend/package.json devDependencies
- packages/backend/nest-cli.json entryFile is "main"
- npm run build --workspace packages/backend succeeds
- All six packages were verified by the human before install
</success_criteria>

<output>
Create .planning/phases/02-platform-kernel-bootstrap-config-error-contract/02-01-SUMMARY.md when done
</output>
