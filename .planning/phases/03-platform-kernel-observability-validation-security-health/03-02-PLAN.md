---
phase: 03
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/backend/src/config/env.schema.ts
  - packages/backend/src/config/env.schema.spec.ts
  - packages/backend/src/common/middleware/extract-correlation-id.ts
  - packages/backend/src/common/middleware/correlation-id.middleware.ts
  - packages/backend/src/app.module.ts
  - packages/backend/src/common/exceptions/global-exception.filter.ts
  - packages/backend/src/common/exceptions/global-exception.filter.spec.ts
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "Every pino request log carries a reqId field whose value matches the x-request-id header (or a generated UUID), sourced from nestjs-cls ALS — not from req.traceId (D-11, D-12)"
    - "GlobalExceptionFilter reads traceId via cls.getId(), not request.traceId"
    - "auth headers (authorization, cookie), secret-like body fields (password, token, apiKey, secret) are redacted to [REDACTED] in pino output (D-13)"
    - "Env schema fails fast on startup when CORS_ORIGINS, LOG_LEVEL, or THROTTLER_* values are missing or invalid (D-16)"
  artifacts:
    - path: "packages/backend/src/common/middleware/extract-correlation-id.ts"
      provides: "extractCorrelationId(req) utility function"
      exports: ["extractCorrelationId"]
    - path: "packages/backend/src/config/env.schema.ts"
      provides: "Extended Zod env schema with CORS_ORIGINS, LOG_LEVEL, THROTTLER_TTL_SECONDS, THROTTLER_LIMIT"
      contains: "CORS_ORIGINS"
    - path: "packages/backend/src/app.module.ts"
      provides: "ClsModule before LoggerModule in imports[]"
      contains: "ClsModule"
  key_links:
    - from: "packages/backend/src/app.module.ts"
      to: "packages/backend/src/common/middleware/extract-correlation-id.ts"
      via: "ClsModule.forRoot idGenerator callback"
      pattern: "extractCorrelationId"
    - from: "packages/backend/src/common/exceptions/global-exception.filter.ts"
      to: "ClsService"
      via: "constructor injection + cls.getId()"
      pattern: "cls\\.getId\\(\\)"
---

<objective>
Migrate the per-request correlation ID from req.traceId (Phase 2) into nestjs-cls AsyncLocalStorage (ALS), add nestjs-pino structured JSON logging with automatic traceId propagation and a redaction deny-list, and extend the Zod env schema with the four new vars this phase's features require.

Purpose: Fulfils INFRA-04 (D-11, D-12, D-13, D-16). Provides ClsService as the canonical traceId source that all downstream plans (interceptors, filters, future tenant context) consume. ClsModule must be in AppModule before any other ALS-consuming module.
Output: Extended env schema, new extract-correlation-id.ts utility, simplified CorrelationIdMiddleware, AppModule with ClsModule before LoggerModule, updated GlobalExceptionFilter using cls.getId().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-platform-kernel-observability-validation-security-health/03-CONTEXT.md
@.planning/phases/03-platform-kernel-observability-validation-security-health/03-RESEARCH.md
@.planning/phases/03-platform-kernel-observability-validation-security-health/03-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/backend/src/config/env.schema.ts (current — lines 1-9):
  envSchema is z.object with keys: NODE_ENV, PORT, DATABASE_URL.
  Pattern for new fields:
    - z.coerce.number().int().positive().default(N) — copies PORT shape
    - z.enum([...]).default('x') — copies NODE_ENV shape
    - z.string().min(1, 'message') — copies DATABASE_URL shape but with min constraint

From packages/backend/src/common/middleware/correlation-id.middleware.ts (current — lines 5-35):
  extractSafeTraceId(raw): validates UUID format (UUID_RE regex), max 128 chars
  extractTraceparentId(header): parses W3C traceparent, converts 32-hex trace-id to UUID
  CorrelationIdMiddleware.use(): picks x-request-id → traceparent → randomUUID, stores on req.traceId

From packages/backend/src/common/exceptions/global-exception.filter.ts (current — lines 23-24, 56):
  constructor(private readonly config: AppConfigService) {}
  traceId: request.traceId ?? crypto.randomUUID()   ← line 56 to change to cls.getId()

From packages/backend/src/common/exceptions/global-exception.filter.spec.ts (current — lines 7-13):
  function makeHost(traceId?: string): any — passes traceId via getRequest().traceId
  function makeConfig(isProduction = false): any — { isProduction }
  new GlobalExceptionFilter(makeConfig()) — single constructor arg
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend env schema + extract-correlation-id.ts utility + simplify middleware</name>
  <files>
    packages/backend/src/config/env.schema.ts,
    packages/backend/src/config/env.schema.spec.ts,
    packages/backend/src/common/middleware/extract-correlation-id.ts,
    packages/backend/src/common/middleware/correlation-id.middleware.ts
  </files>
  <behavior>
    - env.schema.spec.ts: envSchema.parse({ DATABASE_URL: 'postgresql://a:b@localhost/db', CORS_ORIGINS: 'http://localhost:3001' }) succeeds with LOG_LEVEL defaulting to 'info', THROTTLER_TTL_SECONDS to 60, THROTTLER_LIMIT to 100.
    - env.schema.spec.ts: envSchema.parse({ DATABASE_URL: '...' }) throws (CORS_ORIGINS missing, no default).
    - env.schema.spec.ts: envSchema.parse({ ..., LOG_LEVEL: 'invalid' }) throws (not in enum).
    - env.schema.spec.ts: envSchema.parse({ ..., THROTTLER_TTL_SECONDS: '30' }) coerces to number 30 (Zod z.coerce).
  </behavior>
  <read_first>
    packages/backend/src/config/env.schema.ts — read full file; add 4 fields inside the existing z.object({}) call (PATTERNS.md "env.schema.ts" section, lines 1-9).
    packages/backend/src/config/env.schema.spec.ts — read full file; understand existing test structure before adding cases.
    packages/backend/src/common/middleware/correlation-id.middleware.ts — read full file (lines 1-35); functions to extract: extractSafeTraceId (lines 7-11), extractTraceparentId (lines 14-23), UUID_RE constant (line 5).
    .planning/phases/03-platform-kernel-observability-validation-security-health/03-PATTERNS.md — read "env.schema.ts" section and "extract-correlation-id.ts" section for exact field patterns.
    .planning/phases/03-platform-kernel-observability-validation-security-health/03-RESEARCH.md — read "Complete env.schema.ts extension" code example in Code Examples section.
  </read_first>
  <action>
    Step A — env.schema.ts extension (D-16): inside the existing z.object({}) call, after the DATABASE_URL line, add four new fields: CORS_ORIGINS as z.string().min(1, 'CORS_ORIGINS must be a non-empty comma-separated list'); LOG_LEVEL as z.enum(['fatal', 'error', 'warn', 'info', 'debug', 'trace']).default('info'); THROTTLER_TTL_SECONDS as z.coerce.number().int().positive().default(60); THROTTLER_LIMIT as z.coerce.number().int().positive().default(100). The existing NODE_ENV, PORT, DATABASE_URL lines are untouched. The type Env = z.infer<typeof envSchema> line is untouched.

    Step B — extract-correlation-id.ts (new file, D-12): create packages/backend/src/common/middleware/extract-correlation-id.ts. This file contains no class, no @Injectable(), no NestJS imports. It exports exactly one symbol: the function extractCorrelationId(req: { headers: Record<string, string | string[] | undefined> }): string. Move the UUID_RE constant, extractSafeTraceId(), and extractTraceparentId() functions from correlation-id.middleware.ts into this file as private (non-exported) functions. extractCorrelationId returns: extractSafeTraceId(req.headers['x-request-id'] as string | undefined) ?? extractTraceparentId(req.headers['traceparent'] as string | undefined) ?? crypto.randomUUID(). The UUID_RE regex (lines 5 of the middleware) goes here. No other exports from this file.

    Step C — correlation-id.middleware.ts simplification: remove UUID_RE, extractSafeTraceId, extractTraceparentId from this file (they moved to Step B). Import ClsService from 'nestjs-cls'. Add ClsService as constructor injection: constructor(private readonly cls: ClsService). The use() method body becomes: req.traceId = this.cls.getId(); next(). Remove the rawId extraction logic — ClsMiddleware (registered via ClsModule.forRoot in Task 2) runs BEFORE this middleware in the middleware stack and has already set the ALS id via the idGenerator. This middleware's sole remaining purpose is to sync req.traceId for backward compatibility.

    Step D — env.schema.spec.ts: add 4 tests covering CORS_ORIGINS required, LOG_LEVEL default, THROTTLER coercion, and LOG_LEVEL invalid value. Use the same import pattern as the existing tests in that file.
  </action>
  <verify>
    <automated>pnpm --filter @repo/backend test run</automated>
  </verify>
  <done>
    env.schema.ts z.object() contains exactly 7 keys (NODE_ENV, PORT, DATABASE_URL, CORS_ORIGINS, LOG_LEVEL, THROTTLER_TTL_SECONDS, THROTTLER_LIMIT). extract-correlation-id.ts exports only extractCorrelationId. correlation-id.middleware.ts no longer defines UUID_RE, extractSafeTraceId, or extractTraceparentId. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ClsModule + LoggerModule in AppModule; update GlobalExceptionFilter to read traceId from ALS</name>
  <files>
    packages/backend/src/app.module.ts,
    packages/backend/src/common/exceptions/global-exception.filter.ts,
    packages/backend/src/common/exceptions/global-exception.filter.spec.ts
  </files>
  <read_first>
    packages/backend/src/app.module.ts — read full file; current imports[]: [AppConfigModule, PrismaModule]; current providers: APP_FILTER x2. The imports[] order is the critical constraint for this task.
    packages/backend/src/common/exceptions/global-exception.filter.ts — read full file (lines 1-65); specifically the constructor (line 23-24) and the traceId line (line 56).
    packages/backend/src/common/exceptions/global-exception.filter.spec.ts — read full file (lines 1-122); specifically the makeHost factory (lines 7-13) and the 'reads traceId from request' test (lines 98-105) — both must be updated.
    .planning/phases/03-platform-kernel-observability-validation-security-health/03-PATTERNS.md — read "app.module.ts" section (new imports with ordering comment), "global-exception.filter.ts" section (lines for surgical change), "global-exception.filter.spec.ts" section (updated mock setup).
    .planning/phases/03-platform-kernel-observability-validation-security-health/03-RESEARCH.md — read Pattern 1 (nestjs-cls + nestjs-pino ALS Integration) for the exact ClsModule.forRoot config and LoggerModule.forRootAsync factory signature including the inject: [AppConfigService, ClsService] detail.
  </read_first>
  <action>
    Step A — AppModule imports[] (CRITICAL ordering): update app.module.ts imports array to: [AppConfigModule, ClsModule.forRoot({...}), LoggerModule.forRootAsync({...}), PrismaModule]. The exact order is non-negotiable — ClsModule.forRoot must appear BEFORE LoggerModule.forRootAsync. NestJS applies module middleware in import order; if LoggerModule runs before ClsModule, pino-http's genReqId callback calls cls.getId() before the ALS store is initialised, returning undefined. Violation symptom: reqId: null in all log lines.

    ClsModule.forRoot configuration: global: true (so ClsService is injectable everywhere without re-importing ClsModule), middleware.mount: true (registers ClsMiddleware globally), middleware.generateId: true (runs idGenerator per request), middleware.idGenerator: (req) => extractCorrelationId(req) — import extractCorrelationId from './common/middleware/extract-correlation-id'.

    LoggerModule.forRootAsync configuration: inject: [AppConfigService, ClsService], imports: [AppConfigModule], useFactory: (config: AppConfigService, cls: ClsService) => pinoHttp object. The pinoHttp object must have: level: config.get('LOG_LEVEL'); genReqId: () => cls.getId(); redact.paths covering 'req.headers.authorization', 'req.headers.cookie', 'req.headers["set-cookie"]', 'req.body.password', 'req.body.token', 'req.body.apiKey', 'req.body.secret' with censor: '[REDACTED]'; autoLogging.ignore: (req) => Boolean(req.url?.includes('/health')) so health probe logs are suppressed; serializers.req: (req) => ({ method: req.method, url: req.url, id: req.id }) to log method/path/traceId only, never the request body (D-13). Import ClsModule from 'nestjs-cls', LoggerModule from 'nestjs-pino'.

    Step B — GlobalExceptionFilter (surgical change to 2 lines only, per D-12): add import { ClsService } from 'nestjs-cls' at the top of global-exception.filter.ts. Change the constructor from constructor(private readonly config: AppConfigService) to constructor(private readonly config: AppConfigService, private readonly cls: ClsService). Change line 56 from `traceId: request.traceId ?? crypto.randomUUID()` to `traceId: this.cls.getId() ?? crypto.randomUUID()`. All other lines in global-exception.filter.ts are untouched. Do not remove the `request` variable — it is still used for `req.ip` or other request properties if present.

    Step C — global-exception.filter.spec.ts update: add a makeCls() factory function: function makeCls(id = 'test-trace-id'): any { return { getId: vi.fn().mockReturnValue(id) }; }. Change makeHost() to no longer pass traceId via the request: function makeHost(): any { return { switchToHttp: () => ({ getResponse: vi.fn().mockReturnValue({ status: vi.fn().mockReturnValue({ json: vi.fn() }) }), getRequest: vi.fn().mockReturnValue({}) }) }; }. Update ALL occurrences of new GlobalExceptionFilter(makeConfig()) to new GlobalExceptionFilter(makeConfig(), makeCls()) — there are multiple test cases. Update the 'reads traceId from request' test to: create filter with makeCls('expected-id'), assert body.traceId === 'expected-id'. This proves the filter reads from CLS, not from the request object.
  </action>
  <verify>
    <automated>pnpm --filter @repo/backend test run</automated>
  </verify>
  <done>
    pnpm --filter @repo/backend test run exits 0 — all Phase 2 tests pass including the updated GlobalExceptionFilter spec (9+ tests). app.module.ts imports[] order: AppConfigModule at index 0, ClsModule at index 1, LoggerModule at index 2, PrismaModule at index 3. global-exception.filter.ts constructor has 2 parameters: AppConfigService and ClsService. The line `request.traceId` no longer appears in global-exception.filter.ts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request headers → ALS store | x-request-id / traceparent headers are untrusted; extractCorrelationId() sanitises them |
| ALS store → pino log output → monitoring | Log line content must never include credentials or PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | extractCorrelationId(req): x-request-id / traceparent header injection | mitigate | UUID_RE regex validates x-request-id to UUID format only; max 128-char slice; traceparent validated to 32-hex format — prevents log injection via crafted headers |
| T-03-01 | Information Disclosure | pino-http log output: auth headers and body fields | mitigate | redact.paths deny-list covers authorization, cookie, set-cookie, password, token, apiKey, secret; autoLogging suppresses health probe logs; serializers.req never logs body |
| T-03-SC | Tampering | npm packages (nestjs-pino, nestjs-cls) | mitigate | Human-verified in Plan 01 Task 1 before install |
</threat_model>

<verification>
pnpm --filter @repo/backend test run — all tests pass (Phase 2 suite + 4 new env schema tests).
pnpm --filter @repo/backend build — TypeScript compiles without error.
grep -n "ClsModule" packages/backend/src/app.module.ts — appears before LoggerModule (lower line number).
grep "cls\.getId()" packages/backend/src/common/exceptions/global-exception.filter.ts — returns exactly 1 match.
grep "request\.traceId" packages/backend/src/common/exceptions/global-exception.filter.ts — returns 0 matches.
</verification>

<success_criteria>
1. pnpm --filter @repo/backend test run exits 0.
2. app.module.ts imports[]: AppConfigModule → ClsModule.forRoot → LoggerModule.forRootAsync → PrismaModule (confirmed by grepping line positions).
3. global-exception.filter.ts uses cls.getId() not request.traceId for the traceId field.
4. extract-correlation-id.ts exists and exports only extractCorrelationId.
5. env.schema.ts contains CORS_ORIGINS, LOG_LEVEL, THROTTLER_TTL_SECONDS, THROTTLER_LIMIT.
</success_criteria>

<output>
Create .planning/phases/03-platform-kernel-observability-validation-security-health/03-02-SUMMARY.md when done.
</output>
