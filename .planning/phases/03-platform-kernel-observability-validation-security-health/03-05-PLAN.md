---
phase: 03
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-03
files_modified:
  - packages/backend/src/health/prisma-health.indicator.ts
  - packages/backend/src/health/health.controller.ts
  - packages/backend/src/health/health.module.ts
  - packages/backend/src/health/health.controller.spec.ts
autonomous: true
requirements:
  - INFRA-10

must_haves:
  truths:
    - "GET /api/v1/health/liveness returns 200 with body { status: 'ok' } always, with no database check"
    - "GET /api/v1/health/readiness returns 200 with Terminus health JSON when Prisma $queryRaw SELECT 1 succeeds"
    - "GET /api/v1/health/readiness returns 503 with Terminus health JSON when Prisma $queryRaw throws"
    - "HealthController is decorated with @RawResponse() at class level — Terminus JSON is never wrapped in the success envelope"
    - "PrismaHealthIndicator uses HealthIndicatorService.check(key).up()/.down() API — NOT the deprecated HealthIndicator class"
  artifacts:
    - path: "packages/backend/src/health/health.module.ts"
      provides: "HealthModule NestJS module importing TerminusModule"
      exports: ["HealthModule"]
    - path: "packages/backend/src/health/health.controller.ts"
      provides: "HealthController with liveness and readiness endpoints"
      exports: ["HealthController"]
    - path: "packages/backend/src/health/prisma-health.indicator.ts"
      provides: "PrismaHealthIndicator using HealthIndicatorService"
      exports: ["PrismaHealthIndicator"]
  key_links:
    - from: "packages/backend/src/health/health.controller.ts"
      to: "packages/backend/src/common/interceptors/raw-response.decorator.ts"
      via: "@RawResponse() class-level decorator"
      pattern: "RawResponse"
    - from: "packages/backend/src/health/prisma-health.indicator.ts"
      to: "PrismaService"
      via: "this.prisma.$queryRaw`SELECT 1`"
      pattern: "\\$queryRaw"
---

<objective>
Build the health check module: a liveness endpoint (always 200, no deps) and a readiness endpoint (Prisma DB ping via Terminus). This plan runs parallel with Plan 04 — it has no file overlap with Plan 04 and depends only on Plan 03 for the @RawResponse() decorator.

Purpose: Fulfils INFRA-10. Readiness fails with 503 when the DB is unreachable, which is the correct Kubernetes probe semantics. The @RawResponse() decorator ensures Terminus's native health JSON is never wrapped in the success envelope (Pitfall 4 prevention). HealthModule is imported into AppModule in Plan 06.
Output: 4 new source files; 0 AppModule changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-platform-kernel-observability-validation-security-health/03-CONTEXT.md
@.planning/phases/03-platform-kernel-observability-validation-security-health/03-RESEARCH.md
@.planning/phases/03-platform-kernel-observability-validation-security-health/03-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/backend/src/common/interceptors/raw-response.decorator.ts (Plan 03):
  export const RAW_RESPONSE_KEY = 'RAW_RESPONSE'
  export const RawResponse = (): MethodDecorator and ClassDecorator => SetMetadata(RAW_RESPONSE_KEY, true)
  Apply @RawResponse() at HealthController CLASS level (covers all methods).

From @nestjs/terminus v11 (terminus 11+ API — use this, not HealthIndicator class):
  HealthIndicatorService is injectable from TerminusModule.
  Usage: const indicator = this.health.check(key); try { ...; return indicator.up(); } catch { return indicator.down({ message }); }
  NEVER use: new HealthCheckError(...) or class extends HealthIndicator — both are deprecated in v11.

From @repo/database:
  PrismaService is globally exported; $queryRaw is a raw query method.
  ping pattern: await this.prisma.$queryRaw`SELECT 1` — returns [{ '?column?': 1 }] or throws.

From packages/backend/src/app.integration.spec.ts (analog for test module setup, lines 12-37):
  Test.createTestingModule({ imports: [...] }).overrideProvider(PrismaService).useValue({...}).compile()
  Set process.env vars before compile() to satisfy Zod schema.

Routes served:
  GET /api/v1/health/liveness — always 200
  GET /api/v1/health/readiness — 200 OK or 503 Service Unavailable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrismaHealthIndicator + HealthController + HealthModule</name>
  <files>
    packages/backend/src/health/prisma-health.indicator.ts,
    packages/backend/src/health/health.controller.ts,
    packages/backend/src/health/health.module.ts
  </files>
  <read_first>
    packages/backend/src/common/exceptions/prisma-exception.filter.ts — read full file; PrismaService constructor injection pattern (inject via constructor, use in method).
    packages/backend/src/config/config.module.ts — read full file (17 lines); @Module({ imports, providers, exports }) declaration pattern without @Global() to replicate.
    packages/backend/src/common/interceptors/raw-response.decorator.ts — read to confirm the exact export name RawResponse and its signature.
    .planning/phases/03-platform-kernel-observability-validation-security-health/03-PATTERNS.md — read "prisma-health.indicator.ts" section (HealthIndicatorService injection, SELECT 1 try/catch), "health.controller.ts" section (@Version('1'), @RawResponse() at class level, @HealthCheck() on readiness), "health.module.ts" section (@Module structure).
    .planning/phases/03-platform-kernel-observability-validation-security-health/03-RESEARCH.md — read Pattern 5 (Health Checks via Terminus 11) — critical for the HealthIndicatorService API vs deprecated HealthIndicator class distinction.
  </read_first>
  <action>
    File 1 — prisma-health.indicator.ts (src/health/): @Injectable() class PrismaHealthIndicator. Constructor injects PrismaService from '@repo/database' and HealthIndicatorService from '@nestjs/terminus'. Public method async isHealthy(key: string): returns the indicator result object. Body: const indicator = this.health.check(key); then try { await this.prisma.$queryRaw`SELECT 1`; return indicator.up(); } catch (error) { return indicator.down({ message: (error as Error).message }); }. Import Injectable from '@nestjs/common', HealthIndicatorService from '@nestjs/terminus', PrismaService from '@repo/database'. CRITICAL: do NOT import HealthIndicator or HealthCheckError — both are deprecated in @nestjs/terminus v11 and scheduled for v12 removal.

    File 2 — health.controller.ts (src/health/): import Controller, Get, Version from '@nestjs/common'; HealthCheck, HealthCheckService from '@nestjs/terminus'; RawResponse from '../common/interceptors/raw-response.decorator'; PrismaHealthIndicator from './prisma-health.indicator'. Apply three class-level decorators: @Controller('health'), @Version('1'), @RawResponse() — the @RawResponse() must be at class level so ALL methods (liveness and readiness) bypass the ResponseEnvelopeInterceptor. Constructor injects HealthCheckService and PrismaHealthIndicator.

    @Get('liveness') liveness(): { status: string } { return { status: 'ok' }; } — no @HealthCheck(), no DB call, always returns synchronously. This gives Kubernetes a fast liveness probe that never depends on downstream services.

    @Get('readiness') @HealthCheck() readiness() { return this.health.check([() => this.prismaIndicator.isHealthy('prisma')]); } — HealthCheckService handles the 503/200 HTTP status mapping automatically.

    File 3 — health.module.ts (src/health/): @Module({ imports: [TerminusModule], controllers: [HealthController], providers: [PrismaHealthIndicator] }) export class HealthModule. No @Global() — HealthModule is imported explicitly by AppModule in Plan 06. TerminusModule provides both HealthCheckService and HealthIndicatorService to this module's scope, so both are injectable by HealthController and PrismaHealthIndicator.
  </action>
  <verify>
    <automated>pnpm --filter @repo/backend typecheck</automated>
  </verify>
  <done>
    pnpm --filter @repo/backend typecheck exits 0. grep "HealthIndicatorService" packages/backend/src/health/prisma-health.indicator.ts returns 1 match. grep -v "HealthIndicatorService" packages/backend/src/health/prisma-health.indicator.ts | grep "HealthIndicator" returns 0 matches (the deprecated HealthIndicator class is not used). HealthController class has 3 decorators: @Controller, @Version, @RawResponse.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: health.controller.spec.ts integration test</name>
  <files>
    packages/backend/src/health/health.controller.spec.ts
  </files>
  <behavior>
    Test A (liveness 200): GET /api/v1/health/liveness → HTTP 200, response body { status: 'ok' }.
    Test B (readiness 200 with healthy DB): GET /api/v1/health/readiness with mock PrismaService.$queryRaw resolving → HTTP 200, response body contains status: 'ok' (Terminus shape).
    Test C (readiness 503 with broken DB): GET /api/v1/health/readiness with mock PrismaService.$queryRaw rejecting → HTTP 503, response body contains status: 'error'.
    Test D (liveness response has no 'success' key): GET /api/v1/health/liveness → response body does NOT have a 'success' property (proves @RawResponse() bypasses the envelope interceptor — envelope is not registered in this isolated test module, so absence proves the controller itself returns the right shape).
  </behavior>
  <read_first>
    packages/backend/src/app.integration.spec.ts — read full file (72 lines); Test.createTestingModule pattern with module override, process.env setup, beforeAll/afterAll, supertest usage.
    packages/backend/src/health/health.module.ts — read (just created in Task 1); this is the import for the test module.
    packages/backend/src/health/health.controller.ts — read (just created in Task 1); routes and controller shape.
    .planning/phases/03-platform-kernel-observability-validation-security-health/03-PATTERNS.md — read "health.controller.spec.ts" section for the mock PrismaService override pattern and env var setup.
  </read_first>
  <action>
    Create health.controller.spec.ts in src/health/. Setup section: before the describe block, set required env vars — process.env['DATABASE_URL'] must be set (existing Phase 2 requirement); process.env['NODE_ENV'] = 'test'; process.env['CORS_ORIGINS'] = 'http://localhost:3001' (required by the Phase 3 extended env schema even for isolated module tests that import HealthModule, since AppConfigModule may be transitively pulled).

    Note: HealthModule imports only TerminusModule — it does NOT import AppConfigModule or ClsModule. However, if PrismaModule is not overridden, it may try to connect. Override PrismaService.

    Test module setup: Test.createTestingModule({ imports: [HealthModule] }).overrideProvider(PrismaService).useValue({ $queryRaw: vi.fn().mockResolvedValue([{ '?column?': 1 }]) }).compile(). Create app = moduleRef.createNestApplication(), app.setGlobalPrefix('api'), app.enableVersioning({ type: VersioningType.URI }). Do NOT register ResponseEnvelopeInterceptor (not imported in this isolated module) — this is intentional for the @RawResponse isolation test.

    For Test C (readiness 503): use a second test module or override the mock within the test using a different approach. Simplest: create a separate moduleRef in a nested describe block where $queryRaw rejects with new Error('DB connection refused'). Alternatively, use module.get(PrismaService).$queryRaw = vi.fn().mockRejectedValue(new Error('DB down')) to override per-test. Either approach is acceptable.

    afterAll: await app.close().

    Tests use supertest imported as request from 'supertest'. All 4 tests assert the HTTP status code and a key body property.
  </action>
  <verify>
    <automated>pnpm --filter @repo/backend test run</automated>
  </verify>
  <done>
    pnpm --filter @repo/backend test run exits 0 with 4 passing tests in health.controller.spec.ts. Test "readiness 503 when DB down" asserts HTTP 503 status. Test "liveness response has no success key" asserts body.success is undefined. All Phase 2 tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kubernetes probe → /api/v1/health/readiness | Liveness probe must never fail for app-is-running state; readiness probe may fail when deps are down |
| HealthController → PrismaService | DB ping crosses trust boundary; failure must return 503, not 500 (Terminus handles this) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service | Health endpoints: could be hammered by probes generating log noise | mitigate | pino-http autoLogging.ignore suppresses /health probe logs (configured in Plan 02 LoggerModule); ThrottlerGuard applies after Plan 06 wiring |
| T-03-09 | Information Disclosure | readiness failure message exposes DB error details | mitigate | indicator.down({ message: error.message }) — message is generic "DB connection refused" style; Prisma connection errors do not include credentials; Terminus serialises this safely |
| T-03-SC | Tampering | @nestjs/terminus npm package | mitigate | Human-verified in Plan 01 Task 1 |
</threat_model>

<verification>
pnpm --filter @repo/backend test run — exits 0 (all tests, including 4 new health tests).
pnpm --filter @repo/backend typecheck — exits 0.
grep "HealthIndicatorService" packages/backend/src/health/prisma-health.indicator.ts — 1 match (correct new API).
grep "HealthIndicator[^S]" packages/backend/src/health/prisma-health.indicator.ts — 0 matches (deprecated class absent).
grep "@RawResponse" packages/backend/src/health/health.controller.ts — 1 match at class level (not just method level).
</verification>

<success_criteria>
1. 4 health tests pass: liveness 200, readiness 200 with healthy DB, readiness 503 with broken DB, liveness response has no success key.
2. PrismaHealthIndicator uses HealthIndicatorService.check(key).up()/.down() — deprecated HealthIndicator class absent.
3. HealthController applies @RawResponse() at class level.
4. TypeScript compile exits 0.
</success_criteria>

<output>
Create .planning/phases/03-platform-kernel-observability-validation-security-health/03-05-SUMMARY.md when done.
</output>
