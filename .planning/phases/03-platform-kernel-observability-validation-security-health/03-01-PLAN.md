---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/package.json
autonomous: false
requirements:
  - INFRA-04
  - INFRA-07
  - INFRA-08
  - INFRA-09
  - INFRA-10
  - INFRA-11
  - INFRA-12
  - INFRA-13
  - SEAM-06

must_haves:
  truths:
    - "All 10 new runtime packages are installable from the npm registry with no resolution errors"
    - "TypeScript recognises nestjs-pino, nestjs-cls, @nestjs/terminus, @nestjs/swagger, @nestjs/throttler, helmet, class-validator, class-transformer types after install"
    - "packages/backend/package.json lists all 10 new packages in dependencies"
  artifacts:
    - path: "packages/backend/package.json"
      provides: "10 new runtime dependencies recorded"
      contains: "nestjs-pino"
  key_links:
    - from: "packages/backend/src"
      to: "node_modules/nestjs-pino"
      via: "npm workspace resolution"
      pattern: "nestjs-pino"
---

<objective>
Verify every new package is a legitimate npm artifact (not a squatted or AI-generated fake), then install all 10 runtime dependencies into packages/backend. This plan is the blocking prerequisite for every other Phase 3 plan — nothing can be built until packages are on disk and TypeScript types resolve.

Purpose: Prevent supply-chain injection via [ASSUMED]-tagged packages that have not yet been verified against the npm registry and their canonical GitHub repositories.
Output: Updated packages/backend/package.json with all 10 dependencies; node_modules populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-platform-kernel-observability-validation-security-health/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy for all 10 [ASSUMED] packages</name>
  <what-built>
    The RESEARCH.md Package Legitimacy Audit lists 10 packages all tagged [ASSUMED] because slopcheck was unavailable. Every package must be manually confirmed before installation.
  </what-built>
  <how-to-verify>
    For each package below, open the npm page and confirm: (a) the GitHub repository URL shown on the npm page matches the official organisation listed here, (b) weekly download count is above 100,000, (c) last publish date is within the past 24 months.

    1. nestjs-pino — npmjs.com/package/nestjs-pino — must link to github.com/iamolegga/nestjs-pino
    2. pino — npmjs.com/package/pino — must link to github.com/pinojs/pino
    3. pino-http — npmjs.com/package/pino-http — must link to github.com/pinojs/pino-http
    4. nestjs-cls — npmjs.com/package/nestjs-cls — must link to github.com/Papooch/nestjs-cls
    5. @nestjs/terminus — npmjs.com/package/@nestjs/terminus — must link to github.com/nestjs/terminus
    6. @nestjs/swagger — npmjs.com/package/@nestjs/swagger — must link to github.com/nestjs/swagger
    7. @nestjs/throttler — npmjs.com/package/@nestjs/throttler — must link to github.com/nestjs/throttler
    8. helmet — npmjs.com/package/helmet — must link to github.com/helmetjs/helmet
    9. class-validator — npmjs.com/package/class-validator — must link to github.com/typestack/class-validator
    10. class-transformer — npmjs.com/package/class-transformer — must link to github.com/typestack/class-transformer

    Any package failing check (a), (b), or (c) MUST be flagged before proceeding. Do NOT install flagged packages.
  </how-to-verify>
  <resume-signal>Type "approved" once all 10 packages are verified, or list any failed packages with the reason.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install all 10 runtime packages into @repo/backend</name>
  <read_first>
    packages/backend/package.json — read current dependencies before modifying; note existing entries so the install command does not duplicate them.
    .planning/phases/03-platform-kernel-observability-validation-security-health/03-RESEARCH.md — Standard Stack table lists the exact packages and their verified versions (all [ASSUMED] are now human-verified in Task 1).
  </read_first>
  <files>packages/backend/package.json</files>
  <action>
    Run: pnpm --filter @repo/backend add nestjs-pino pino pino-http nestjs-cls @nestjs/terminus @nestjs/swagger @nestjs/throttler helmet class-validator class-transformer

    After install, run: pnpm --filter @repo/backend test run
    This confirms all existing Phase 2 tests still pass with the new packages on disk.

    Run: pnpm --filter @repo/backend build
    This confirms TypeScript resolves the new packages' type declarations without errors.

    Do NOT install pino-pretty (development pretty-printer) as a runtime dependency — it is not needed for structured JSON output and would increase the production bundle. It may be added as a devDependency in a future phase if developers request it.

    Do NOT add @types/helmet — the helmet package ships its own TypeScript declarations.
  </action>
  <verify>
    <automated>pnpm --filter @repo/backend test run &amp;&amp; pnpm --filter @repo/backend build</automated>
  </verify>
  <done>
    packages/backend/package.json contains nestjs-pino, pino, pino-http, nestjs-cls, @nestjs/terminus, @nestjs/swagger, @nestjs/throttler, helmet, class-validator, class-transformer in the dependencies section. All existing Phase 2 tests pass. pnpm --filter @repo/backend build exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted package content crosses here; human verification gates this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm install of 10 [ASSUMED] packages | mitigate | Blocking checkpoint:human-verify Task 1 requires manual npm page confirmation before install proceeds; slopcheck bypass documented |
| T-03-01 | Information Disclosure | pino redaction (future Plan 02) | mitigate | Deny-list redaction configured in Plan 02; no sensitive fields logged |
</threat_model>

<verification>
After Task 2: pnpm --filter @repo/backend test run exits 0 (all Phase 2 tests green). pnpm --filter @repo/backend build exits 0 (TypeScript compiles with new packages).
</verification>

<success_criteria>
All 10 packages verified as legitimate by human review and installed with no npm resolution errors. pnpm --filter @repo/backend test run exits 0. pnpm --filter @repo/backend build exits 0.
</success_criteria>

<output>
Create .planning/phases/03-platform-kernel-observability-validation-security-health/03-01-SUMMARY.md when done.
</output>
