---
phase: 04-authentication-entra-id-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/package.json
  - packages/backend/src/config/env.schema.ts
  - packages/backend/src/auth/current-user.type.ts
  - packages/backend/src/auth/token-validator.ts
  - packages/backend/src/auth/decorators/public.decorator.ts
  - packages/backend/src/auth/decorators/current-user.decorator.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "envSchema rejects NODE_ENV=production + AUTH_MODE=stub with a ZodError (fail-fast at startup)"
    - "envSchema rejects AUTH_MODE=entra when any ENTRA_TENANT_ID / ENTRA_CLIENT_ID / ENTRA_AUDIENCE is absent"
    - "envSchema accepts AUTH_MODE=stub (default) with no ENTRA_* vars present"
    - "TokenValidator is exported as an abstract class (not interface) from token-validator.ts"
    - "IS_PUBLIC_KEY constant and Public decorator factory are both exported from public.decorator.ts"
    - "GetCurrentUser param decorator reads request.user and returns null when absent"
    - "All 5 runtime packages and 3 dev-type packages appear in packages/backend/package.json after install"
  artifacts:
    - path: "packages/backend/src/config/env.schema.ts"
      provides: "Extended Zod env schema with AUTH_MODE enum + ENTRA_* optional fields + superRefine chain"
      contains: "superRefine"
    - path: "packages/backend/src/auth/current-user.type.ts"
      provides: "CurrentUser interface: entraId, email, tenantId, displayName"
    - path: "packages/backend/src/auth/token-validator.ts"
      provides: "abstract class TokenValidator with abstract validate(rawToken: string): Promise<CurrentUser>"
    - path: "packages/backend/src/auth/decorators/public.decorator.ts"
      provides: "IS_PUBLIC_KEY constant + Public decorator factory"
    - path: "packages/backend/src/auth/decorators/current-user.decorator.ts"
      provides: "GetCurrentUser param decorator"
  key_links:
    - from: "packages/backend/src/config/env.schema.ts"
      to: "packages/backend/src/auth/auth.module.ts"
      via: "AUTH_MODE env var drives conditional TokenValidator selection in Plan 02"
      pattern: "AUTH_MODE"
    - from: "packages/backend/src/auth/token-validator.ts"
      to: "packages/backend/src/auth/entra-token-validator.ts"
      via: "abstract class DI token — same pattern as IAuditContextProvider"
      pattern: "extends TokenValidator"
---

<objective>
Install auth library dependencies, extend the Zod env schema with AUTH_MODE and ENTRA_* vars
(including the production-guard superRefine), and create the four type-contract files that Plans 02
and 03 build against: CurrentUser type, TokenValidator abstract class, @Public() decorator, and
@GetCurrentUser() param decorator.

Purpose: Establishes the seam contracts and schema validation before any implementation code is
written. Plans 02 and 03 import from these files — they must exist first.

Output: 6 files created/modified; npm install completes; TypeScript compiles clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-authentication-entra-id-infrastructure/04-CONTEXT.md
@.planning/phases/04-authentication-entra-id-infrastructure/04-RESEARCH.md
@.planning/phases/04-authentication-entra-id-infrastructure/04-PATTERNS.md

<interfaces>
<!-- Key contracts the executor must follow precisely. Extracted from codebase. -->

From packages/backend/src/config/env.schema.ts (current — to be extended in Task 2):
```
export const envSchema = z.object({
  NODE_ENV: z.enum(['development', 'production', 'test']).default('development'),
  PORT: z.coerce.number().int().min(1).max(65535).default(3000),
  DATABASE_URL: z.string().url('DATABASE_URL must be a valid URL'),
  CORS_ORIGINS: z.string().min(1, 'CORS_ORIGINS must be a non-empty comma-separated list'),
  LOG_LEVEL: z.enum(['fatal', 'error', 'warn', 'info', 'debug', 'trace']).default('info'),
  THROTTLER_TTL_SECONDS: z.coerce.number().int().positive().default(60),
  THROTTLER_LIMIT: z.coerce.number().int().positive().default(100),
});
export type Env = z.infer<typeof envSchema>;
```
Task 2 extends this z.object({}) in-place and chains .superRefine() before export type Env.

From packages/backend/src/audit/audit-context-provider.interface.ts (abstract-class-as-DI-token analog):
```
export abstract class IAuditContextProvider {
  abstract getContext(): AuditContext | null;
}
```
TokenValidator follows the same pattern exactly — abstract class, not interface.

From packages/backend/src/common/interceptors/raw-response.decorator.ts (SetMetadata decorator analog):
```
export const RAW_RESPONSE_KEY = 'RAW_RESPONSE';
export const RawResponse = (): MethodDecorator & ClassDecorator =>
  SetMetadata(RAW_RESPONSE_KEY, true);
```
public.decorator.ts follows this exact pattern (export key + export decorator factory).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install auth runtime and dev-type packages</name>
  <files>packages/backend/package.json</files>

  <read_first>
    - packages/backend/package.json — confirm none of the packages are already listed
    - .planning/phases/04-authentication-entra-id-infrastructure/04-RESEARCH.md §
      "Package Legitimacy Audit" — all 8 packages are [OK]; no blocking checkpoint needed
  </read_first>

  <action>
    Run two npm install commands (workspace syntax confirmed from Phase 3 STATE.md D-01: project
    uses npm@10.9.8 workspaces, not pnpm).

    Runtime install (saves to "dependencies"):
      npm install --workspace=packages/backend \
        @nestjs/passport@11.0.5 passport@0.7.0 passport-jwt@4.0.1 \
        jwks-rsa@4.1.0 jsonwebtoken@9.0.3

    Dev-type install (saves to "devDependencies"):
      npm install --workspace=packages/backend --save-dev \
        @types/passport-jwt@4.0.1 @types/passport@1.0.17 @types/jsonwebtoken@9.0.10

    All 8 packages passed slopcheck [OK] (no postinstall scripts; verified in RESEARCH.md).
    No blocking human checkpoint is required — slopcheck gate already cleared by the researcher.

    After install, verify packages/backend/package.json lists all 5 runtime deps under
    "dependencies" and all 3 @types/* under "devDependencies".
  </action>

  <verify>
    <automated>
      grep -c "nestjs/passport" packages/backend/package.json
    </automated>
  </verify>

  <acceptance_criteria>
    - packages/backend/package.json "dependencies" contains "@nestjs/passport", "passport",
      "passport-jwt", "jwks-rsa", "jsonwebtoken"
    - packages/backend/package.json "devDependencies" contains "@types/passport-jwt",
      "@types/passport", "@types/jsonwebtoken"
    - npm install exits 0 (no peer-dep errors for NestJS 11)
  </acceptance_criteria>

  <done>
    All 8 packages installed and listed in packages/backend/package.json at the pinned versions
    from RESEARCH.md. npm install exits 0 with no peer-dependency errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend env.schema.ts with AUTH_MODE + ENTRA_* + superRefine</name>
  <files>packages/backend/src/config/env.schema.ts</files>

  <read_first>
    - packages/backend/src/config/env.schema.ts — current schema (lines 1-14); extend in-place
    - packages/backend/src/config/env.schema.spec.ts — existing tests (must remain green);
      tests 1 and 7 assert on the full parsed object shape — adding fields with defaults will
      break these assertions if AUTH_MODE is now included in the expected result
    - .planning/phases/04-authentication-entra-id-infrastructure/04-PATTERNS.md §
      "src/config/env.schema.ts (modify)" — exact field patterns and superRefine shape (lines 406-469)
    - node_modules/zod/lib/ZodError.d.ts — verify whether ZodIssueCode.custom is a string literal
      'custom' or an enum member; use the string literal 'custom' (RESEARCH.md flags ZodIssueCode
      as ASSUMED in Zod v4; string literal is safe in either version)
  </read_first>

  <behavior>
    - Test A: envSchema.parse({ DATABASE_URL, CORS_ORIGINS, NODE_ENV: 'test' }) — no AUTH_MODE
      supplied — result.AUTH_MODE === 'stub' (default)
    - Test B: envSchema.parse({ ..., AUTH_MODE: 'entra' }) throws ZodError because ENTRA_TENANT_ID
      is absent (superRefine required-when-entra check)
    - Test C: envSchema.parse({ ..., AUTH_MODE: 'entra', ENTRA_TENANT_ID: 'x',
      ENTRA_CLIENT_ID: 'y', ENTRA_AUDIENCE: 'z' }) succeeds
    - Test D: envSchema.parse({ ..., NODE_ENV: 'production', AUTH_MODE: 'stub' }) throws ZodError
      (superRefine production-stub guard)
    - Test E: existing tests 1-10 in env.schema.spec.ts must all still pass — update test 1 and
      test 7 expected objects to include AUTH_MODE: 'stub' in the result shape if those tests
      assert full object equality
  </behavior>

  <action>
    Extend the existing z.object({}) in env.schema.ts in-place by adding four new fields inside
    the existing object literal:

    AUTH_MODE: z.enum(['stub', 'entra']).default('stub')
    ENTRA_TENANT_ID: z.string().min(1, 'ENTRA_TENANT_ID must be non-empty').optional()
    ENTRA_CLIENT_ID: z.string().min(1, 'ENTRA_CLIENT_ID must be non-empty').optional()
    ENTRA_AUDIENCE: z.string().min(1, 'ENTRA_AUDIENCE must be non-empty').optional()

    Then chain .superRefine((data, ctx) => { ... }) immediately after the closing brace of
    z.object({}) before the semicolon. The superRefine callback must call ctx.addIssue with
    code: 'custom' (string literal — do NOT use z.ZodIssueCode.custom) for two cases:
    (1) data.NODE_ENV === 'production' && data.AUTH_MODE === 'stub' — path: ['AUTH_MODE']
    (2) data.AUTH_MODE === 'entra' and any of ENTRA_TENANT_ID / ENTRA_CLIENT_ID / ENTRA_AUDIENCE
        is absent — addIssue for each missing key individually, path: [key]

    Move the existing `export type Env = z.infer<typeof envSchema>` line to AFTER the .superRefine()
    chain so the inferred type includes all new optional fields.

    After modifying env.schema.ts, check env.schema.spec.ts: existing Test 1 and Test 7 assert
    exact object equality with toEqual(). Since AUTH_MODE now has a default of 'stub', the parsed
    result will include AUTH_MODE: 'stub'. Update those two test expected objects to include
    AUTH_MODE: 'stub' so they remain green (no other test changes required — the behavior contract
    of tests 2-6, 8-10 is unchanged).

    Do NOT remove the existing `import { z } from 'zod'` line. Do not add new imports.
    Do not change any existing field definitions.
  </action>

  <verify>
    <automated>npm run lint --workspace=packages/backend && npm run test --workspace=packages/backend -- --reporter verbose env.schema</automated>
  </verify>

  <acceptance_criteria>
    - envSchema.parse({ DATABASE_URL: 'postgresql://x', CORS_ORIGINS: 'http://localhost' }) returns
      object with AUTH_MODE: 'stub' (default applied)
    - envSchema.parse({ ..., AUTH_MODE: 'stub', NODE_ENV: 'production' }) throws ZodError
    - envSchema.parse({ ..., AUTH_MODE: 'entra' }) throws ZodError (missing ENTRA_* vars)
    - envSchema.parse({ ..., AUTH_MODE: 'entra', ENTRA_TENANT_ID: 'a', ENTRA_CLIENT_ID: 'b',
      ENTRA_AUDIENCE: 'c' }) succeeds
    - All 10 existing env.schema.spec.ts tests pass (npm test exits 0)
    - export type Env = z.infer<typeof envSchema> includes AUTH_MODE and optional ENTRA_* keys
  </acceptance_criteria>

  <done>
    env.schema.ts exports the extended envSchema with AUTH_MODE, ENTRA_* fields, and the
    superRefine production-guard. All 10 pre-existing env.schema spec tests pass; the Env type
    includes the new fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create type contract files in src/auth/</name>
  <files>
    packages/backend/src/auth/current-user.type.ts,
    packages/backend/src/auth/token-validator.ts,
    packages/backend/src/auth/decorators/public.decorator.ts,
    packages/backend/src/auth/decorators/current-user.decorator.ts
  </files>

  <read_first>
    - packages/backend/src/audit/audit-context-provider.interface.ts — abstract-class-as-DI-token
      pattern: export abstract class IAuditContextProvider { abstract getContext(): X | null; }
      Token-Validator.ts follows this exactly.
    - packages/backend/src/common/interceptors/raw-response.decorator.ts — SetMetadata key +
      decorator factory pattern: export const KEY = 'string'; export const Decorator = () =>
      SetMetadata(KEY, value). public.decorator.ts follows this exactly.
    - .planning/phases/04-authentication-entra-id-infrastructure/04-PATTERNS.md §
      "src/auth/token-validator.ts" (lines 30-65),
      § "src/auth/current-user.type.ts" (lines 67-91),
      § "src/auth/public.decorator.ts" (lines 234-269),
      § "src/auth/current-user.decorator.ts" (lines 272-294)
    - .planning/phases/04-authentication-entra-id-infrastructure/04-CONTEXT.md § Specific Ideas
      (D-01, D-03 corrected, D-06) for exact CurrentUser field names
  </read_first>

  <action>
    Create four files. Create the src/auth/ and src/auth/decorators/ directories if absent.

    FILE 1 — packages/backend/src/auth/current-user.type.ts:
    Export the CurrentUser interface with exactly these four fields:
      entraId: string  — maps from Entra oid claim (immutable; recommended cross-app key)
      email: string    — maps from preferred_username ?? email (v2.0; NOT upn — v1.0 only)
      tenantId: string — maps from tid claim
      displayName?: string | null  — maps from name claim (nullable, requires profile scope)
    Add a JSDoc comment referencing D-01 and the D-03 correction (preferred_username ?? email).

    FILE 2 — packages/backend/src/auth/token-validator.ts:
    Import CurrentUser from './current-user.type'. Export abstract class TokenValidator with one
    abstract method: abstract validate(rawToken: string): Promise<CurrentUser>.
    Add JSDoc: "Abstract class used as NestJS DI token. Implementations: EntraTokenValidator
    (AUTH_MODE=entra) and StubTokenValidator (AUTH_MODE=stub). Per D-02: never returns roles
    or permissions — Phase 5 concern."
    Do NOT use interface — NestJS DI cannot inject TypeScript interfaces at runtime.

    FILE 3 — packages/backend/src/auth/decorators/public.decorator.ts:
    Import SetMetadata from '@nestjs/common'. Export IS_PUBLIC_KEY = 'isPublic' as a const string.
    Export Public = (): MethodDecorator & ClassDecorator => SetMetadata(IS_PUBLIC_KEY, true).
    The guard imports IS_PUBLIC_KEY from this file (not a magic string) — consistent with
    RAW_RESPONSE_KEY / AUDIT_KEY / IDEMPOTENCY_KEY_META convention in the codebase.

    FILE 4 — packages/backend/src/auth/decorators/current-user.decorator.ts:
    Import createParamDecorator and ExecutionContext from '@nestjs/common'. Import type CurrentUser
    from '../current-user.type' (use import type to avoid circular-import risk). Export
    GetCurrentUser = createParamDecorator((_: unknown, ctx: ExecutionContext): CurrentUser | null =>
    ctx.switchToHttp().getRequest<{ user?: CurrentUser }>().user ?? null).
    Name the export GetCurrentUser (not CurrentUser) to avoid shadowing the type import at call
    sites; handlers write @GetCurrentUser() user: CurrentUser | null.

    No barrel index.ts required — consumers import directly from the file path.
  </action>

  <verify>
    <automated>npm run lint --workspace=packages/backend && npx tsc --noEmit -p packages/backend/tsconfig.json</automated>
  </verify>

  <acceptance_criteria>
    - packages/backend/src/auth/current-user.type.ts exists and exports interface CurrentUser
      with entraId, email, tenantId, displayName fields
    - packages/backend/src/auth/token-validator.ts exports abstract class TokenValidator (not
      interface) with abstract validate(rawToken: string): Promise<CurrentUser>
    - packages/backend/src/auth/decorators/public.decorator.ts exports IS_PUBLIC_KEY = 'isPublic'
      AND Public decorator factory
    - packages/backend/src/auth/decorators/current-user.decorator.ts exports GetCurrentUser
      createParamDecorator that returns request.user ?? null
    - npx tsc --noEmit exits 0 — no TS errors across all four new files
    - npm run test --workspace=packages/backend exits 0 — no existing tests broken
  </acceptance_criteria>

  <done>
    Four contract files exist in src/auth/ and src/auth/decorators/. TypeScript compiles clean.
    No existing tests are broken.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → Zod schema | Untrusted env values enter the app; Zod schema must reject unsafe combinations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | `env.schema.ts` AUTH_MODE field | mitigate | Zod `.superRefine()` chain (Task 2) uses `code: 'custom'` to reject `NODE_ENV=production` + `AUTH_MODE=stub` at startup before any request is processed |
| T-04-05 | Information Disclosure | pinoHttp logger in `app.module.ts` | accept | `Authorization` header already in `redact.paths` (Phase 3 D-13, `app.module.ts` line 46). ENTRA_* env vars are only read by AppConfigService — they never pass through the pino serializer. No token fields are logged in the type-contract files this plan creates. |
| T-04-SC | Tampering | npm installs (Task 1) | accept | All 8 packages verified [OK] in RESEARCH.md Package Legitimacy Audit (no postinstall scripts, no [ASSUMED] or [SUS] packages). Blocking human checkpoint is NOT required — slopcheck gate already cleared by the researcher pre-planning. |
</threat_model>

<verification>
After completing all three tasks in order:

1. packages/backend/package.json lists all 5 runtime auth packages and 3 dev @types/* packages
2. npx tsc --noEmit -p packages/backend/tsconfig.json exits 0
3. npm run test --workspace=packages/backend exits 0 (all existing tests pass)
4. grep -r "superRefine" packages/backend/src/config/env.schema.ts returns a match
5. grep -r "abstract class TokenValidator" packages/backend/src/auth/ returns a match
6. grep -r "IS_PUBLIC_KEY" packages/backend/src/auth/decorators/public.decorator.ts returns a match
</verification>

<success_criteria>
- All 8 auth packages installed and appear in packages/backend/package.json
- env.schema.ts extended in-place with AUTH_MODE (default 'stub'), ENTRA_* optional fields,
  and superRefine that rejects stub-in-production and entra-without-vars at startup
- TokenValidator exported as abstract class (NestJS DI requirement — interfaces are erased)
- IS_PUBLIC_KEY string constant exported alongside @Public() factory (no magic strings in guard)
- GetCurrentUser param decorator reads request.user (null-safe return)
- npx tsc --noEmit clean; npm test exits 0
</success_criteria>

<output>
Create `.planning/phases/04-authentication-entra-id-infrastructure/04-01-SUMMARY.md` when done.
</output>
